# UEBA 用户行为日志可疑事件与异常场景预研（非网络日志为主）

> 日期：2026-09-08；状态：预研设计，尚未接入真实 AD/Keycloak 环境验证。
> 基础文档：[Zeek 版预研报告](/Users/wayne/Documents/code/UEBA/UEBA可疑事件与异常场景预研_Zeek版_2026-09-07.md)。
> 本报告新增用户行为视角，并保留 Zeek 作为可选关联证据，不要求每个事件依赖网络日志。

## 1. 范围和结论

本期研究 Windows AD 域控、Windows 目标主机登录、Keycloak、Linux 登录，以及可选 VPN、NAC、堡垒机的认证和会话审计。包含账号生命周期、权限、认证配置和身份审计管理操作；暂不考虑命令、sudo、服务和计划任务审计。

**暂不研究文件访问/读写/下载/压缩/共享/外传、对象存储、云 IAM、SaaS 应用内操作及云应用审计。** Keycloak 的 clientId 只用于定位认证入口，不扩展为应用内数据访问。身份目录中的账号、角色、委派属于本期；文件目录和文件 ACL 不属于本期。审计关闭和 Security 日志清除按系统管理事件纳入。

用户行为日志回答“哪个身份在哪个系统认证、获得什么权限、发起什么操作”。这里的身份可能是人员账号、服务账号或管理员代理身份，不能自动等同于真实人员。共享账号和服务身份需单独建模。

新事件使用独立编号 **UE-001～UE-027**，场景使用 **US-001～US-022**。与 Zeek 的编号仅放在映射说明中，既不继承也不替换原编号。既有多源版包含本期排除的内容，不直接复制其全部范围。

## 2. 数据源、原生字段与采集前提

### 2.1 Windows / AD

- Windows 事件共同字段为 System 下的 Provider、EventID、Version、Computer、Channel、EventRecordID、TimeCreated.SystemTime。下面检测卡中的其他字段为相应 EventData 字段；不能假定每个事件都有全部字段。
- 域控采集 4768/4769/4771（Kerberos）、4776（凭据验证）、账号/组管理及目录服务事件；目标主机采集 4624/4625/4634 等登录/注销及审计策略事件。
- AD 安全日志与成员服务器安全日志不是同一个观测点。4776 成功代表凭据验证，4769 成功代表服务票据签发，4624 代表目标系统建立登录会话。三者保留不同结果范围。
- 按需启用高级审计策略；目录服务 5136/4662 还依赖对应对象和操作的 SACL。本期无需接入进程、PowerShell、Sysmon、服务和任务日志。
- 4624 的 TargetUserSid/TargetLogonId 指新登录身份；管理事件中的 SubjectUserSid 通常指操作者。组事件中的 MemberSid 是成员，TargetSid 是组。[Microsoft 事件采集说明](https://learn.microsoft.com/en-us/defender-for-identity/deploy/event-collection-overview)、[4776](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4776)。

### 2.2 Keycloak：用户事件和管理员事件分开

| 日志类型 | 原生字段 | 用途与前提 |
|---|---|---|
| 用户 Events | time、type、realmId、clientId、userId、sessionId、ipAddress、error、details；id 是否存在按部署版本验证 | 登录、注销、刷新等；需启用目标事件保存或事件监听器，成功和失败都应验证 |
| Admin Events | time、realmId、authDetails、operationType、resourceType、resourcePath、representation、error；id/details 按版本验证 | 管理操作；authDetails.userId 是操作者，目标常需从 resourcePath 解析 |
| 用户/角色/认证流配置快照 | 用户 enabled、角色映射、复合角色、组继承、认证流/客户端配置 | 用于解释状态和差异；快照是辅助上下文，不是已经发生的操作日志 |
| 扩展认证证据 | 策略版本、实际执行因子、上游认证信息、可信 acr/amr、流程执行关联ID | 仅 MFA 判定等检测需要；不是保证存在于标准 LOGIN 的字段 |

REST 采集入口为 `/admin/realms/{realm}/events` 和 `/admin/realms/{realm}/admin-events`，realm 路径参数和事件 realmId 的含义应由适配器统一。事件 time 按接口定义转换为 UTC；常见 REST 表示使用毫秒，不能与 Zeek 秒时间戳直接比较。

details 键、错误枚举和操作 resourcePath 必须在部署版本采集样本后适配；“字段未出现”不等于“未执行该认证步骤”。representation 是可选操作表示，不能当作完整变更前后值；如需读取它，应避免采入密码、客户端秘密或令牌。[Keycloak REST schema](https://www.keycloak.org/docs-api/latest/rest-api/index.html)、[用户事件结构](https://www.keycloak.org/docs-api/latest/javadocs/org/keycloak/representations/idm/EventRepresentation.html)、[管理员事件结构](https://www.keycloak.org/docs-api/latest/javadocs/org/keycloak/representations/idm/AdminEventRepresentation.html)。

Keycloak 可进行 LDAP/AD 联邦认证，但一次 LDAP 查询不必对应一次真实人员登录。失败可能没有 userId；不同 realm 同名账号也不能自动合并。事件保存、监听器与留存策略参见 [Keycloak 管理指南](https://www.keycloak.org/docs/latest/server_admin/index.html)。

### 2.3 Linux、VPN/NAC、堡垒机

Linux sshd/PAM 仅采集登录、身份验证和会话开始/结束信息，解析为主体、来源、会话、动作和结果；不采集命令或sudo操作。VPN/NAC/堡垒机产品未指定，使用规范字段及配置化原生字段映射。堡垒机本期只使用认证和会话审计，不使用会话内命令回放。

## 3. 判定和身份关联约定

R=明确规则，Q=时间窗聚合/序列，B=历史基线，S=多个特征评分。同一卡片有两个分支时用 reason_code 区分，不将多个分支重复记为独立攻击。

所有阈值均为起始建议，并非实测有效阈值。基线使用已知正常的同身份/同角色历史；28天、100样本只是起始门槛，不保证每个星期小时槽都足够。样本不足时回退同角色或只运行规则；MAD为0需要尺度下限，不能除零或对极小变化过度告警。

- 账号键：AD 为目录域/租户加 SID；Keycloak 为 realmId+userId；Linux 为 host.id+本地UID并保留账号生命周期。跨系统通过可信联邦映射/目录属性建立 person_id，禁止仅凭同名或邮箱字符串硬合并。
- 操作者 actor 与被操作主体 subject 分开；例如帮助台给 alice 重置密码，actor 是帮助台，subject 是 alice。
- Windows LogonId 仅在主机及启动周期内使用；Keycloak sessionId 加 realm，Linux会话ID加主机及启动上下文。不能把这些局部ID当全企业唯一ID。
- IP归属必须按事件时间关联DHCP/VPN/NAC/NAT。未知归属保留 unknown，不填成上一个用户。
- Keycloak反向代理必须正确配置可信客户端IP；不直接相信任意客户端传入的转发头。
- 成功、失败、被拒绝、结果未知分开。异步刷新、缓存登录和已有SSO会话也单独标记。
- 没有匹配审批时称“未匹配审批”；只有明确违反授权矩阵才能称“非授权”。不把正常运维仅凭缺工单直接判攻击。

## 4. 可疑事件清单

每张卡依次说明通俗行为、日志字段、判断步骤、关联方式和排除条件。以检测卡代替超宽表格，便于同时阅读业务解释与实施细节。

### UE-001 密码喷洒

对很多账号各试少量登录，可能在批量猜测弱密码。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | Windows Security 4625：TargetUserName、TargetDomainName、IpAddress、Status、SubStatus；Keycloak 用户事件：type、realmId、clientId、ipAddress、userId、details.username、error。 |
| 详细起始判据 | 按身份域+认证入口+来源分组，10分钟密码类失败账号≥10，且每账号1～3次；缺失账号标识的记录不参与账号去重。排除MFA失败、过期会话等非密码错误。 |
| 关联日志、字段及关联方式 | 4625的IpAddress关联设备历史；4776只有Workstation等字段，不能假造来源IP；Keycloak失败userId可能为空，使用realm内提交的用户名作低置信标识。 |
| 常见正常原因及误报排除条件 | 代理/NAT出口、多账号批处理旧密码；不能从这类日志证明尝试的密码相同。 |
| 与 Zeek 的关系 | ZE-003 |

### UE-002 单账号反复认证失败

同一账号短时间反复登录失败，可能猜密码，也可能一直使用旧密码。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | Windows 4625字段同上；DC 4771：TargetUserName、IpAddress、Status；4776：TargetUserName、Workstation、Status；Keycloak type=LOGIN_ERROR、error、userId、details。 |
| 详细起始判据 | 同身份域+账号+认证入口10分钟密码类失败≥10；按错误原因和来源分别统计。4771正常预认证协商不计密码失败；4776用Status判定验证结果。 |
| 关联日志、字段及关联方式 | 同次请求的多源记录归入观察组；先按各认证入口检测，不把DC验证和目标登录机械相加。 |
| 常见正常原因及误报排除条件 | 密码变更后的服务重试、时钟偏差、认证服务器故障。 |
| 与 Zeek 的关系 | ZE-002、ZE-004 |

### UE-003 连续失败后认证成功

账号失败多次后终于成功，需要核实是本人重试还是凭据被猜中。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | Windows 4625→4624：TargetUserSid/TargetUserName、IpAddress、LogonType、TargetLogonId；Keycloak LOGIN_ERROR→LOGIN：realmId、userId、clientId、sessionId、ipAddress。 |
| 详细起始判据 | 同账号和入口30分钟内≥5次密码类失败，随后同来源出现成功；跨来源仅作为弱关联。成功账号必须出现在前序失败集合中。 |
| 关联日志、字段及关联方式 | 4624成功是目标系统登录；4776成功只是凭据验证，4769成功只是票据签发。分开保存outcome_scope。 |
| 常见正常原因及误报排除条件 | 本人输错密码；不能直接宣布账号失陷。 |
| 与 Zeek 的关系 | ZE-005 |

### UE-004 服务账号交互式登录

只供程序使用的账号被人拿来登录桌面或交互终端。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | Windows 4624：TargetUserSid、LogonType、TargetLogonId、IpAddress；Linux sshd/PAM：账号、认证结果、会话、TTY；账号目录：service_account。 |
| 详细起始判据 | 账号被明确标为服务账号，4624 LogonType为2或10即可触发；Linux须有交互会话/TTY证据，单纯SSH认证不等于交互登录。 |
| 关联日志、字段及关联方式 | 账号标签按SID或目录userId关联；Windows会话键为Computer+启动周期+TargetLogonId。 |
| 常见正常原因及误报排除条件 | 批准应急操作；类型3网络登录、4批处理、5服务启动不能算桌面登录。 |
| 与 Zeek 的关系 | 无直接等价事件 |

### UE-005 停用账号出现新认证成功

已停用的账号又通过了新的身份验证，需要核查状态同步或访问控制。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | Windows 4725/4722：TargetSid；4624：TargetUserSid、LogonType、AuthenticationPackageName；Keycloak管理员事件目标用户enabled变更；用户事件LOGIN。 |
| 详细起始判据 | 构造账号停用/启用有效时间区间；成功时间落在停用区间且超出配置的复制宽限。必须区分新认证、缓存登录、已有SSO会话和刷新令牌。 |
| 关联日志、字段及关联方式 | 按SID或realmId+userId关联状态历史；实时快照不能证明过去已停用。HR状态仅作背景，不自动等同认证系统停用。 |
| 常见正常原因及误报排除条件 | 跨DC复制延迟、缓存登录；已有会话继续使用另行调查，不命名为新密码认证成功。 |
| 与 Zeek 的关系 | Zeek认证可作旁证 |

### UE-006 非常用时间认证

账号在与自身或岗位习惯明显不同的时间使用系统。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | B |
| 主判断日志及字段 | Windows 4624 TimeCreated、TargetUserSid；Keycloak time、userId、clientId、type；Linux成功登录时间和账号。 |
| 详细起始判据 | 成功记录映射到用户本地工作日/小时；历史≥28天且有效登录≥100，当前时间槽占比<1%形成线索；同岗位也罕见可提高优先级。 |
| 关联日志、字段及关联方式 | 班次、值班和时区是配置上下文；基线以逻辑认证记录计数。 |
| 常见正常原因及误报排除条件 | 出差、值班、月末作业；新账号样本不足不评个人基线。 |
| 与 Zeek 的关系 | ZE-007的时间分支 |

### UE-007 来源设备或网段异常

账号从从未使用过的设备或不常用网络进入系统。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | B |
| 主判断日志及字段 | 4624：IpAddress、WorkstationName、TargetUserSid；Keycloak ipAddress、userId；VPN/NAC：设备ID、分配IP、会话开始结束（厂商映射字段）。 |
| 详细起始判据 | 同账号30天未见来源设备/网段；稳定设备标识缺失时仅输出“新来源IP/网段”，不能输出“新设备”。 |
| 关联日志、字段及关联方式 | IP按事件时刻关联VPN/NAC/DHCP；WorkstationName可缺失或不可信。 |
| 常见正常原因及误报排除条件 | 共享出口、移动办公、代理；IP变化不代表设备变化。 |
| 与 Zeek 的关系 | ZE-007、ZE-021的来源分支 |

### UE-008 禁止或罕见认证方式

账号使用了策略不允许或很少使用的认证方式。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R/B |
| 主判断日志及字段 | 4624：AuthenticationPackageName、LmPackageName、LogonType；Keycloak details中的实际可用认证上下文；审批策略。 |
| 详细起始判据 | 策略明确禁止且成功时按R触发；仅低频时按B输出线索，历史≥100次且方式占比<1%。两个分支分别保存reason_code。 |
| 关联日志、字段及关联方式 | NTLM网络证据关联ZE-006；Keycloak auth_method=openid-connect是协议，不能据此断言仅密码或已完成MFA。 |
| 常见正常原因及误报排除条件 | 系统升级、域认证回退；不能由未出现Kerberos推断使用NTLM。 |
| 与 Zeek 的关系 | ZE-006、ZE-013 |

### UE-009 异常并发会话

同一账号同时在不相容的位置或设备上持续使用。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | Windows 4624/4634、4778/4779：登录/注销/远程会话状态；Keycloak LOGIN/LOGOUT的sessionId；会话查询快照；VPN会话记录。 |
| 详细起始判据 | 同账号两个不同会话重叠≥5分钟，且来源设备或位置冲突。缺少注销以明确超时结束并标记估算，不无限延长。 |
| 关联日志、字段及关联方式 | 以会话ID及身份域关联；SSO同sessionId访问多个client不算多个独立登录。 |
| 常见正常原因及误报排除条件 | 多终端办公、远程桌面断开未注销、共享账号。 |
| 与 Zeek 的关系 | Zeek多连接不等于多会话 |

### UE-010 疑似不可能旅行

账号在短时间从地理距离很远的地方登录，可能由不同人员使用。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | IdP/VPN成功事件的时间、账号、可信客户端IP；GeoIP版本、位置和精度为外部富化字段。 |
| 详细起始判据 | 同账号相邻登录距离>500km且速度>900km/h作为起始线索；地址无法定位或来自已知代理时不评分。 |
| 关联日志、字段及关联方式 | 与会话和VPN出口映射关联，不能用网关位置当用户位置。 |
| 常见正常原因及误报排除条件 | VPN、运营商出口、GeoIP误差；单事件不直接判账号被盗。 |
| 与 Zeek 的关系 | 无直接等价事件 |

### UE-011 多个账号集中锁定

许多账号因认证问题被锁住，可能有批量猜测或错误程序。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | Windows 4740：TargetSid、TargetUserName、CallerComputerName；Keycloak锁定状态/版本支持的事件及状态快照。 |
| 详细起始判据 | 同CallerComputerName或已核实来源10分钟内不同锁定账号≥5。Keycloak缺少独立锁定事件时需先实现状态变化采集，不能把全部LOGIN_ERROR当锁定。 |
| 关联日志、字段及关联方式 | 关联此前UE-001/UE-002和目录状态。 |
| 常见正常原因及误报排除条件 | 批量改密后旧凭据、共享应用故障。 |
| 与 Zeek 的关系 | ZE-003、ZE-004可增强 |

### UE-012 未经批准重置他人密码

操作者替另一个账号更换密码，可能夺取该账号的使用权。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | Windows 4724：SubjectUserSid、SubjectLogonId、TargetSid、审计成功/失败；Keycloak Admin Events：authDetails.userId、resourcePath、operationType、error。 |
| 详细起始判据 | 确认密码重置操作成功，actor≠target且缺少匹配审批或权限范围不符；4723是更改密码，不能与4724混作同一行为。 |
| 关联日志、字段及关联方式 | Keycloak目标从已验证的users/{id}/reset-password路径适配提取；不读取或保存密码内容。 |
| 常见正常原因及误报排除条件 | 帮助台正常重置、账号恢复流程；日志缺少审批只能表述“未匹配审批”。 |
| 与 Zeek 的关系 | 无直接等价事件 |

### UE-013 未经批准获得高权限

账号加入管理员组或获得关键角色，能操作更多系统。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | AD 4728/4732/4756：SubjectUserSid、MemberSid、TargetSid；Keycloak Admin Events的角色/组映射操作和representation；角色快照。 |
| 详细起始判据 | 成功授予已定义的高权限组/角色，且无批准授权。Windows MemberSid是被加成员、TargetSid是组，不能把两者调换。 |
| 关联日志、字段及关联方式 | Keycloak按authDetails识别操作者、resourcePath识别目标；角色ID、复合角色和组继承通过快照计算有效权限。 |
| 常见正常原因及误报排除条件 | JIT临时授权、IAM同步；4672表示登录获得特殊权限，不等于新提权。 |
| 与 Zeek 的关系 | ZE-012只提供目录操作线索 |

### UE-014 新账号快速获得高权限

账号刚创建就成为管理员，需要核查创建目的。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | Windows 4720：TargetSid；4728/4732/4756：MemberSid、TargetSid；Keycloak用户CREATE及角色授予事件。 |
| 详细起始判据 | 创建后30分钟内同账号成功获关键权限；账户创建本身无需告警，创建与高权限授予关联后形成事件。 |
| 关联日志、字段及关联方式 | 以SID或realmId+userId关联创建、授予；分别保留两个操作者。 |
| 常见正常原因及误报排除条件 | 批准新管理员入职、自动部署账户。 |
| 与 Zeek 的关系 | 无直接等价事件 |

### UE-015 短时授权后撤回

账号短暂获得高权限，操作后又撤回，可能掩盖权限使用。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | AD加组4728/4732/4756及移出4729/4733/4757：MemberSid、TargetSid、SubjectUserSid；Keycloak角色映射增加/删除。 |
| 详细起始判据 | 同账号+同高权限组/角色2小时内先授予后撤回；单凭这一序列只判“短时授权”，中间有敏感操作才判“短时权限使用”。 |
| 关联日志、字段及关联方式 | 用目标账号的有效会话关联操作，不把授予者的操作当成被授权者操作。 |
| 常见正常原因及误报排除条件 | PAM/JIT正常短期授权；必须检查对应批准范围。 |
| 与 Zeek 的关系 | ZE-012可能观察到两次目录操作 |

### UE-016 新账号使用后删除

新建账号完成登录或操作后很快被删除，可能是临时账号滥用。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | AD 4720→4624→4726：TargetSid/TargetUserSid；Keycloak用户CREATE→LOGIN→DELETE。 |
| 详细起始判据 | 同账号2小时内依次创建、成功登录、删除；删除后还原旧SID/userId，不按复用用户名匹配。 |
| 关联日志、字段及关联方式 | 登录与操作通过有效session关联；创建、使用、删除操作者分别记录。 |
| 常见正常原因及误报排除条件 | 自动测试账户、一次性维护账户。 |
| 与 Zeek 的关系 | 无直接等价事件 |

### UE-017 目录关键权限或委派修改

有人改了谁可以管理账号或代表别人访问服务的配置。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | AD 5136：ObjectGUID、ObjectDN、AttributeLDAPDisplayName、AttributeValue、OperationType、OpCorrelationID、SubjectUserSid。 |
| 详细起始判据 | 对nTSecurityDescriptor、msDS-AllowedToDelegateTo、msDS-AllowedToActOnBehalfOfOtherIdentity等策略清单属性，重建删除/新增值并解析权限是否扩大；成功且无授权时触发。 |
| 关联日志、字段及关联方式 | 按ObjectGUID+属性+OpCorrelationID配对；仅有新值且无旧快照，输出“关键属性变更待核查”。5145是共享访问检查，不是ACL修改证据。 |
| 常见正常原因及误报排除条件 | 域管理变更、集群配置；要求Directory Service Changes及对象SACL。 |
| 与 Zeek 的关系 | ZE-012 |

### UE-018 非批准主体使用目录复制权限

普通主体使用域目录复制能力，可能在获取账号凭据相关数据。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | AD 4662：SubjectUserSid、SubjectLogonId、ObjectName、ObjectType、AccessMask、Properties、审计结果。 |
| 详细起始判据 | 成功Control Access（按AccessMask解释）且Properties命中目录复制权限GUID集合，目标为相关域目录对象，主体不在批准复制清单。GUID按部署schema/官方定义维护，不以任意4662判DCSync。 |
| 关联日志、字段及关联方式 | 同DC及启动周期内SubjectLogonId关联4624来源；再与Zeek drsuapi按主体/来源/目标/时间关联。 |
| 常见正常原因及误报排除条件 | 正常域控、身份同步、备份；只能确认权限使用，不凭该日志断言所有密码哈希已取走。 |
| 与 Zeek 的关系 | ZE-023 |

### UE-019 Keycloak认证策略被弱化

登录流程或客户端配置变得更容易通过，例如去掉必需认证步骤。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | Keycloak Admin Events：time、realmId、authDetails、operationType、resourceType、resourcePath、representation、error；认证流/客户端版本快照。 |
| 详细起始判据 | 成功UPDATE/DELETE等操作涉及authentication flows、bindings或客户端认证配置；差异显示必需步骤被禁用、允许不批准流程等，且无批准变更。 |
| 关联日志、字段及关联方式 | 按realm+配置对象ID关联前后快照；representation可能缺失且通常不是old/new完整对，不得凭UPDATE就认定弱化。 |
| 常见正常原因及误报排除条件 | 测试realm、获批流程迁移；客户端名称不作为安全边界。 |
| 与 Zeek 的关系 | Zeek无法观察TLS内配置变更 |

### UE-020 Keycloak成功会话未满足认证要求

用户进入系统，但实际完成的认证强度低于该入口要求。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R（条件具备后） |
| 主判断日志及字段 | Keycloak LOGIN：realmId、userId、clientId、sessionId；扩展采集的策略版本、实际认证步骤、acr/amr来源证明。 |
| 详细起始判据 | 成功且适用策略要求的因子/强度未被实际会话满足；必须把上游IdP认证、已有SSO会话和step-up纳入判定。普通LOGIN缺少MFA字段时标为不可检测。 |
| 关联日志、字段及关联方式 | sessionId+客户端+认证流程执行上下文关联，不能只取token请求参数当实际完成结果。 |
| 常见正常原因及误报排除条件 | 可信上游MFA、免重复验证策略；仅缺少amr或OTP事件不证明绕过。 |
| 与 Zeek 的关系 | Zeek无法单独判断 |

### UE-021 Keycloak认证器异常变更

账号的OTP或其他登录凭据被删除、替换或新增，可能影响账号控制权。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R/Q |
| 主判断日志及字段 | Keycloak用户/管理员事件：type或operationType、resourcePath、userId或authDetails.userId、sessionId、time、error；凭据元数据快照。 |
| 详细起始判据 | 确认受支持版本的凭据创建/删除/更新操作，提取目标、凭据类型和操作者；非本人或非批准帮助台修改高权限账号时触发。 |
| 关联日志、字段及关联方式 | 不用跨版本固定事件枚举；先在目标版本验证成功、失败和自助操作样本。仅保存类型/ID，不保存OTP种子或密钥。 |
| 常见正常原因及误报排除条件 | 换手机、遗失认证器、自助恢复。 |
| 与 Zeek 的关系 | 无直接等价事件 |

### UE-022 Keycloak异常身份代理登录

管理员代另一用户操作或登录，可能超出支持职责。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | Keycloak管理员IMPERSONATE操作（按版本实测）：authDetails.userId、resourcePath、realmId、time、error；用户事件details中可用的代理信息。 |
| 详细起始判据 | 代理操作成功且目标为敏感用户、操作者不在批准支持组或不在工单范围，单事件即可判代理使用异常。 |
| 关联日志、字段及关联方式 | actor与被代理subject分别存储；若没有可靠session映射，不将后续任意同用户登录归给代理者。 |
| 常见正常原因及误报排除条件 | 批准客服排障、自助测试；不能把被代理用户记录为发起人。 |
| 与 Zeek 的关系 | 无直接等价事件 |

### UE-023 Keycloak令牌刷新异常

同一会话反复刷新访问凭据，或从冲突来源使用会话，可能是配置故障或会话被复用。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q/B |
| 主判断日志及字段 | Keycloak用户事件REFRESH_TOKEN/REFRESH_TOKEN_ERROR（版本确认）：time、realmId、userId、clientId、sessionId、ipAddress、error、details。 |
| 详细起始判据 | 同session+client5分钟刷新≥100且高于其基线，或两个冲突来源持续交替使用并有会话证据；仅IP变化不定为令牌盗用。 |
| 关联日志、字段及关联方式 | sessionId只代表会话，不代表相同原始token；无可靠凭据关联ID时只称“会话刷新异常”。 |
| 常见正常原因及误报排除条件 | 多标签页、客户端重试、代理；不记录原始token。 |
| 与 Zeek 的关系 | 无直接等价事件 |

### UE-024 安全审计被关闭或日志被清除

系统用于记录操作的能力被削弱，或已有审计记录被清空。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | R |
| 主判断日志及字段 | Windows 4719：SubjectUserSid、SubjectLogonId、SubcategoryGuid、AuditPolicyChanges；1102：SubjectUserSid、SubjectUserName、SubjectLogonId；Keycloak审计配置管理员事件及前后快照。 |
| 详细起始判据 | 明确关闭所需审计类别、清除Security日志或关闭Keycloak用户/管理员事件保存，且无匹配批准记录。一般ERROR和无日志不证明人为关闭。 |
| 关联日志、字段及关联方式 | 按主体会话关联操作；外部采集心跳、配置快照作为旁证。本期包含审计清除系统事件，不包含普通文件删除审计。 |
| 常见正常原因及误报排除条件 | 维护、留存策略；正常轮转不等于清除安全审计。 |
| 与 Zeek 的关系 | ZE-051、ZE-052只有质量关联 |

### UE-025 账号跨多主机成功登录

一个账号短时间成功进入许多主机，可能在扩大控制范围。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q/B |
| 主判断日志及字段 | 目标Windows 4624：Computer、TargetUserSid、TargetLogonId、LogonType、IpAddress；Linux sshd/PAM：目标主机、账号、成功会话、来源。 |
| 详细起始判据 | 同账号30分钟成功登录≥5台目标且超过同角色基线；分开统计Windows类型3网络登录与10远程桌面，以及Linux交互/非交互会话。 |
| 关联日志、字段及关联方式 | 关联ZE-020的源→目标连接；同一DC多个4769不能替代多个目标主机登录成功。 |
| 常见正常原因及误报排除条件 | 补丁、堡垒机、服务账号；多个成功登录不等于远程命令执行。 |
| 与 Zeek 的关系 | ZE-020、ZE-021 |

### UE-026 用户角色使用范围突变

账号开始进入与岗位或服务职责无关的系统。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | B/R |
| 主判断日志及字段 | 4624目标Computer与账号；Keycloak LOGIN的clientId、userId、realmId；岗位/账号用途、应用分级快照。 |
| 详细起始判据 | 同账号首次访问高价值系统且角色无常规业务关系；有明确禁止策略则R，否则B候选，需异常来源/时间等证据升级。 |
| 关联日志、字段及关联方式 | clientId从应用目录解析；身份映射必须有联邦目录或管理台账支持。 |
| 常见正常原因及误报排除条件 | 转岗、新项目、灾备切换；首次SSO授权不证明应用内执行了操作。 |
| 与 Zeek 的关系 | ZE-007、ZE-021弱重叠 |

### UE-027 Keycloak批量账号或角色管理

一个操作者短时间修改很多人的账号或角色，可能是批量滥用。

| 项目 | 预研设计 |
|---|---|
| 判定方式 | Q |
| 主判断日志及字段 | Keycloak Admin Events：time、authDetails.userId、realmId、operationType、resourcePath、resourceType、error；目标ID从已验证路径适配提取。 |
| 详细起始判据 | 同actor+realm10分钟成功修改用户/角色映射目标≥20，或关键角色目标≥5；按用户停用、角色授予等动作分桶，避免无关CRUD混加。 |
| 关联日志、字段及关联方式 | 关联管理会话、工单和IAM同步作业；representation无目标信息时不得虚构。 |
| 常见正常原因及误报排除条件 | 批量入职、停用同步、自动化管理。 |
| 与 Zeek 的关系 | ZE-012仅可见部分LDAP侧操作 |

## 5. 异常场景：按实际证据成立

单个事件已经足以说明具体异常行为时可独立形成场景。包含“之后”“利用”“执行”等含义的场景必须具备相应阶段证据。基线偏离通常只作为线索；命中事件不自动证明攻击成功。

下表“单事件”表示一个检测器输出足够，并不意味着只读取一条原始日志。所有场景继承检测卡的授权与质量前提。

| 场景ID | 场景名称 | 成立方式与必要证据 | 可选增强及结论边界 | 实体与观察窗口 |
|---|---|---|---|---|
| US-001 | 疑似密码喷洒 | UE-001聚合模式成立即可单事件 | UE-011锁定增强；不能断言同一密码或成功入侵 | 来源、入口、账号集合；10分钟 |
| US-002 | 疑似单账号爆破 | UE-002排除配置错误后单事件 | Zeek认证尝试增强；不要求后续成功 | 账号、入口；10分钟 |
| US-003 | 密码攻击后出现成功认证 | UE-001或UE-002，加UE-003；成功账号须属于被攻击集合 | 来源与设备一致增强；仅认证成功，不直接判接管 | 账号、来源、入口；30分钟 |
| US-004 | 服务账号交互式使用违规 | UE-004中账号用途和交互式会话均明确 | 异常来源、时段和账号用途标签增强 | 服务账号、目标会话；单次 |
| US-005 | 停用账号新认证异常 | UE-005且状态历史、复制宽限和新认证证据完整 | 缓存或旧会话单独调查 | 账号、状态区间、认证入口 |
| US-006 | 疑似会话被多方使用 | UE-009，加UE-007或UE-010的独立冲突证据 | 排除VPN及正常多终端；UE-023可增强 | 账号、会话；30分钟 |
| US-007 | 异常来源或时段的特权账号使用 | UE-006或UE-007，加特权标签及明确授权范围违规 | UE-008、UE-026增强；只有新时间/地址保留线索 | 账号、来源、目标；1小时 |
| US-008 | 非批准密码重置 | UE-012成功操作及授权违规即可单事件 | 重置后从操作者来源登录则升级疑似接管调查 | actor、subject；单次/30分钟增强 |
| US-009 | 非批准高权限授予 | UE-013明确授予结果和授权违规即可单事件 | UE-014标记新账号分支 | actor、目标账号、组/角色；30分钟 |
| US-010 | 短时权限使用后撤回 | UE-015授予→撤回区间内，有目标账号执行受该权限保护的操作 | 仅授权和撤回只判临时授权异常 | 账号、角色、操作会话；2小时 |
| US-011 | 临时账号异常使用后删除 | UE-016并排除测试和批准任务 | UE-014高权限授予增强 | 新账号稳定ID；2小时 |
| US-012 | 目录权限或委派违规修改 | UE-017解析出成功扩大权限及授权违规 | ZE-012仅证明网络侧目录操作 | actor、目录对象GUID；单次 |
| US-013 | 疑似目录复制权限滥用 | UE-018满足复制权限及主体条件即可单事件 | ZE-023可增强来源和复制请求；不称全部凭据已泄露 | 主体、DC、登录会话；5分钟 |
| US-014 | Keycloak认证配置违规弱化 | UE-019证实实际配置差异且违规即可单事件 | 不要求有人随后登录；UE-020为后果证据 | realm、配置对象、actor；单次 |
| US-015 | 成功会话认证强度不达标 | UE-020具有完整策略和实际因子证据即可单事件 | UE-019增强；缺MFA字段不成立 | realm、subject、session、client；单次 |
| US-016 | 认证器异常变更后账号使用 | UE-021后同账号出现新会话/新来源成功登录 | 自助恢复需排除；凭据变更本身可另记策略违规场景 | 账号、凭据元数据、会话；1小时 |
| US-017 | Keycloak身份代理使用违规 | UE-022成功代理及授权违规即可单事件 | 后续操作归属actor需有可信会话关联 | actor、被代理subject；单次 |
| US-018 | Keycloak会话刷新异常 | UE-023持续多窗异常且排除客户端重试 | 冲突来源增强；不能仅此断言token被盗 | realm、session、client；15分钟 |
| US-019 | 审计能力被人为削弱 | UE-024明确审计关闭/清除即可单事件 | 数据中断是影响旁证；无日志本身不证明人为行为 | actor、主机/realm；单次 |
| US-020 | 疑似横向登录扩散 | UE-025排除批准批量登录后单事件 | ZE-020/ZE-021增加网络证据；结论限于登录扩散 | 账号、源设备、目标集合；30分钟 |
| US-021 | Keycloak批量管理异常 | UE-027显著超正常管理范围并无匹配任务 | UE-013或账号停用等具体结果增强 | realm、actor、目标集合；10分钟 |
| US-022 | 疑似账号接管后扩大访问 | UE-003、UE-012后异常登录或UE-021后异常登录任一有接管线索，再有UE-025或UE-026 | 需源/会话关系；多事件数量本身不提升可信度 | 账号、可信映射设备、会话；2小时 |

## 6. 与 Zeek 重叠时怎么办

### 6.1 三种关系必须分清

| 关系 | 例子 | 正确处理 |
|---|---|---|
| 同一动作的重复观测 | Zeek NTLM与Windows凭据验证观察同一次认证 | 合并到一个逻辑动作的证据集合，保留原始记录，不把失败次数加倍 |
| 同一过程的不同阶段 | 域控验证凭据→签发服务票据→目标4624登录 | 保留为不同动作，按会话/主体/目标关联为一个场景；不做原始记录去重 |
| 同名但不同过程 | 同NAT出口下两个人分别在AD和Keycloak失败 | 身份/入口/请求不一致时保留不同事件，不因名称和来源IP相同合并 |

事件ID代表检测器定义，不是每次行为的唯一ID。UE-001和ZE-003可以映射到同一行为类型 `authentication.password_spray`；具体发生一次行为时生成独立 event_instance_id。场景同理区分 definition_id 与 case_id。

### 6.2 建议映射

| 新检测 | Zeek检测 | 统一行为/证据关系 | 场景处理 |
|---|---|---|---|
| UE-001 | ZE-003 | password_spray；多源认证观察 | 同来源设备、认证入口、时间窗和账号集合高度一致时同US-001 case |
| UE-002 | ZE-002/ZE-004 | brute_force；SSH缺账号时只能主机级相关 | 对得上账号/目标才融合；不把Zeek auth_attempts变成精确失败数 |
| UE-003 | ZE-005 | failure_then_success | 相同受攻击账号与成功入口时同US-003；认证与登录结果范围分开 |
| UE-008 | ZE-006/ZE-013 | auth_method_anomaly | 罕见方式和明确禁止方式保留不同reason_code |
| UE-017 | ZE-012 | directory_change | LDAP请求是尝试证据，5136是对象修改证据；结果不冲突时同场景 |
| UE-018 | ZE-023 | directory_replication_abuse | AD说明权限使用及主体，Zeek说明复制操作及网络来源 |
| UE-025 | ZE-020/ZE-021 | remote_access_expansion | 网络连接、凭据验证、目标登录分别记录阶段，合入US-020或更长case |
| UE-024 | ZE-051/ZE-052 | audit_control_change 与 telemetry_quality | 不去重、不相加为攻击分；仅有影响关联才展示在同调查中 |

### 6.3 可实施的处理顺序

1. **原始传输去重**：Windows使用采集源实例+Computer+Channel+日志世代+EventRecordID；日志清除后RecordID可重用。Keycloak优先用稳定原生事件id；缺id时用源、realm、精确时间、动作、主体、会话、资源及稳定内容指纹。无ID且完全相同的并发记录可能无法区分，记录去重不确定性，不能武断丢弃。
2. **标准化但保留原文**：字段转换带adapter_version，保存source_ref。成功字段必须映射到明确的auth_validation、ticket_issue、target_logon或admin_change。
3. **构建观察组**：优先共同请求/会话/操作ID；跨Zeek与AD没有共同ID时，使用来源端口/地址、目标、规范账号、协议、结果和校正时间匹配，起始容差±3秒且按采集时钟误差调整。同一连接可有多次认证，uid不能独自确定单次认证。
4. **有歧义时不强制归并**：同秒多个请求、代理、共享账号等形成候选关联。失败计数采用完整度已验证的指定主数据源；另一个源补证，不直接SUM，也不靠简单MAX冒充准确合并。
5. **形成可疑事件**：行为键建议为behavior_type+身份域+主体或来源实体+认证入口/目标+检测窗口；喷洒另保存受攻击账号集合和失败分布。滑动窗口重复命中通过事件活跃期更新同一实例。
6. **场景归并**：同一行为过程维护case_id、first_seen、last_seen、necessary_evidence、supporting_evidence和状态。必要阶段到齐后从“喷洒”升级“喷洒后成功”，发送升级通知并保留原历史，不再创建无关的重复高危告警。
7. **明确关闭和重开规则**：按场景定义的无活动间隔结束；之后再次发生创建新episode并链接旧case。不能只用“最近30分钟”把长期互不相关的行为无限合并。

### 6.4 融合后的证据对象示意

以下是平台自定义结构，不是任何产品的原生日志：

```json
{
  "event_instance_id": "evt-auth-001",
  "behavior_type": "authentication.password_spray",
  "definition_ids": ["UE-001", "ZE-003"],
  "subject_scope": "ad-domain-A",
  "source_entity": "host-023",
  "target": "auth-service-A",
  "attempt_count_source": "windows-security",
  "unique_failed_accounts": 12,
  "evidence_refs": ["win-record-1", "win-record-2", "zeek-record-1"],
  "correlation_confidence": "high",
  "outcome_scope": "credential_validation",
  "case_id": "case-001"
}
```

同一认证的两条记录是两份证据，不是两次独立异常。评分按“行为维度”和“证据独立性”校准：来源罕见、授权违规、后续目标登录等可独立贡献；同一失败被两套传感器看见只提升观测可靠性，不将风险分翻倍。

### 6.5 身份或结果冲突

- AD成功、Zeek失败：先确认是否同一次尝试、相同结果范围和采集完整性。保留双方证据，不用简单多数投票。
- Keycloak用户名alice与AD用户名alice：没有联邦映射时保持两个身份，最多显示疑似关联。
- 共享出口无法还原用户设备：允许源IP级喷洒检测，但不要把后续另一个用户成功登录合成“喷洒成功”。
- 网络证据缺失但AD/Keycloak主证据完备：可以形成用户行为事件；反之按Zeek能力输出网络线索。跨源关联不是全部事件的必需条件。

## 7. 具体判断示例（合成数据，非实测攻击）

### 7.1 AD和Zeek均发现喷洒，只形成一个行为事件

10:00～10:08，源设备host-023在同一AD认证入口对12个账号各失败2次。主身份日志经请求去重后为24次，Zeek同时观察到对应24条认证记录。

UE-001和ZE-003都命中，但失败数仍为24、账号数仍为12。创建一个US-001 case，保留两个definition_id和48份原始观察引用。10:09只有出现前述12账号之一的可信成功认证，才能将该过程升级US-003。如果成功账号不在该集合，另行记录成功，不能直接升级。

若主身份日志只有4776，其成功只表示凭据验证；若目标服务器另有4624，则增补“目标登录成功”阶段。

### 7.2 一次管理员操作就能形成场景

AD记录4728：SubjectUserSid=帮助台账号，MemberSid=temp01账号SID，TargetSid=域管理员组SID。操作成功，授权表不允许该帮助台授予此组权限。

这一个操作已经使UE-013和US-009成立，不需等待失败登录或Zeek目录日志。若只是没有查到工单但帮助台具备批准职责，先输出“高权限授予待核查”，不自动断言恶意提权。

### 7.3 Keycloak认证策略变化与MFA判断

11:00管理员事件为UPDATE，resourcePath指向已验证的认证流配置对象；旧快照显示强认证步骤必需，新快照显示它被禁用，且不在批准变更范围。UE-019及US-014可成立。

11:05出现LOGIN，并不自动证明MFA绕过。只有同一session/client的实际执行步骤、上游IdP认证和有效策略证明认证强度不达标，才形成UE-020及US-015。没有这份扩展证据，应明确写“配置已弱化，后续会话是否受影响待核查”。

### 7.4 网络连接与目标登录分别保留

Zeek先观察host-023连接6台服务器的远程管理端口，其中5台服务器4624显示同账号成功登录。将ZE-020与UE-025关联到US-020，不能把每台连接和4624合成“两次登录”。

这一过程说明连接尝试覆盖6台、目标登录成功覆盖5台。本期仅调查认证、会话和账号权限，不能进一步推断远程命令执行或数据访问。

## 8. 采集与验证计划

| 批次 | 接入/实现 | 可优先验证的事件 | 通过标准 |
|---|---|---|---|
| 第一批 | AD/DC与目标Windows Security；Keycloak用户和管理员事件；账号/角色快照 | UE-001～UE-005、UE-011～UE-016、UE-019、UE-027 | 成功/失败、actor/subject、域/realm身份、关键目标字段均可从样本追溯；明确记录不可用字段 |
| 第二批 | Linux登录、VPN/NAC/堡垒机认证与会话 | UE-009、UE-025 | 用户→来源设备→目标会话可关联；共享账号、重连、NAT负样本不会错误归人 |
| 第三批 | Keycloak认证流程扩展证据、配置版本化、跨源观察归组 | UE-020～UE-023及跨源case更新 | 不以缺字段判断MFA绕过；同一行为多源重复接入后计数与告警量不翻倍 |
| 持续校准 | 28～56天基线与同岗位分组、审批范围和白名单 | UE-006～UE-010、UE-026及量异常 | 有充足有效样本；角色改变可重建；异常数据不立即学习为正常 |

每个事件至少验证：一组明确正样本、一组正常操作、一组关键字段缺失、一组身份/时间冲突。管理操作用非生产测试域和测试realm生成。不得对真实账号实施密码喷洒、撤销MFA或破坏审计来做验证。

重叠验收必须包含：同事件重放、两传感器重复观察、同IP不同账号、同名不同realm、同连接多次认证、跨DC验证、已有SSO刷新、事件乱序、日志清除后RecordID重用。结果应分别是传输去重、证据融合、保留不同主体/阶段或明确不确定，而不是一律合并。

## 9. 新认证或日志系统的配置化接入设计

### 9.1 “不修改代码”的实现边界

目标是平台具备通用采集、解析、语义映射和规则执行引擎以后，接入新厂商时只发布配置包，不修改核心代码、不重新编译部署。以下为拟建设计，不能理解为当前仓库已实现这些能力。

| 新系统情况 | 处理方式 | 是否能够只改配置 |
|---|---|---|
| 已支持传输方式，输出JSON/XML/键值对/可解析文本，语义属于现有认证或管理动作 | 新建来源配置、字段/结果映射及测试样本 | 可以 |
| 现有传输方式，新增字段或厂商错误码 | 添加扩展字段、枚举映射和能力声明，运行契约校验 | 可以 |
| 新认证流程，需要不同分组或时间序列 | 使用平台支持的规则DSL配置检测器和场景 | 在现有DSL表达能力内可以 |
| 新私有二进制协议、专有SDK、未支持的鉴权握手或复杂采集状态机 | 先开发可复用连接器或使用外部适配服务转换为标准输入 | 不能保证零开发；核心检测代码仍可保持不变 |
| 源系统没有记录实际MFA步骤、来源或成功结果 | 标记相应能力不可用，推动源端补采 | 字段映射不能生成不存在的证据 |
| 新行为需要现有引擎没有的算法或操作符 | 扩展通用引擎后再配置规则 | 需要引擎开发 |

零代码接入主要覆盖“传输、格式及行为语义都已被平台支持”的情况。插件代码、任意Python脚本和自定义SQL存储过程不应包装成“仅配置”。

### 9.2 处理流程与配置边界

`通用连接器 → 原始存储 → 解析器 → 字段及语义映射 → 质量/能力检查 → 实体与观察关联 → 通用事件规则 → 场景归并`

| 层次 | 可配置内容 | 通用引擎承担的职责 |
|---|---|---|
| 连接器 | Syslog、HTTP推送、Kafka、文件采集代理、REST轮询；来源地址、主题、时间范围、鉴权引用 | 接收、限速、重试、游标、分页、断点恢复；只开放已实现驱动 |
| 解析器 | JSONPath/XPath、键值分隔符、受限正则、多行边界、字符编码、时间格式 | 类型检查、资源限制、多行合并、解析错误隔离 |
| 规范化 | 原始字段→规范字段、值字典、条件映射、时间单位、空值策略 | 运行固定转换操作符，记录映射版本和原始字段血缘 |
| 能力配置 | 有无稳定用户ID/会话ID/结果/密码失败原因/实际认证因子/历史配置 | 决定哪些规则可启动，哪些只能降级或不可检测 |
| 身份与上下文 | 身份域命名、可信联邦关联、资产/班次/权限/审批数据表映射 | 时间有效的实体连接，歧义保持未关联 |
| 可疑事件 | 过滤、分组、窗口、去重、数量、基线、序列、输出语义 | 统一执行R/Q/B/S检测，维护状态和事件实例 |
| 场景 | 必要证据、可选证据、实体约束、顺序、观察期、通知升级策略 | 统一归并case，判断成立/升级/结束，避免重复通知 |

REST模板必须明确分页方式（offset、cursor等）、排序键、时间范围与恢复规则。新API只有在其流程能被模板表达时才是配置化接入；不能假设所有分页/登录流程相同。

### 9.3 最小统一事件模型

以下字段为平台设计，不是AD、Keycloak或其他产品必然存在的原生字段。所有规范字段需要记录原始路径和转换依据。

| 规范字段 | 含义与约束 |
|---|---|
| schema_version、mapping_version | 规范和映射版本，随事件保存 |
| source.instance_id、source.type、source.native_event_id | 产品实例与原始记录身份，不能只用厂商名称区分来源 |
| event.time、event.ingested_at | UTC事件时间与入库时间；配置秒/毫秒、时区和精度 |
| event.action | auth.login、auth.logout、auth.refresh、account.disable、account.password_reset、role.grant、role.revoke、auth.policy_change、authenticator.change、session.impersonate、audit.clear等受控语义 |
| event.outcome | success、failure、denied、unknown；未知值不默认成功 |
| event.outcome_scope | credential_validation、ticket_issue、target_logon、idp_session、token_refresh、admin_change，避免混淆不同认证阶段 |
| event.reason、event.native_reason | 规范原因与原始原因同时保留，如invalid_password、account_locked、mfa_failed、expired_session |
| identity.domain、actor.id、subject.id | 身份域、操作者和被操作主体；登录可相同，重置密码等通常不同 |
| subject.claimed_name、subject.id_verified | 失败时提交的用户名与已核实稳定身份分开，不能把攻击者输入的用户名当真实登录人 |
| source.ip、source.device_id、source.ip_trust | 可信来源及设备；反向代理/NAT情况显式标记 |
| target.id、application.id | 登录目标主机或认证入口；IdP入口和下游业务应用不能任意互换 |
| session.id、request.id、operation.id | 能力存在时填入；缺失保留空，禁止生成假ID冒充源端关联证明 |
| auth.protocol、auth.factors、auth.assurance_verified | 协议、已证明的实际因子与证明状态；requested acr只保存为请求，不作实际强度 |
| resource.type、resource.id、change.before、change.after | 账号/角色/认证配置对象与已证实的前后值；不引入文件或云应用对象 |
| evidence.ref、quality.flags | 原始记录引用及缺失/冲突/延迟标记；原始秘密脱敏或不采集 |

可扩展的厂商字段放到受控的extensions命名空间，不直接修改核心模型。检测器需要使用某个扩展能力时，在配置中显式声明，不在代码中写“如果是某厂商”。

### 9.4 来源配置示例

以下YAML是拟设计的配置DSL示意，不可直接当作现成产品配置运行。假设新认证系统输出JSON，包含at、kind、status、tenant、principal_id、app、client_ip、sid和request_id。

```yaml
api_version: ueba/v1
source:
  instance_id: auth-system-b-prod
  type: generic_identity
connector:
  driver: kafka
  brokers_secret_ref: secrets/auth-b/kafka
  topic: authentication.audit
  checkpoint_store: platform
parser:
  format: json
mapping:
  event.time: {path: "$.at", type: timestamp, format: rfc3339}
  identity.domain: {path: "$.tenant", type: string}
  actor.id: {path: "$.principal_id", type: string}
  subject.id: {path: "$.principal_id", type: string}
  subject.claimed_name: {path: "$.username", type: string}
  application.id: {path: "$.app", type: string}
  source.ip: {path: "$.client_ip", type: ip}
  session.id: {path: "$.sid", type: string, missing: null}
  request.id: {path: "$.request_id", type: string, missing: null}
  source.native_event_id: {path: "$.event_id", type: string, missing: null}
semantics:
  action:
    input: "$.kind"
    values: {SIGN_IN: auth.login, SIGN_OUT: auth.logout}
    otherwise: quarantine
  outcome:
    input: "$.status"
    values: {OK: success, BAD_PASSWORD: failure, LOCKED: denied}
    otherwise: unknown
  reason:
    input: "$.status"
    values: {OK: none, BAD_PASSWORD: invalid_password, LOCKED: account_locked}
    otherwise: unknown
  outcome_scope: idp_session
capabilities:
  stable_subject_id: true
  session_id: optional
  explicit_password_failure: true
  actual_authentication_factors: false
quality:
  required: [event.time, identity.domain, event.action, event.outcome]
  on_parse_error: quarantine
  on_missing_subject_id: retain_as_claimed_identity
```

本例只适用于登录/注销；新增管理员密码重置时，actor与subject必须分别映射，不能复用“二者均为principal_id”的模板。instance_id标明来源，identity.domain还需配置映射到可信身份域；新系统tenant字符串与AD域名相同不能自动归为同一域。

capabilities是声明，并不证明每条记录字段都齐全。启用规则时同时验证近期样本覆盖率，运行时再次检查必要字段。建议规则状态为enabled、degraded、unavailable并显示原因。

### 9.5 一份检测配置复用多个厂商

将UE-001实现为按规范语义执行的检测器。AD、Keycloak和未来产品只要提供“密码类登录失败、账号或提交名称、来源、入口”，即可复用。

```yaml
detector_id: UE-001
behavior_type: authentication.password_spray
requires:
  - login_outcome
  - explicit_password_failure
  - source_identity
  - attempted_account_identity
input: logical_auth_attempts
filter:
  all:
    - {field: event.action, eq: auth.login}
    - {field: event.outcome, eq: failure}
    - {field: event.reason, eq: invalid_password}
group_by: [identity.domain, auth.entrypoint_id, source.entity_key]
window: {size: 10m, slide: 1m, allowed_lateness: 2m}
aggregate:
  per_subject_failures: count_by_subject
  distinct_subjects: count_distinct_subjects
condition:
  all:
    - {metric: distinct_subjects, gte: 10}
    - {metric: subjects_with_1_to_3_failures, gte: 10}
output:
  event_instance_mode: update_active_episode
  preserve_evidence_refs: true
```

auth.entrypoint_id、source.entity_key和logical_auth_attempts由实体/观察归组层产生，是派生字段，不能当作源端字段。subject.id缺失时，只有身份域内的claimed_name可用才允许低置信分组，并避免大小写规范化造成账号混淆。

新来源接入后，不应自动把它的失败记录加入既有计数。需先配置该入口的主计数来源或可靠跨源观察归组策略；没有共同request.id时使用本报告第6章的关联边界。不同入口的密码喷洒先分别检测，需要跨入口场景时再显式关联。

同理，UE-019～UE-023及UE-027虽然目前以Keycloak命名，底层可映射为认证策略变化、认证强度不足、认证器变更、身份代理、会话刷新、批量身份管理。未来系统具有相同语义和足够证据时可复用这些规则，不必另写一套同义代码；产品差异在映射包和能力条件中表达。

### 9.6 零代码接入的发布步骤

1. 管理员选择已支持连接器，填写来源实例、鉴权秘密引用、消费主题或REST分页游标配置。
2. 导入脱敏样本，至少包含成功、密码失败、账号锁定、缺字段、异常时间格式、重复和乱序记录。
3. 配置字段、枚举、时间单位、身份域、actor/subject、结果范围和敏感字段处理。
4. 在预览中并排显示原始记录、规范事件、派生身份、缺失字段、可启用/不可启用规则及原因。
5. 配置包保存为版本化文件或管理平台对象，经过schema和语义契约校验后进入影子运行；影子模式不发正式告警，也不训练正式基线。
6. 用回放数据验证原始计数、逻辑动作计数、事件和场景数量，尤其确认同次认证跨源不翻倍。
7. 发布版本，记录生效时间；可灰度到单实例，发现错误可回滚。模型/基线是否重建取决于字段语义是否改变，不能让新映射悄悄污染旧基线。

管理界面需要提供样本测试、解析预览、规则能力矩阵、统计口径差异、版本对比和回滚。配置变化也是平台管理操作，应有操作者与版本审计；本期不采集其他业务应用的内容审计。

### 9.7 配置安全、可靠性与验收

- 配置只允许白名单转换操作符及有限制的正则、聚合和序列语法；禁止嵌入任意脚本，限制资源消耗。
- 凭据只用secret_ref引用，不在配置、事件或预览中显示原始秘密。平台按来源与身份域隔离访问权限。
- 接入采用可恢复传输加幂等去重；明确游标、重放范围、事件顺序和迟到更新策略，不承诺未实现的端到端恰好一次。
- 未知枚举进入unknown或隔离队列并产生质量提示；关键字段缺失时不自动补成功或补MFA完成。
- 配置包需要版本、适用来源schema、样本与预期规范输出、能力列表。源版本变化后用同一契约回归验证。
- 验收示例：新产品通过已支持HTTP/Kafka/Syslog/REST接入，仅新增配置包；UE-001/UE-002等核心规则无代码diff仍正常运行；缺少认证因子时UE-020明确显示unavailable；接入第二观测源后失败数保持一致。
- 本方案解决接入和已有行为检测复用；对全新业务行为的安全含义仍需要预研和规则配置，并非自动理解任何日志。


## 10. 尚需目标环境验证的项目与参考

本报告没有获得实际 Windows版本、Keycloak版本、日志样本、事件配置、身份联邦映射和审批数据。字段表是采集契约草案，不能据此宣称检测器已具备生产可用性。尤其要确认Keycloak事件枚举/详情字段、4776来源恢复、5136/4662审计范围、MFA步骤证明和服务账号标签。

技术语义参考官方资料；事件阈值、聚合方式、场景边界和融合规则是本报告的设计建议，不是厂商提供的现成检测规则。

- [Zeek 版原始基础文档](/Users/wayne/Documents/code/UEBA/UEBA可疑事件与异常场景预研_Zeek版_2026-09-07.md)：保留其独立事件与场景体系，本期仅映射相关身份和网络行为。
- [Microsoft 5136 目录修改](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-5136)：ObjectGUID、属性和值以及操作关联字段用于重建目录变更。
- [Microsoft 4662 目录对象操作](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4662)：须解释AccessMask/Properties及对象审计条件，不能把任何目录访问都认定复制攻击。
- [Keycloak管理员事件源码](https://github.com/keycloak/keycloak/blob/main/core/src/main/java/org/keycloak/representations/idm/AdminEventRepresentation.java)：用于核对字段模型；main与latest会变化，实施时固定部署版本验证。

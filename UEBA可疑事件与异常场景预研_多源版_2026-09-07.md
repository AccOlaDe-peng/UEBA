# UEBA 可疑事件与异常场景预研（多源日志版）

> 日期：2026-09-07  
> 网络侧基础：`Zeek日志字段详解_2026-09-04.md`  
> 范围：Zeek + Windows/AD + Linux auditd/syslog + EDR/Sysmon + IAM/SSO/VPN + DNS/代理/防火墙 + 数据库 + SaaS/云审计 + DLP/文件审计。

## 1. 结论与总体架构

多源 UEBA 的关键不是堆规则，而是先把不同日志中的 `人—账号—会话—进程—设备—IP—资源—目的地` 串起来。Zeek 提供跨网络的客观通信证据；身份日志确认认证和账号状态；端点日志确认进程与文件动作；应用/数据库/云审计确认具体资源和管理操作。只有这些证据在实体和时间上能够关联，才升级为异常场景。

建议统一事件最少保留：

| 维度 | 规范字段示例 |
|---|---|
| 时间 | `event_time`, `ingest_time`, `timezone`, `source_clock_skew` |
| 主体 | `user.id`, `user.name`, `user.domain`, `service_account`, `principal_arn` |
| 会话 | `session_id`, `logon_id`, `token_id`, `zeek.uid`, `trace_id` |
| 设备 | `host.id`, `host.name`, `mac`, `src_ip`, `nat_src_ip`, `device_id` |
| 行为 | `event.action`, `event.outcome`, `auth_method`, `logon_type`, `process.command_line` |
| 对象 | `resource.id`, `file.path`, `db.name/table`, `dst_ip/domain`, `cloud.account` |
| 证据质量 | `sensor`, `schema_version`, `packet_loss`, `field_completeness`, `confidence` |

## 2. 数据源和关联键

| 数据源 | 主要用途 | 最关键字段/事件 | 与 Zeek 的关联 |
|---|---|---|---|
| Windows Security | 登录、账号/组、策略、服务、计划任务、日志清除 | 4624/4625/4634/4648、4688、4697/4698、4719、4720/4726、4728/4729、4732/4733、4740、4768/4769/4771、1102 | `IpAddress/WorkstationName/Computer/TargetUserName/LogonId` ↔ Zeek IP、主机、账号、时间 |
| Sysmon/EDR | 进程树、命令行、哈希、网络、文件、注册表、注入 | Sysmon 1/3/8/10/11/12-14/17-22/25；EDR vendor 字段 | `ProcessGuid + DestinationIp/Port + Image + User` ↔ `conn` 五元组和时间 |
| Linux auditd | 登录、exec、文件、权限、sudo、审计配置 | `USER_AUTH/USER_LOGIN/EXECVE/SYSCALL/PATH/CONFIG_CHANGE`; `auid, uid, ses, exe, proctitle, syscall, success, name` | 主机/IP/时间/进程网络遥测 |
| Keycloak/SSO/VPN | 登录结果、MFA、客户端、会话、管理事件 | `type`, `realmId`, `clientId`, `userId`, `ipAddress`, `error`, `auth_method`, `auth_type`, admin resource/action | 源 IP、userId、会话和访问目标 |
| DNS/代理/防火墙 | 解析、URL、TLS 解密、NAT、允许/拒绝 | user、src/dst、NAT、URL/category、bytes、rule、action、TLS SNI | 补足 Zeek NAT、用户、HTTPS URL 和策略动作 |
| 数据库审计 | 登录、SQL、对象、结果行数/字节、权限操作 | db_user、client_ip、database/schema/table、statement/object、rows/bytes、success | client_ip + DB 目标 + 时间；Zeek 校验流量量级 |
| 文件/NAS/DLP | 文件读写删、标签、内容命中、外设/打印/上传 | actor、path/object_id、operation、bytes、classification、channel、policy | 主机/账号/时间；Zeek 提供网络目的地和传输量 |
| 云/SaaS 审计 | IAM、API、对象共享、下载、审计变更 | actor、eventName/action、sourceIPAddress、userAgent、resource、request/response、error | IP、账号、时间、目的域；云端行为为主证据 |
| CMDB/HR/PAM | 人员状态、部门、资产角色、敏感资源、审批 | employment_status、manager、role、asset_owner/type、change_ticket、PAM_session | 为规则和基线提供“是否允许”的上下文 |

## 3. 可疑事件：如何从日志判断

本章采用独立编号 `ME-001～ME-058`。字段名优先写日志原生字段；不同厂商没有统一字段时，使用第 1 章的规范字段。实现时必须同时记录主日志、关键字段、过滤条件、分组键、时间窗口、计算方法、阈值和关联键。

关联优先级如下：

1. 稳定 ID：`user.id`、`session_id/LogonId`、`ProcessGuid`、文件哈希、云 `accessKeyId`、Zeek `uid/fuid`。
2. 复合键：`host.id + user.id + event_time`，或网络五元组加时间容差。
3. IP 只能在时间有效的 DHCP、VPN、NAT 租约中转换为设备或用户，不能用当前 IP 归属反推历史事件。
4. 只有主日志明确记录 `success/outcome` 时才能断言操作成功；Zeek 流量只作为网络旁证，不能替代身份、进程、文件或云审计结果。
5. 下表阈值均为生产校准前的起始值；基线条件默认要求至少 28 天历史和 100 个有效样本。

### 3.1 身份认证

| ID | 可疑事件 | 主判断日志及字段 | 详细起始判据 | 关联日志、字段及关联方式 |
|---|---|---|---|---|
| ME-001 | 停用/离职账号登录成功 | IdP/AD：`event_time,userId,event_type,outcome,source_ip,session_id`；HR/目录：`userId,enabled,employment_status,disabled_at,effective_at` | 登录 `outcome=success`，按 `userId` 关联事件时刻有效的身份快照；`enabled=false` 或离职/冻结已生效，且 `login.event_time >= status.effective_at` | `userId` 为主键；Zeek 认证日志中的规范账号、来源 IP 和近邻时间只用于补充后续网络行为 |
| ME-002 | 服务账号交互式登录 | Windows 4624：`TimeCreated,TargetUserSid,TargetUserName,LogonType,IpAddress,WorkstationName,TargetLogonId`；Linux：`USER_LOGIN/USER_START` 的 `acct,auid,ses,terminal,addr,res` | 账号目录 `service_account=true`，且 Windows `LogonType in (2,10)`；或 Linux `res=success` 且存在 TTY/SSH 交互终端。类型 3/4/5 不按交互式登录处理 | 以 SID/userId 关联账号标签；Windows 用 `TargetLogonId` 关联后续进程，Linux 用 `ses/auid`；Zeek SSH/RDP 只确认网络会话 |
| ME-003 | 禁止认证方式登录 | Windows 4624：`TargetUserSid,LogonType,AuthenticationPackageName,LmPackageName,KeyLength,IpAddress,TargetLogonId`；IdP/VPN：`userId,clientId,auth_method,amr,acr,outcome` | 仅统计成功登录；按 `(账号,应用,资产角色)` 查询允许认证方式策略，实际方式不在允许集合即命中，例如禁止 NTLM 或禁止 password-only | SID/userId 关联策略；Zeek `ntlm.success=true` 或 Kerberos/RADIUS 记录用于协议旁证，不能覆盖主日志结果 |
| ME-004 | MFA 绕过或降级 | IdP/VPN：`event_time,userId,clientId,session_id,required_factors,completed_factors,amr,acr,policy_id,step_up,outcome` | `outcome=success`，但 `completed_factors` 未覆盖策略要求，或实际 `acr` 低于应用最低级别；同账号/应用从历史强认证降级只作增强 | 用 `session_id` 关联 token/VPN 会话；Zeek 仅按源 IP、目的应用和时间补充，不能单独判定 MFA |
| ME-005 | 非常用时间登录 | IdP/Windows/Linux 成功登录：`event_time,user.id,application,source_ip,session_id` | 将事件转换为用户本地 `weekday×hour`；当前时间槽在用户过去 8 周占比 `<1%` 且成功样本 `>=100`，或异常分超过 `median+6*MAD`；夜间本身不直接告警 | HR 时区/班次、值班表和维护窗关联；后续远程管理或数据访问提高分数 |
| ME-006 | 非常用地区登录 | 登录：`event_time,user.id,source_ip,device_id,session_id,outcome`；IP 情报：`country,region,city,ASN,proxy/VPN` | 仅成功登录；国家/ASN 过去 30 天未见，或历史占比 `<1%` 且样本充分；先还原企业 VPN/代理出口后的真实客户端 IP | 用 `session_id` 关联 VPN；地理库须保存版本。云出口、移动网络和企业代理需要排除 |
| ME-007 | 首次或非常用设备 | IdP：`userId,device_id,user_agent,browser,managed,compliant,outcome,session_id`；EDR：`host.id` | 成功登录且 `device_id` 在该用户过去 30 天未见，或出现频率 `<1%`；`managed=false`/`compliant=false` 增强。无稳定 device_id 时仅作低置信 | 用 EDR host.id、证书设备 ID 或 DHCP `mac+assigned_addr+租约时间` 校验；UA 不能作为唯一设备标识 |
| ME-008 | 非常用认证方式 | IdP/VPN：`userId,clientId,auth_method,amr,acr,outcome,event_time` | 对 `(userId,clientId,auth_method)` 建 30 天频率；成功方式首次出现或占比 `<1%` 且历史样本 `>=100` 时命中 | Zeek NTLM/Kerberos/RADIUS 通过规范账号+近邻时间补充协议证据；系统认证方式切换需变更单排除 |
| ME-009 | 异常并发会话 | IdP/VPN/OS：`userId,session_id,login_time,logout_time,source_ip,device_id,application,outcome` | 构造会话区间；同一 userId 的两个成功会话重叠 `>=5分钟`，且设备不同并且网段/地区不兼容时命中 | VPN 分配 IP、NAT 和 Zeek 活跃连接确认会话仍在使用；共享终端与服务账号单独建模 |
| ME-010 | 不可能旅行 | 两次成功登录：`userId,event_time,source_ip,session_id`；GeoIP：`latitude,longitude,confidence` | 按 userId 排序相邻登录，计算大圆距离/时间；速度 `>900km/h` 且距离 `>500km` 时命中，登录间隔过短时需设最小分母 | 先排除 VPN、云出口、代理和移动运营商；低精度 GeoIP 不生成高置信事件 |
| ME-011 | 密码喷洒 | Windows 4625/4771/4776：`TimeCreated,TargetUserName,IpAddress/Workstation,Status,SubStatus`；IdP：`userId,source_ip,error,outcome` | 按 `(source_ip,应用/目标)` 建 10 分钟窗：失败账号去重 `>=10`、失败总数 `>=10`，每账号失败 `1～3`；随后成功由 ME-013 单独判断 | Zeek NTLM/RADIUS 失败聚集用账号+源/目标+时间关联；共享 NAT、失效批处理要排除 |
| ME-012 | 暴力破解 | 与 ME-011 相同的失败登录字段 | 按 `(账号,应用/目标)` 建 10 分钟窗：失败 `>=10`、失败率 `>=80%`；来源 IP `<=3` 为集中爆破，来源高基数标记为分布式爆破 | SSH `auth_attempts` 和 Zeek 连接失败只能增强；密码变更后的旧凭据重试应抑制 |
| ME-013 | 失败后成功 | 同一身份源的失败/成功记录：`event_time,userId,source_ip,device_id,application,outcome,error,session_id` | 同 `(userId,application)` 前 30 分钟失败达到 `ME-011` 或 `ME-012` 阈值，之后出现 `outcome=success`；相同来源和设备为高置信，不同来源为中置信 | 用成功 `session_id/LogonId` 关联后续进程、资源与 Zeek 网络行为 |
| ME-014 | 账号集中锁定 | Windows 4740：`TimeCreated,TargetUserName,CallerComputerName,SubjectUserName`；IdP：`userId,source_ip,event_type=lock` | 按 `CallerComputerName/source_ip` 建 10 分钟窗：不同锁定账号 `>=5`；同一账号反复锁定另作为账号配置问题 | DHCP/EDR 将 caller/source 映射到设备；Zeek 扫描和认证流量用于增强 |

### 3.2 账号权限与审计规避

| ID | 可疑事件 | 主判断日志及字段 | 详细起始判据 | 关联日志、字段及关联方式 |
|---|---|---|---|---|
| ME-015 | 加入高权限组 | AD 4728/4732/4756 `SubjectUserName,MemberName,TargetUserName`; IdP admin event | 成员加入 `Domain Admins/Administrators` 或企业关键组，且无审批/PAM 会话 | `helpdesk1` 将 `temp01` 加入 Domain Admins |
| ME-016 | 创建高权限账号 | 4720 后关联 4728/4732；云/IdP create user + role grant | 新账号在 30 分钟内获关键角色 | 关联创建者、目标账号和组 |
| ME-017 | 创建隐藏账号 | 4720/Linux useradd audit；`name, uid, shell, home` | 名称伪装、异常 UID/GID、可登录 shell、未入 CMDB | 不把单纯 `$` 结尾机器账号当异常 |
| ME-018 | 修改关键 ACL/委派 | AD Directory Service Changes 5136/5145；Windows 4670；云策略审计 | 关键对象 DACL/SACL/owner 或委派属性扩大；比较 old/new value | 必须解析“谁获得了什么权限” |
| ME-019 | 修改安全策略 | Windows 4719/4739/5136；GPO 变更；云策略日志 | 审计策略、域策略、信任或安全控制变弱 | 变更单和维护窗作抑制 |
| ME-020 | 修改 sudoers | auditd `PATH name=/etc/sudoers*`, `SYSCALL`, `auid`, `exe`, `success` | 非批准主体成功写入 sudoers 或创建 `/etc/sudoers.d/*` | 关联后续 `USER_CMD/EXECVE` |
| ME-021 | 短时提权后恢复 | 组/角色 grant 与 revoke 日志 | 同一主体 2 小时内获权→敏感操作→撤权 | 三段必须共享目标账号/角色 |
| ME-022 | 新账号使用后删除 | create/delete + 登录/API/资源访问 | 新账号创建→使用→短时删除 | 账号 ID 优先于可复用用户名 |
| ME-023 | 关闭或降低审计 | Windows 4719：`CategoryId,SubcategoryGuid,AuditPolicyChanges,SubjectUserName,SubjectLogonId`；auditd：`CONFIG_CHANGE,DAEMON_END` 的 `auid,ses,res,op`；云审计配置事件：`actor,action,resource,outcome` | 事件结果成功，且审计类别从成功/失败审计变为关闭、auditd 停止，或云 trail/diagnostic setting 被删除、停用或缩小范围；无批准变更单即命中 | Windows 用 `SubjectLogonId`、Linux 用 `auid+ses` 关联操作者和前序进程；同时生成数据质量事件，缺口期间其他检测降低置信度 |
| ME-024 | 删除或清空日志 | Windows 1102；Linux audit/syslog 文件操作；云 log group delete | 明确清除安全日志或删除审计存储 | 关联操作者 LogonId/auid 与之前高风险行为 |

### 3.3 进程、执行与持久化

| ID | 可疑事件 | 主判断日志及字段 | 详细起始判据 | 关联日志、字段及关联方式 |
|---|---|---|---|---|
| ME-025 | 禁止工具执行 | Sysmon 1/EDR `Image,CommandLine,Hashes,Signature,User,ProcessGuid` | 哈希/签名/工具特征命中阻止或监控清单 | 关联同进程网络连接 |
| ME-026 | 凭据转储 | Sysmon 10 `SourceImage,TargetImage,GrantedAccess`; EDR technique | 非授权进程访问 LSASS/SAM/浏览器凭据；结合 dump 文件创建 | 随后认证扩散或外传升级场景 |
| ME-027 | 高风险 PowerShell | 4104 ScriptBlock、4688/Sysmon 1 命令行、AMSI | 编码命令、下载执行、反射加载、绕过策略等多特征评分 | HTTP/TLS/DNS 目的地 |
| ME-028 | 下载后执行 | 代理/浏览器/EDR file origin + Sysmon 1/11 | 同主机同文件哈希/路径 30 分钟内下载→落盘→执行 | Zeek `http/files` 提供 fuid、大小、MIME；TLS 时靠代理/EDR |
| ME-029 | Office/浏览器启动解释器 | Sysmon 1 `ParentImage,Image,CommandLine` | Office/浏览器/PDF 阅读器父进程启动 shell、PowerShell、脚本宿主 | 子进程外联目的地增强 |
| ME-030 | 系统进程父子异常 | Sysmon 1/EDR 父子链 | 违反已知合法父进程、路径、签名和会话关系 | 不能只按进程名判断，需路径/签名/用户 |
| ME-031 | 稀有命令/进程链/执行账号 | Sysmon 1/auditd EXECVE | 对 `(host role, user, parent→child, normalized command)` 建基线 | 首次只加分，需命令风险或外联增强 |
| ME-032 | 多主机执行相同命令 | EDR/Sysmon/auditd 规范化命令哈希 | 同账号或相似命令 30 分钟出现在多主机 | Zeek 横向边与认证扩散共同确认 |
| ME-033 | 执行后清理日志 | 高风险进程 + 1102/日志文件删除 | 同主体/LogonId/ProcessGuid 的高风险执行后 30 分钟清理 | 网络流量不能替代主机证据 |
| ME-034 | 新建系统服务 | Windows 4697/7045 `ServiceName,ServiceFileName,AccountName`; systemd audit | 非批准服务、用户可写路径、远程共享/脚本解释器作为 binary | SMB/RPC 横向连接增强 |
| ME-035 | 新建计划任务 | Windows 4698 `TaskName,TaskContent,SubjectUserName`; EDR | 新任务运行高风险路径/命令、触发器异常、无变更单 | 创建前的远程管理连接 |
| ME-036 | cron/systemd/启动项 | auditd PATH/SYSCALL；Sysmon Registry 12-14 | 写入 cron、systemd unit、Run/RunOnce、登录脚本等持久化位置 | 后续周期外联可形成 C2 场景 |
| ME-037 | 新增 SSH key | auditd/EDR 文件修改 `~/.ssh/authorized_keys` | 非批准主体写入关键账号密钥，记录新增 key 指纹 | 随后 Zeek SSH 新来源登录 |
| ME-038 | 关闭终端防护 | EDR tamper alert、服务状态、策略变更 | 安全代理/防火墙/杀软停用或排除范围扩大 | 紧随外联/横向行为升级严重度 |

### 3.4 文件、数据、网络与应用

| ID | 可疑事件 | 主判断日志及字段 | 详细起始判据 | 关联日志、字段及关联方式 |
|---|---|---|---|---|
| ME-039 | 访问禁止资源 | 文件/应用/数据库审计 `actor,resource,action,outcome` + RBAC | 成功访问明确禁止的资源 | “拒绝访问”与“成功读取”分级 |
| ME-040 | 扩大敏感资源权限 | 文件 ACL、NAS、SaaS sharing audit | 敏感对象从私有/部门内变为更广、匿名或外部可访问 | 保存 old/new ACL 和操作者 |
| ME-041 | 首次高价值/跨部门访问 | 资源标签、HR 部门、访问日志 | 首次或低频访问且角色无业务关系 | 新颖度不是恶意，需数据量/异常时间增强 |
| ME-042 | 读取量/文件数异常 | NAS/文件审计 `read_bytes,file_id,path`; DB/SaaS download | 对用户+资源类别做日/小时基线，绝对量和偏离度双阈值 | Zeek SMB/conn 校验网络字节，但不替代读取结果 |
| ME-043 | 批量枚举 | 文件 list/stat、对象 List API、数据库元数据查询 | 短时目录/对象/表高基数且读取深度异常 | 搜索索引、备份、资产盘点白名单 |
| ME-044 | 集中归集与压缩 | 文件创建/复制 + 进程命令 `zip/7z/tar/rar` | 多源敏感文件→同目录→压缩包，实体与时间连续 | 仅看到 `.zip` 文件不足以判断其内容 |
| ME-045 | 批量加密/重命名/删除 | EDR file telemetry、NAS audit | 单进程/用户短时影响大量文件，扩展名/熵改变，删除备份 | 与横向共享访问组合为勒索场景 |
| ME-046 | 异常外联时间/上行量/比例 | Zeek conn、NetFlow、代理、防火墙 `bytes_in/out` | 仅对内网源→外网目标计算；按主机/角色/时段基线 | NAT 前日志用于实体归属；方向字段先统一 |
| ME-047 | TLS/DNS/C2 异常 | 见 Zeek 版对应事件 | 罕见指纹、周期连接、DNS 隧道、NXDOMAIN 分别检测后组合评分 | 避免把所有周期心跳当 C2 |
| ME-048 | 数据库访问/查询量异常 | DB audit `db_user,client_ip,object,statement,rows,bytes,outcome` | 新数据库/表、敏感对象、结果行/导出量异常 | Zeek PostgreSQL 只在可解析明文流量时增强 |
| ME-049 | SaaS 下载/批量导出 | SaaS audit `actor,object,operation,bytes,count,ip,device` | 同主体多资源下载/导出超过自身与同岗位基线 | API 分页需合并为一次逻辑导出 |

### 3.5 云与 SaaS

| ID | 可疑事件 | 主判断日志及字段 | 详细起始判据 | 关联日志、字段及关联方式 |
|---|---|---|---|---|
| ME-050 | 创建长期访问密钥 | CloudTrail：`eventTime,eventName=CreateAccessKey,userIdentity.arn,requestParameters.userName,responseElements.accessKey.accessKeyId,sourceIPAddress,userAgent,errorCode` | `errorCode` 为空且创建成功；操作者或目标用户不在批准范围即命中，为其他用户创建、来源新颖或 Root 身份提高严重度 | 用新 `accessKeyId` 关联后续 API；审批/PAM 以 actor、目标和时间关联 |
| ME-051 | 扩大 IAM 权限 | CloudTrail：`eventName,eventTime,userIdentity.arn,requestParameters,responseElements,errorCode,sourceIPAddress`，关注 `Put*Policy/Attach*Policy/UpdateAssumeRolePolicy` | API 成功，解析策略变更前后差异；新增高风险 `Action`、更广 `Resource`、外部 `Principal` 或弱化 Condition 即命中 | Cloud Config/策略快照按 policy ARN 关联 old/new；IaC 流水线和变更单用于排除 |
| ME-052 | 关闭云审计 | CloudTrail：`StopLogging/DeleteTrail/UpdateTrail` 的 `eventTime,userIdentity.arn,requestParameters,errorCode,sourceIPAddress` | 调用成功，且停止/删除 trail，或缩小 Region、数据事件或组织覆盖范围；任何非批准变更直接命中 | 组织级 CloudTrail、Config 和日志存储访问日志用于旁证；同时生成数据质量事件 |
| ME-053 | 对象存储公开 | 云审计/Config：bucket policy、ACL、Public Access Block 的 `actor,resource,old_value,new_value,eventTime,outcome` | old/new 差异导致 `Principal=*`、匿名 ACL、关闭公共访问阻止，或新增组织外 principal，且操作成功 | 资源 ARN 关联敏感标签和后续对象读取；批准网站托管桶按资源范围排除 |
| ME-054 | 创建外部共享 | SaaS/存储审计：`event_time,actor.id,object.id,object.classification,action,target_user,target_domain,link_scope,outcome` | `outcome=success`，敏感对象被设为 anonymous/public，或共享给非批准外域；从内部变外部的 old/new scope 为强证据 | 以 `object.id` 关联后续外部下载；DLP 分类和审批记录补充敏感性与授权 |
| ME-055 | 非常用敏感 API | CloudTrail：`eventTime,eventSource,eventName,userIdentity.arn,sourceIPAddress,userAgent,errorCode,resources` | 仅统计成功调用；对 `(principal,eventSource,eventName)` 建 30 天频率，首次或占比 `<1%` 且 API 属于高风险集合时命中 | IaC/CI 身份、维护窗和变更单排除；Zeek/代理验证来源网络和目的服务 |
| ME-056 | 令牌多地并发 | 云/SaaS：`event_time,credential_id/accessKeyId,principal,source_ip,userAgent,session_id,outcome` | 同一凭据在重叠 10 分钟窗从两个不兼容国家、ASN 或设备成功使用；仅按用户名不能判定同一令牌 | GeoIP、VPN/代理、云 NAT 清单关联；以 accessKeyId/token ID 为核心关联键 |
| ME-057 | 新凭据创建后立即使用 | 创建事件：`credential_id,creator,target,event_time`；使用事件：`accessKeyId,principal,source_ip,userAgent,eventName,event_time` | 新 credential ID 在创建后 30 分钟内被使用，且来源 ASN/设备对目标主体为新，或调用敏感 API；必须直接关联 credential ID | 创建审批、PAM 会话和 IaC 作业用于排除；不能只按同名用户关联 |
| ME-058 | 审计关闭期间敏感操作 | 审计配置：`actor,resource,action,outcome,event_time`；旁路控制面/数据面日志：敏感 API/资源访问字段 | 构造审计缺口区间，在区间内同一 actor/account 出现权限扩大、资源公开或批量读取；关闭和敏感操作均需成功证据 | 使用组织级日志、Config 快照、对象访问日志和网络日志补证；无旁证时标为“可见性受损” |

## 4. 异常场景组合

场景使用独立编号 `MS-001～MS-019`，只引用本文的 `ME-*` 事件。Zeek 网络检测若未在本文单独列为 ME 事件，应作为“网络增强证据”按日志字段描述，不再引用其他文档编号。

| 场景 ID | 异常场景 | 必要事件 | 增强证据 | 关联对象 | 窗口 |
|---|---|---|---|---|---|
| MS-001 | 密码喷洒攻击 | `ME-011` | `ME-013`；Zeek 内网扫描 | 来源 IP、目标应用、账号集合 | 30 分钟 |
| MS-002 | 暴破后账号失陷 | `ME-012 + ME-013` | `ME-006,ME-007,ME-031` | 账号、设备、成功会话 | 30 分钟 |
| MS-003 | 账号接管 | `ME-013` 或 `ME-010` | `ME-004,ME-006,ME-007,ME-009,ME-041` | 账号、会话、设备 | 1 小时 |
| MS-004 | 权限滥用 | `ME-015`、`ME-016` 或 `ME-018` | `ME-021,ME-041,ME-055` | 操作者、目标账号/资源 | 2 小时 |
| MS-005 | 凭据窃取与使用 | `ME-026` + 后续成功认证 | `ME-008`；Zeek 横向扩散 | 主机、账号、LogonId/会话 | 1 小时 |
| MS-006 | 下载执行 | `ME-028` | `ME-027,ME-029`；恶意目的地 | 主机、文件哈希、ProcessGuid | 30 分钟 |
| MS-007 | 主机失陷并持久化 | 高风险执行 + `ME-034`、`ME-035`、`ME-036`、`ME-037` 任一 | `ME-038`；周期外联 | 主机、进程、账号 | 2 小时 |
| MS-008 | 内网侦察 | Zeek 主机/端口扫描 | LDAP/RPC/DNS 枚举日志 | 源主机 | 30 分钟 |
| MS-009 | 横向移动 | Zeek 横向扩散 + 目标成功登录 | `ME-032`、异常远程管理进程 | 账号、源/目标主机 | 2 小时 |
| MS-010 | 数据打包暂存 | `ME-044` | `ME-041,ME-042,ME-043` | 用户、主机、文件集合 | 2 小时 |
| MS-011 | 数据收集后外传 | `ME-042`、`ME-044`、`ME-048`、`ME-049` 任一，加 `ME-046` 或明确上传 | `ME-054`、DLP 内容命中 | 用户、主机、数据集、目的地 | 4 小时 |
| MS-012 | 勒索软件 | `ME-045` | `ME-038`、Zeek 横向扩散、删除备份 | ProcessGuid、主机、共享 | 30 分钟 |
| MS-013 | DNS 隧道 | Zeek DNS 隧道检测 | 周期连接、NXDOMAIN、新域名 | 主机、注册主域 | 30 分钟 |
| MS-014 | 云身份异常 | `ME-056` | `ME-055,ME-057,ME-006,ME-007` | 云身份、credential ID | 1 小时 |
| MS-015 | 云权限滥用 | `ME-051` | `ME-050,ME-055,ME-057` | actor、role/policy ARN | 2 小时 |
| MS-016 | 云审计规避 | `ME-052` | `ME-058,ME-053,ME-023` | 云账号、actor | 2 小时 |
| MS-017 | 内部人员异常访问 | `ME-041 + ME-042` | `ME-005,ME-044` | 用户、资源、主机 | 4 小时 |
| MS-018 | 内部人员数据外传 | `MS-017` + 明确上传或 `ME-054` | `ME-046` | 用户、主机、目的地 | 8 小时 |
| MS-019 | 检测链路失效 | 数据源中断/字段缺失质量事件 | `ME-023,ME-024,ME-052` | 数据源、采集器 | 15 分钟 |

## 5. 完整示例：账号接管 → 横向移动 → 数据外传

### 5.1 原始证据

1. Keycloak：同一 `userId=8f...` 从 `203.0.113.24` 对 `clientId=vpn` 连续出现 18 条 `LOGIN_ERROR error=invalid_user_credentials`，随后一条 `LOGIN` 成功；成功会话 `acr` 低于策略要求。
2. VPN：分配内部地址 `10.0.0.40`，记录 `session_id=V123`，与 Keycloak 登录事件共享账号和来源。
3. Windows：目标服务器出现 4624，`TargetUserName=alice, LogonType=10, IpAddress=10.0.0.40, LogonId=0x91a2`。
4. Zeek：`10.0.0.40` 在 20 分钟内通过 445/3389 访问 16 台服务器；NTLM 多次成功，形成横向扩散。
5. Sysmon：源主机上 `mstsc.exe` 和 `powershell.exe` 的 Process Create；PowerShell 随后创建 `C:\\Users\\Public\\export.zip`。
6. 文件审计：`alice` 读取财务共享 2,300 个文件，共 4.8 GB。
7. 代理 + Zeek：同主机向首次目的地 `upload.example` 发出 5.1 GB 上行；代理记录 URL、用户和允许动作，Zeek `conn.orig_bytes` 验证量级。

### 5.2 事件与场景升级

- 18 次失败后成功 → `ME-012 + ME-013`；MFA 条件不满足 → `ME-004`；组合 `MS-002/MS-003`。
- 4624 类型 10 + Zeek 多目标远程管理 + NTLM 成功 → `MS-009 横向移动`。
- 文件读取异常 + 压缩包创建 → `MS-010 数据打包暂存`。
- 代理确认上传 + Zeek 校验上行量，并与收集行为共享主机、用户和 4 小时窗口 → `MS-011 数据收集后外传`。

最终告警应展示行为链，而不是四个互不关联的单点告警：

```text
alice / V123 / 10.0.0.40
  登录失败聚集 → 低强度认证成功 → RDP 登录
  → SMB/NTLM 横向扩散 → 财务文件批量读取
  → export.zip 创建 → upload.example 5.1 GB 上传
```

## 6. 实施优先级

### P0：先做数据和确定性事件

- 身份：成功/失败、账号状态、MFA、会话、锁定。
- Windows/端点：4624/4625、账号组变更、1102、Sysmon 1/3/10/11。
- Zeek：conn、dns、ssl、ntlm、kerberos、smb、http/files。
- 统一 DHCP/NAT/VPN/代理的 IP→设备→用户时间映射。
- 确定性事件：停用账号成功登录、高权限组变更、日志清除、审计关闭、公开共享、非授权 Modbus 写。

### P1：聚合与行为链

- 密码喷洒/暴破后成功、扫描、横向扩散、批量读取、下载后执行、权限短时授予后回收。
- 构建 `session_id / LogonId / ProcessGuid / zeek.uid / file hash / cloud credential ID` 关联。

### P2：基线和评分

- 非常用时间/地区/设备/认证方式、稀有进程链、异常访问量、DNS 隧道、Beacon、云 API 稀有度。
- 每个模型都输出特征贡献、基线范围、数据完整性和抑制原因。

## 7. 验证要求

每个检测器上线前至少验证：

1. 正样本：用 CALDERA、Atomic Red Team 或自建脚本产生明确操作并保存 ground truth。
2. 负样本：备份、补丁、扫描、发布、月末报表等高噪声业务回放。
3. 字段覆盖：生产中关键字段非空率、日志时延、时钟偏差和丢包率。
4. 关联正确性：动态 IP、NAT、共享账号、跳板机、服务账号的归属是否准确。
5. 指标：事件级 precision/recall、每天每千实体告警数、场景升级率、调查可解释率。
6. 降级策略：主证据缺失时不得用弱网络信号冒充确定性结论。

## 8. 参考资料

- [本项目 Zeek 字段实测文档](./Zeek日志字段详解_2026-09-04.md)
- [Zeek 8.2 Common Logs](https://docs.zeek.org/en/current/reference/logs/index.html)
- [Microsoft：Windows 登录审计事件与 Logon Type](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/basic-audit-logon-events)
- [Microsoft：Sysmon 事件和字段](https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon)
- [Microsoft：Sysmon 事件关联方法](https://learn.microsoft.com/en-us/windows/security/operating-system-security/sysmon/sysmon-events)
- [Red Hat：Linux Audit EXECVE/SYSCALL/PATH/USER_LOGIN 示例](https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/html/security_hardening/auditing-the-system_security-hardening)
- [Keycloak：Server Administration Guide / Events](https://www.keycloak.org/docs/latest/server_admin/index.html)
- [AWS：CloudTrail record 字段](https://docs.aws.amazon.com/awscloudtrail/latest/userguide/cloudtrail-event-reference-record-contents.html)
- [AWS：CloudTrail userIdentity、sessionContext 与 MFA 字段](https://docs.aws.amazon.com/awscloudtrail/latest/userguide/cloudtrail-event-reference-user-identity.html)

> 各厂商字段和事件 ID 会随平台、版本及审计策略变化。实施时应保存原始事件、schema 版本和采集配置，并以目标环境实测结果为准。

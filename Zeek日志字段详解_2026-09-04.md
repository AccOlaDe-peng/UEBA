# Zeek 日志字段详解（2026-09-04 实测）

> 来源：`/data/zeek-logs/`（34 个 Zeek 8.2.2 协议分析器输出）
> 数据规模：8,709,553 条 DNS + 130,805 条 DHCP + 96,944 条 conn + 各协议事件若干
> 采样时间：2026-09-02 ~ 2026-09-04 dlp-testbed 实验期间

本文档按协议家族分组，逐一讲解每个 Zeek 日志的字段含义 + 实战数据样例。

---

## 目录

1. [核心元数据层](#1-核心元数据层)
   - [conn.log](#11-connlog-连接元数据)
   - [files.log](#12-fileslog-文件传输记录)
   - [weird.log](#13-weirdlog-异常协议行为)
   - [reporter.log](#14-reporterlog-zeek-自身警告)
   - [analyzer.log](#15-analyzerlog-协议分析器加载)
   - [notice.log](#16-noticelog-zeek-触发告警)
   - [packet_filter.log](#17-packet_filterlog-抓包丢包统计)
2. [认证授权层](#2-认证授权层)
   - [ssh.log](#21-sshlog-ssh-握手)
   - [ssl.log](#22-ssllog-tls-握手)
   - [kerberos.log](#23-kerberoslog-kerberos-认证)
   - [ntlm.log](#24-ntlmlog-ntlm-认证)
   - [ldap.log](#25-ldaplog-ldap-操作)
   - [dce_rpc.log](#26-dce_rpclog-windows-rpc-调用)
   - [radius.log](#27-radiuslog-radius-aaa)
3. [Web/应用层](#3-web应用层)
   - [http.log](#31-httplog-http-请求)
   - [websocket.log](#32-websocketlog-websocket-握手)
   - [sip.log](#33-siplog-sip-会话)
   - [irc.log](#34-irclog-irc-聊天)
4. [文件传输层](#4-文件传输层)
   - [ftp.log](#41-ftplog-ftp-控制)
   - [smtp.log](#42-smtplog-邮件)
5. [基础设施层](#5-基础设施层)
   - [dns.log](#51-dnslog-dns-查询)
   - [dhcp.log](#52-dhcplog-地址分配)
   - [ntp.log](#53-ntplog-时间同步)
   - [snmp.log](#54-snmplog-网络管理)
   - [syslog.log](#55-sysloglog-系统日志)
6. [数据库层](#6-数据库层)
   - [postgresql.log](#61-postgresqllog-postgres-查询)
   - [redis.log](#62-redislog-redis-命令)
7. [物联网与工控层](#7-物联网与工控层)
   - [mqtt_connect.log](#71-mqtt_connectlog-mqtt-连接)
   - [mqtt_publish.log](#72-mqtt_publishlog-mqtt-发布)
   - [mqtt_subscribe.log](#73-mqtt_subscribelog-mqtt-订阅)
   - [modbus.log](#74-modbuslog-modbus-工控)
8. [SMB 文件共享](#8-smb-文件共享)
   - [smb_files.log](#81-smb_fileslog-smb-文件访问)
   - [smb_mapping.log](#82-smb_mappinglog-smb-共享映射)

---

# 1. 核心元数据层

所有 Zeek 日志的"骨架"——7 个日志记录 Zeek 自身运行状态 + 所有连接的元信息。

---

## 1.1 conn.log（连接元数据）

**行数：96,944**（**最大日志**）
**职责**：记录**每一条** TCP/UDP/ICMP 连接的元信息（所有协议的流量都会经过这里）

### 字段含义

| 字段 | 类型 | 含义 | 实战样例 |
|------|------|------|---------|
| `ts` | float | **时间戳**（Unix epoch，含小数秒） | `1788508301.069001` → `2026-09-04 16:11:41` |
| `uid` | string | **连接唯一 ID**（Zeek 内部生成的 13 字符 ID），可关联其他 log（http/ssl/ssh 等） | `CDsTS32kEj5n5bydN3` |
| `id.orig_h` | IP | **发起方 IP** | `10.0.0.40`（trex 容器） |
| `id.orig_p` | int | **发起方端口** | `49948` |
| `id.resp_h` | IP | **响应方 IP** | `10.0.0.50`（ostinato/Samba） |
| `id.resp_p` | int | **响应方端口** | `445`（SMB） |
| `proto` | string | **传输层协议** | `tcp` / `udp` / `icmp` |
| `service` | string | **Zeek 识别的应用层协议**（DPD 探测后） | `ssl` / `http` / `ssh` / `sip` / `finger` / `-`（未知） |
| `duration` | float | **连接持续时间**（秒） | `0.000568`（极短）/ `3.001`（保持 3 秒） |
| `orig_bytes` | int | **发起方 → 响应方** 字节数 | `92` / `0` |
| `resp_bytes` | int | **响应方 → 发起方** 字节数 | `32` / `5685`（attacker 偷文件） |
| `conn_state` | string | **TCP 状态机结束标志** | `SF`（正常完成）/ `S2`（半开）/ `REJ`（拒绝）/ `OTH`（其他）/ `RSTR`/`RSTO`（被重置） |
| `local_orig` | bool | 发起方是否在本地子网（Zeek 配置的 local_nets） | `true` |
| `local_resp` | bool | 响应方是否在本地子网 | `true` |
| `missed_bytes` | int | 抓包丢失的字节数 | `0` / `91943`（mirror 丢包） |
| `history` | string | **TCP 状态机字符编码**（Zeek 自定义） | `ShADadFf`（S=SYN, h=half, A=ACK, D=Data, d=close, F=FIN） |
| `orig_pkts` | int | 发起方发送的包数 | `6` |
| `orig_ip_bytes` | int | 发起方 IP 层字节（含 IP header） | `412` |
| `resp_pkts` | int | 响应方发送的包数 | `4` |
| `resp_ip_bytes` | int | 响应方 IP 层字节 | `248` |
| `ip_proto` | int | IP 协议号 | `6`（TCP） / `17`（UDP） / `1`（ICMP） |

### 实战样例

```json
{"ts":1788508301.069001,"uid":"CDsTS32kEj5n5bydN3","id.orig_h":"10.0.0.40","id.orig_p":49948,"id.resp_h":"10.0.0.50","id.resp_p":445,"proto":"tcp","service":"smb","duration":2.34,"orig_bytes":2232,"resp_bytes":15048,"conn_state":"SF","local_orig":true,"local_resp":true,"missed_bytes":0,"history":"ShADadFf","orig_pkts":18,"orig_ip_bytes":2628,"resp_pkts":12,"resp_ip_bytes":16056,"ip_proto":6}
```
→ **10.0.0.40 → 10.0.0.50:445 SMB 连接**，2.3 秒，22 字节上行 + 15048 字节下行（员工从 Samba 共享下载文件）

```json
{"ts":1788514305.626803,"id.orig_h":"10.0.0.131","id.orig_p":53866,"id.resp_h":"10.0.0.50","id.resp_p":3389,"proto":"tcp","service":"-","duration":3.001,"orig_bytes":108,"resp_bytes":87,"conn_state":"SF","orig_pkts":7,"orig_ip_bytes":480,"resp_pkts":5,"resp_ip_bytes":305,"ip_proto":6}
```
→ **RDP 连接（端口 3389）** ob=108, rb=87（CR + CC + MCS 数据）

### conn_state 含义对照

| 标志 | 含义 | 实战场景 |
|------|------|---------|
| `SF` | SYN + FIN 正常完成 | 正常业务请求 |
| `S2` | SYN + 收到 SYN-ACK 后无 FIN（半开） | 客户端掉线、扫描器探测 |
| `S1` | SYN 后连接建立但无数据 | 慢速攻击 |
| `REJ` | 收到 RST/ICMP 拒绝 | **端口扫描** / 无人监听的服务 |
| `RSTR` | 接收方重置 | 服务端强制断开 |
| `RSTO` | 发起方重置 | 客户端异常中断 |
| `OTH` | 其他异常 | 通常 mirror 抓包丢包导致 |

### DLP 价值

- **流量基线**：每个 IP 的 conn_state 比例（如某 IP `S2:SF > 50%` 说明它在扫端口）
- **异常时长**：`duration` 0 = 端口扫描；`duration > 300` = 数据外泄
- **字节异常**：`resp_bytes >> orig_bytes` = 下载文件；`orig_bytes >> resp_bytes` = 上传
- **history 字符串**：Zeek 自定义 TCP 状态机简写，可识别半开、并发等异常

---

## 1.2 files.log（文件传输记录）

**行数：3**
**职责**：所有**应用层文件传输**的汇总（FTP/SMB/HTTP 下载/邮件附件等）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `fuid` | **文件唯一 ID**（Zeek 内部，可关联 ftp.log/smtp.log） |
| `uid` | 所属连接的 UID（关联 conn.log） |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `source` | **来源**协议类型：`HTTP` / `FTP_DATA` / `SMB` / `SMTP` |
| `depth` | 文件嵌套深度（0 = 顶层文件） |
| `analyzers` | Zeek 进一步分析的协议列表（空=无） |
| `mime_type` | MIME 类型 |
| `duration` | 传输耗时 |
| `local_orig` | 发起方是否本地 |
| `is_orig` | **方向**：`true` = orig→resp 上行；`false` = resp→orig 下行 |
| `seen_bytes` | **已观察到的字节数**（可能小于 total_bytes，因 mirror 抓包丢包） |
| `total_bytes` | **总字节数**（来自协议字段） |
| `missing_bytes` | 缺失字节（差值） |
| `overflow_bytes` | 超出预期字节 |
| `timedout` | 是否传输超时 |

### 实战样例（FTP 上传）

```json
{"ts":1788508301.069001,"fuid":"Ffj1RM1qA4ZjpyGzi9","uid":"CRBYGu23VOLWoqgAtb","id.orig_h":"10.0.0.10","id.orig_p":55122,"id.resp_h":"10.0.0.20","id.resp_p":40058,"source":"FTP_DATA","depth":0,"analyzers":[],"mime_type":"text/plain","duration":0.0,"local_orig":true,"is_orig":true,"seen_bytes":173,"total_bytes":173,"missing_bytes":0,"overflow_bytes":0,"timedout":false}
```
→ **员工 10.0.0.10 通过 FTP_DATA 上传 173 字节文件到 server 10.0.0.20**

### DLP 价值

- `is_orig: false` 表示 **下载**（下载敏感文件 = 外泄）
- `mime_type` 可识别敏感文件类型（zip/csv/xlsx/sql/dump）
- `seen_bytes` 接近 `total_bytes` 表示抓包完整

---

## 1.3 weird.log（异常协议行为）

**行数：191**
**职责**：Zeek 检测到的**不符合协议规范**的异常流量（攻击指示）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 关联连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `name` | **异常类型名称**（Zeek 内部定义） |
| `addl` | **附加信息**（不同 weird 类型有不同字段，如 DNS 异常的查询名） |
| `notice` | 是否升级为 notice |
| `peer` | 触发角色：`orig` / `resp` |
| `conn` | 关联 conn.log 标记 |

### 典型 weird.name 类型

| name | 含义 | 安全场景 |
|------|------|---------|
| `data_after_reset` | RST 后还有数据 | 异常关闭 |
| `DNS_RR_bad_length` | DNS 资源记录长度错误 | DNS 攻击 |
| `bad_HTTP_request` | HTTP 请求格式错 | HTTP 扫描器 |
| `SMB_encrypted_data` | SMB 加密数据未解密 | 加密 SMB |
| `inappropriate_FIN` | 不当的 FIN | 连接劫持 |
| `line_terminator_in_request` | HTTP 含换行符注入 | CRLF 注入 |
| `multiple_HTTP_request_in_a_row` | 一个连接多个请求 | Keep-Alive |

### DLP 价值

- **异常协议行为 = 攻击信号**（DLP 系统应高优先级告警）
- `addl` 字段含具体异常内容（如 DNS 查询名）

---

## 1.4 reporter.log（Zeek 自身警告）

**行数：1**
**职责**：Zeek 进程本身的错误和警告（脚本加载错误、分析器异常）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `level` | 严重级别：`Reporter::WARNING` / `ERROR` / `INFO` |
| `message` | 错误消息 |
| `location` | 出错的 zeek 脚本位置（file:line） |

### 实战样例

```json
{"ts":1788507104.661621,"level":"Reporter::WARNING","message":"get_file_handle() handler missing for Analyzer::ANALYZER_POP3","location":"/opt/zeek/share/zeek/base/frameworks/files/main.zeek, line 523"}
```
→ **Zeek POP3 分析器未实现文件句柄 handler**（无害警告）

---

## 1.5 analyzer.log（协议分析器加载）

**行数：1**
**职责**：Zeek 启动时加载的所有协议分析器列表

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳（Zeek 启动时间） |
| `kind` | 类型：`activate`（激活） / `deactivate` / `loaded` / `unloaded` |
| `name` | 分析器名称（如 `HTTP` / `TCP` / `SMB_Tree`） |

### 用途

- 确认哪些协议分析器已加载（决定能否解析）
- 排查"为什么 Zeek 不解析某协议"

---

## 1.6 notice.log（Zeek 触发告警）

**行数：183**
**职责**：Zeek 脚本框架触发的**高优先级告警**（与 weird.log 不同，notice 需要逻辑推理而非简单协议异常）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 关联连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `note` | **告警类型**（如 `SSL::Invalid_Server_Cert`） |
| `msg` | 告警消息（人类可读） |
| `sub` | 详细信息（结构化） |
| `src_addr` / `dst_addr` | 源/目的 IP |
| `p` | 端口 |
| `peer_desc` | 触发角色 |

### 典型 notice 类型

| note | 含义 |
|------|------|
| `SSL::Invalid_Server_Cert` | SSL 证书无效 |
| `SSH::Password_Guessing` | SSH 密码暴力 |
| `HTTP::SQL_Injection` | HTTP SQL 注入 |
| `Software::Vulnerable` | 检测到易受攻击软件版本 |
| `Heartbleed::SSL_Heartbeat_Attack` | Heartbleed 攻击 |

---

## 1.7 packet_filter.log（抓包丢包统计）

**行数：1**
**职责**：Zeek 从 libpcap 读取包时的丢包统计

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `node` | 节点名（默认 `zeek`） |
| `filter` | BPF 过滤器（如 `tcp`） |
| `init` | 启动时已丢字节数 |
| `dropped` | 增量丢字节数 |
| `received` | 增量接收字节数 |
| `accepted` | 增量接受字节数 |

### 用途

- **判断抓包是否完整**（dropped/accepted > 5% 应警告）
- 我们的测试床 mirror 丢包较多，触发过该警告

---

# 2. 认证授权层

7 个协议日志，涉及所有"谁用什么密码登什么系统"的流量。

---

## 2.1 ssh.log（SSH 握手）

**行数：20**
**职责**：SSH 协议握手元数据（**仅元数据，不解密 SFTP 内容**）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 关联 conn UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `version` | SSH 协议版本（通常 2） |
| `auth_attempts` | **认证尝试次数**（>1 = 暴力破解信号） |
| `client` | **客户端版本字符串**（`SSH-2.0-OpenSSH_8.4p1`） |
| `server` | 服务器版本字符串 |
| `cipher_alg` | 对称加密算法（如 `aes128-ctr`） |
| `mac_alg` | MAC 算法（`hmac-sha2-256`） |
| `compression_alg` | 压缩算法（`none`） |
| `kex_alg` | **密钥交换算法**（`curve25519-sha256`） |
| `host_key_alg` | 主机密钥算法（`ssh-ed25519`） |
| `host_key` | 主机密钥指纹（截断） |

### 实战样例

```json
{"ts":1788508301.064954,"uid":"CDsTS32kEj5n5bydN3","id.orig_h":"10.0.0.40","id.orig_p":49948,"id.resp_h":"10.0.0.50","id.resp_p":22,"version":2,"auth_attempts":0,"client":"SSH-2.0-OpenSSH_9.7","server":"SSH-2.0-OpenSSH_9.7","cipher_alg":"chacha20-poly1305@openssh.com","mac_alg":"hmac-sha2-512","compression_alg":"none","kex_alg":"curve25519-sha256","host_key_alg":"ssh-ed25519","host_key":"xx:xx:xx..."}
```

### DLP 价值

- **可识别 SSH 版本**（已知漏洞版本警告）
- `auth_attempts > 3` = **SSH 暴力破解信号**
- **看不到** SFTP 文件名（SSH 加密）

---

## 2.2 ssl.log（TLS 握手）

**行数：3**
**职责**：TLS/SSL 握手记录（含证书 SNI、cipher、版本）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `version` | TLS 版本（如 `TLSv12` / `TLSv13`） |
| `cipher` | **协商的 cipher suite**（如 `TLS_AES_128_GCM_SHA256`） |
| `curve` | 椭圆曲线（`X25519`） |
| `server_name` | **SNI（Server Name Indication）** = 客户端访问的域名（**关键 DLP 字段**） |
| `resumed` | 是否 TLS 会话恢复 |
| `next_protocol` | ALPN 协议（`h2` = HTTP/2） |
| `established` | 是否握手成功 |
| `cert_chain_fuids` | 证书链 file UID（关联 files.log） |
| `client_cert_chain_fuids` | 客户端证书链（mTLS 场景） |
| `subject` | 证书 Subject（CN/O 等） |
| `issuer` | 证书 Issuer（CA） |
| `client_subject` / `client_issuer` | 客户端证书（mTLS） |
| `validation` | 证书验证结果 |
| `ja3` / `ja3s` | JA3 客户端/服务端指纹（**机器学习识别**） |
| `last_alert` | 最后告警 |

### 实战样例（HTTPS 自签证书）

```json
{"ts":1788507750.5,"uid":"Cxxxx","version":"TLSv12","cipher":"TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256","curve":"secp256r1","server_name":"10.0.0.20","resumed":false,"next_protocol":"http/1.1","established":true}
```

### DLP 价值

- **`server_name` 是 HTTPS 场景的"域名"**（如 `internal.company.com` 表示访问内网）
- **JA3 指纹**可识别恶意软件（同一恶意软件族 JA3 相同）
- 看不到 HTTPS body 内容（加密）

---

## 2.3 kerberos.log（Kerberos 认证）

**行数：6**
**职责**：Kerberos AS-REQ / TGS-REQ 请求和响应

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `request_type` | 请求类型：`AS`（Authentication Service） / `TGS`（Ticket Granting Service） |
| `client` | **客户端主体**（如 `Administrator/TESTBED.LOCAL`） |
| `service` | **服务主体**（`krbtgt/TESTBED.LOCAL` = TGT 请求） |
| `success` | 是否认证成功 |
| `error_code` | Kerberos 错误码 |
| `error_msg` | 错误消息 |
| `from` | 票据生效时间 |
| `till` | **票据到期时间**（**DLP 重点**：超长票据 = 异常） |
| `cipher` | 票据加密类型（`aes256-cts-hmac-sha1-96`） |
| `forwardable` | 是否可转发 |
| `renewable` | 是否可续期 |
| `request_client` | 请求中的 client（请求 vs 响应可能不同） |

### 实战样例

```json
{"ts":1788513357.058506,"uid":"C2jiX61NLV7UKry94d","id.orig_h":"10.0.0.40","id.orig_p":57792,"id.resp_h":"10.0.0.50","id.resp_p":88,"request_type":"AS","client":"Administrator/TESTBED.LOCAL","service":"krbtgt/TESTBED.LOCAL","success":false,"error_msg":"Reply packet too large","till":1788599757.0,"forwardable":false,"renewable":false}
```
→ **域管理员 Administrator 请求 TGT**（AS-REQ），但 UDP 包过大自动回退 TCP

### DLP 价值

- **横向攻击信号**：`Administrator@DOMAIN` 在异常时间申请 TGS = **Pass-the-Ticket**
- **`till` 异常长** = 攻击者延长票据有效期
- **TGT 请求量大** = Kerberoasting（SPN 暴力）

---

## 2.4 ntlm.log（NTLM 认证）

**行数：3**
**职责**：NTLM/NTLMv2 认证（Windows / Samba 域认证）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `username` | **NTLM 用户名**（明文） |
| `hostname` | 客户端主机名 |
| `domainname` | 域名/工作组 |
| `server_nb_computer_name` | NetBIOS 服务器名 |
| `server_dns_computer_name` | DNS 服务器名 |
| `success` | 是否认证成功 |

### 实战样例

```json
{"ts":1788513477.664987,"uid":"CZKEO548aH9YU1ehU1","id.orig_h":"10.0.0.40","id.orig_p":54938,"id.resp_h":"10.0.0.50","id.resp_p":445,"username":"Administrator","hostname":"TREX","domainname":"MYGROUP","server_nb_computer_name":"DC01","server_dns_computer_name":"dc01.testbed.local","success":true}
```
→ **TREX 主机以 Administrator 身份登 dc01.testbed.local 域控成功**

### DLP 价值

- **NTLM 凭证明文可见**（相比 Kerberos 加密的票据）
- **横向移动检测**：某 IP 用 `Administrator` 访问多个主机 = 域内横向

---

## 2.5 ldap.log（LDAP 操作）

**行数：3**
**职责**：LDAP bind / search / modify 操作

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `message_id` | LDAP message ID |
| `version` | LDAP 版本（3） |
| `opcode` | **操作类型**：`bind` / `search` / `unbind` / `modify` / `add` / `delete` / `extended`（StartTLS） |
| `result` | **结果**：`success` / `failed` / `stronger auth required` 等 |
| `diagnostic_message` | 服务器诊断消息 |
| `object` | **LDAP DN（Distinguished Name）** = 操作对象（关键字段） |
| `argument` | 操作参数（如 bind 的密码——**默认 REDACTED**） |
| `status` | 详细结果码 |
| `error_code` | 错误码 |

### 实战样例

```json
{"ts":1788513445.09866,"uid":"C9itorz6iLwXg9vsg","id.orig_h":"10.0.0.40","id.orig_p":39794,"id.resp_h":"10.0.0.50","id.resp_p":389,"message_id":1,"version":3,"opcode":"bind simple","result":"stronger auth required","diagnostic_message":"BindSimple: Transport encryption required.","object":"Administrator@testbed.local","argument":"REDACTED"}
```
→ **管理员尝试明文 bind** 被拒（Samba AD 要求加密）

### DLP 价值

- **LDAP 枚举检测**：连续 `opcode=search` 不同 `object` = 目录枚举
- **匿名 bind** = 配置错误风险
- **修改操作**（modify/add/delete）监控 = 权限滥用

---

## 2.6 dce_rpc.log（Windows RPC 调用）

**行数：17**
**职责**：DCE/RPC over SMB（Windows 远程过程调用）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `rtt` | 往返时间（毫秒） |
| `named_pipe` | **命名管道**（如 `\\pipe\\samr`） |
| `endpoint` | **RPC 端点**（`samr` / `lsarpc` / `drsuapi` / `netlogon`） |
| `operation` | **RPC 操作**（如 `SamrConnect5` / `SamrEnumerateUsersInDomain`） |

### 实战样例（域用户枚举）

```json
{"ts":1788513477.702886,"uid":"CZKEO548aH9YU1ehU1","id.orig_h":"10.0.0.40","id.orig_p":54938,"id.resp_h":"10.0.0.50","id.resp_p":445,"rtt":0.002,"named_pipe":"\\\\pipe\\\\samr","endpoint":"samr","operation":"SamrConnect5"}
{"ts":1788513477.705284,"uid":"CZKEO548aH9YU1ehU1","id.orig_h":"10.0.0.40","id.orig_p":54938,"id.resp_h":"10.0.0.50","id.resp_p":445,"rtt":0.0007,"named_pipe":"\\\\pipe\\\\samr","endpoint":"samr","operation":"SamrLookupDomainInSamServer"}
{"ts":1788513477.706501,"uid":"CZKEO548aH9YU1ehU1","id.orig_h":"10.0.0.40","id.orig_p":54938,"id.resp_h":"10.0.0.50","id.resp_p":445,"rtt":0.0004,"named_pipe":"\\\\pipe\\\\samr","endpoint":"samr","operation":"SamrEnumerateUsersInDomain"}
```

### 关键 endpoint 含义

| endpoint | 功能 | 攻击信号 |
|----------|------|---------|
| `samr` | SAM Remote（用户/组管理） | 域用户枚举 |
| `lsarpc` | LSA Remote（密码策略/账户策略） | 密码策略提取 |
| `drsuapi` | Directory Replication Service | **DCSync 攻击** |
| `netlogon` | NetLogon（域认证） | **Pass-the-Hash** |
| `drsproxy` | DRS Proxy | DC 之间复制 |
| `wkssvc` | Workstation Service | 用户枚举 |

### DLP 价值

- **DCSync 检测**：`endpoint=drsuapi` + `operation=DRSGetNCChanges` = 域控复制（攻击者提取所有域用户哈希）
- **横向移动检测**：高频 `netlogon` 操作 = Pass-the-Hash
- **数据外泄检测**：`drsuapi` 操作 = 整个域被 dump

---

## 2.7 radius.log（RADIUS AAA）

**行数：3**
**职责**：RADIUS 协议认证、计费、授权请求

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `username` | **RADIUS 用户名**（明文） |
| `result` | **认证结果**：`success` / `failed` |
| `ttl` | RADIUS 响应往返时间（秒） |

### 实战样例

```json
{"ts":1788514609.942223,"uid":"CATPls3UGe47To2l21","id.orig_h":"10.0.0.40","id.orig_p":58357,"id.resp_h":"10.0.0.50","id.resp_p":1812,"username":"bob","result":"failed","ttl":1.001}
```

### DLP 价值

- **用户名明文可见**（虽然密码用 shared secret XOR 加密）
- **`result: failed` 高频出现** = 密码暴力破解
- 同一用户名多次 `failed` 后突然 `success` = 弱口令爆破成功

---

# 3. Web/应用层

4 个日志，覆盖 HTTP、WebSocket、SIP、IRC 应用协议。

---

## 3.1 http.log（HTTP 请求）

**行数：1**（实战触发较少）
**职责**：HTTP 请求和响应元数据

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `trans_depth` | **HTTP 管道深度**（同一连接上第几个请求，>1 = pipelining） |
| `method` | HTTP 方法：`GET` / `POST` / `PUT` / `DELETE` / `HEAD` / `OPTIONS` |
| `host` | **Host 头** = 域名（关键 DLP 字段） |
| `uri` | **完整 URI**（含 query string） |
| `referrer` | Referer 头 |
| `version` | HTTP 版本（`1.1` / `2.0`） |
| `user_agent` | **User-Agent 字符串**（浏览器/工具） |
| `origin` | Origin 头（CORS） |
| `request_body_len` | 请求体字节数 |
| `response_body_len` | 响应体字节数 |
| `status_code` | HTTP 响应码（200/301/404/500） |
| `status_msg` | 状态消息 |
| `info_code` | 100-level 中间响应码 |
| `info_msg` | 中间响应消息 |
| `tags` | 自定义标签 |
| `username` | HTTP Basic/Digest 认证用户名 |
| `password` | HTTP Basic 认证密码 |
| `proxied` | 是否通过代理 |
| `orig_fuids` / `resp_fuids` | 关联 files.log UID |
| `orig_mime_types` / `resp_mime_types` | MIME 类型 |

### DLP 价值

- **URI 含敏感路径**：`/api/secret`、`/download/confidential.pdf`
- **POST body**：超大请求体 = 数据上传外泄
- **密码字段**：HTTP Basic 认证 `password: <明文>`
- **MIME 类型**：识别下载敏感文件（xlsx/pdf/sql）

---

## 3.2 websocket.log（WebSocket 握手）

**行数：1**
**职责**：WebSocket 协议升级握手记录

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `host` | WebSocket Host 头 |
| `uri` | WebSocket 升级请求 URI |

### 实战样例

```json
{"ts":1788514013.625411,"uid":"C7j23j34cyfPlelUG9","id.orig_h":"10.0.0.40","id.orig_p":36318,"id.resp_h":"10.0.0.50","id.resp_p":8080,"host":"10.0.0.50:8080","uri":"/chat"}
```

### DLP 价值

- **CSWSH 攻击检测**：恶意网站 → 内网 WS 服务（Cross-Site WebSocket Hijacking）
- WS 帧内容（JSON / protobuf）Zeek 不解析

---

## 3.3 sip.log（SIP 会话）

**行数：6**
**职责**：SIP VoIP 协议请求和响应

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `trans_depth` | SIP 事务深度 |
| `method` | **SIP 方法**：`REGISTER` / `INVITE` / `BYE` / `CANCEL` / `ACK` / `OPTIONS` / `SUBSCRIBE` / `NOTIFY` / `PUBLISH` / `INFO` / `PRACK` / `UPDATE` / `REFER` / `MESSAGE` |
| `uri` | **SIP URI**（如 `sip:alice@testbed.local`） |
| `request_from` | From 头（`<sip:alice@testbed.local>`） |
| `request_to` | To 头 |
| `response_from` | 响应的 From |
| `response_to` | 响应的 To |
| `call_id` | **Call-ID**（标识一个 SIP 会话） |
| `seq` | CSeq 头（`1 INVITE`） |
| `request_path` | Via 头路径列表 |
| `response_path` | 响应的 Via |
| `status_code` | SIP 响应码（200/401/486/487） |
| `status_msg` | 状态消息 |
| `request_body_len` | 请求体长度 |
| `response_body_len` | 响应体长度（含 SDP） |

### 实战样例

```json
{"ts":1788513748.982428,"uid":"CuiyP032xu2QHmqyo1","id.orig_h":"10.0.0.40","id.orig_p":45554,"id.resp_h":"10.0.0.50","id.resp_p":5060,"trans_depth":0,"method":"REGISTER","uri":"sip:testbed.local","request_from":"<sip:alice@testbed.local>","request_to":"<sip:alice@testbed.local>","call_id":"register-py-1@testbed","seq":"1 REGISTER","request_path":["SIP/2.0/UDP 10.0.0.40:5060"],"response_path":["SIP/2.0/UDP 10.0.0.40:5060"],"status_code":200,"status_msg":"OK"}
```

### DLP 价值

- **SIP 注册频繁** = 扫描/枚举
- **INVITE 频繁** = VoIP 滥用
- **Call-ID** 关联整个会话（信令 vs RTP）

---

## 3.4 irc.log（IRC 聊天）

**行数：6**
**职责**：IRC 协议消息（频道、昵称、消息）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `command` | **IRC 命令**：`NICK` / `USER` / `JOIN` / `PART` / `PRIVMSG` / `NOTICE` / `QUIT` / `TOPIC` / `NAMES` / `LIST` / `WHO` / `WHOIS` |
| `value` | 命令参数 |
| `nick` | **昵称** |
| `user` | 用户名 |
| `addl` | 附加参数 |
| `request_path` | 路径 |
| `response_path` | 响应路径 |

### 实战样例

```json
{"ts":1788506916.769718,"uid":"Cem8OwaJm0mGWqZDc","id.orig_h":"10.0.0.131","id.orig_p":35331,"id.resp_h":"10.0.0.50","id.resp_p":6667,"command":"NICK","value":"alice"}
{"ts":1788506916.769718,"uid":"Cem8OwaJm0mGWqZDc","id.orig_h":"10.0.0.131","id.orig_p":35331,"id.resp_h":"10.0.0.50","id.resp_p":6667,"nick":"alice","command":"USER","value":"alice","addl":"0 * AliceEmployee "}
{"ts":1788506916.769718,"uid":"Cem8OwaJm0mGWqZDc","id.orig_h":"10.0.0.131","id.orig_p":35331,"id.resp_h":"10.0.0.50","id.resp_p":6667,"nick":"alice","user":"alice","command":"JOIN","value":"#general","addl":""}
```

### DLP 价值

- **PRIVMSG 内容不在日志**（Zeek 不解析消息正文）
- 监控 JOIN/QUIT 频率 = 异常活动
- IRC 是 C2 通道常见协议

---

# 4. 文件传输层

---

## 4.1 ftp.log（FTP 控制）

**行数：12**
**职责**：FTP 控制连接的命令和响应（**完全明文**）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `user` | **FTP 用户名**（明文） |
| `password` | **FTP 密码**（明文！最严重 DLP 风险） |
| `command` | **FTP 命令**：`USER` / `PASS` / `TYPE` / `CWD` / `LIST` / `RETR` / `STOR` / `DELE` / `PASV` / `EPSV` / `QUIT` |
| `arg` | **命令参数**（完整文件名如 `ftp://10.0.0.50/sensitive_financial_q3_2026.xlsx`） |
| `reply_code` | 响应码（`226` = Transfer complete） |
| `reply_msg` | 响应消息 |
| `fuid` | 关联 files.log 文件 UID |
| `data_channel.passive` | 是否 PASV 模式 |
| `data_channel.orig_h` | 数据通道 orig 端 IP |
| `data_channel.resp_h` | 数据通道 resp 端 IP |
| `data_channel.resp_p` | 数据通道 resp 端口 |
| `last_auth_request` | 同一连接最后 auth 请求 |

### 实战样例（FTP 完整流程）

```json
{"ts":1788508301.0,"uid":"C..","id.orig_h":"10.0.0.10","id.resp_p":21,"user":"alice","password":"alice123","command":"USER"}
{"ts":1788508301.0,"uid":"C..","id.orig_h":"10.0.0.10","id.resp_p":21,"command":"PASS","arg":"alice123","reply_code":230}
{"ts":1788508301.0,"uid":"C..","command":"EPSV","reply_code":229,"data_channel.passive":true,"data_channel.resp_p":40058}
{"ts":1788508301.0,"uid":"C..","command":"STOR","arg":"ftp://10.0.0.50/sensitive_financial_q3_2026.xlsx","reply_code":226,"reply_msg":"Transfer complete.","fuid":"Ffj1RM1qA4ZjpyGzi9"}
{"ts":1788513477.7,"command":"RETR","arg":"ftp://10.0.0.50/passwords.txt","reply_code":226,"fuid":"FfXAUh3hVg6HWBrl2e"}
```

### DLP 价值（⭐⭐⭐⭐⭐）

- **密码明文可见** = Zeek 抓 FTP 凭证
- **完整文件名可见** = 文件级 DLP（识别 `.xlsx` `.sql` `.key` `.confidential` 等）
- **`fuid` 关联数据通道**（files.log）
- `is_orig: false` 表示下载（外泄信号）

---

## 4.2 smtp.log（邮件）

**行数：2**
**职责**：SMTP 协议信封、头、附件

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `trans_depth` | SMTP 事务深度 |
| `helo` | **HELO/EHLO** 主机名（客户端标识） |
| `mailfrom` | **MAIL FROM** 发件人（信封） |
| `rcptto` | **RCPT TO** 收件人（**可多个**，数组） |
| `date` | Date 头 |
| `from` | **From 头**（显示名） |
| `to` | **To 头**（多个收件人，数组） |
| `cc` | Cc 抄送 |
| `bcc` | Bcc 密送 |
| `reply_to` | Reply-To |
| `msg_id` | **Message-ID** 头 |
| `in_reply_to` | In-Reply-To（线程关联） |
| `subject` | **邮件主题** |
| `x_other_client_headers` | 其他客户端头 |
| `first_received` | **第一个 Received 头**（传输链） |
| `second_received` | 第二个 Received 头 |
| `last_reply_code` | 最后响应码 |
| `last_reply_msg` | 最后响应消息 |
| `is_webmail` | 是否 Web 邮箱 |
| `user_agent` | 客户端 User-Agent |
| `tls` | 是否 TLS |
| `fuids` | 关联 files.log（附件 UID） |

### DLP 价值

- **主题关键词**：`confidential` / `secret` / `password` / `key`
- **附件 UID** → files.log 查文件名 + 大小
- **收件人为外网域名** = 数据外泄
- **同主题大量收件人** = 群发钓鱼

---

# 5. 基础设施层

---

## 5.1 dns.log（DNS 查询）

**行数：8,709,553**（**最大日志**）
**职责**：所有 DNS 查询和应答

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `version` | 版本 |
| `query` | **查询名称**（关键字段） |
| `query_class` | 查询类（`C_INTERNET`） |
| `query_type` | **查询类型**（`A` / `AAAA` / `MX` / `TXT` / `NS` / `CNAME` / `PTR` / `SOA` / `SRV`） |
| `rcode_name` | **响应码**（`NOERROR` / `NXDOMAIN` / `SERVFAIL` / `REFUSED`） |
| `rcode` | 数字响应码（0/3/2/5） |
| `AA` | Authoritative Answer（权威应答） |
| `TC` | Truncated（截断） |
| `RD` | Recursion Desired（递归期望） |
| `RA` | Recursion Available |
| `Z` | 保留位 |
| `answers` | 应答记录（数组，如 `["10.0.0.20"]`） |
| `TTLs` | TTL 数组 |
| `rejected` | 是否被拒绝 |
| `total_answers` | 答案数 |
| `total_replies` | 回复数 |
| `saw_query` | 是否看到查询 |
| `saw_reply` | 是否看到应答 |
| `trans_id` | DNS transaction ID |

### 实战样例

```json
{"ts":1788500000.0,"uid":"C..","id.orig_h":"10.0.0.10","id.resp_h":"10.0.0.50","id.resp_p":53,"proto":"udp","query":"dc01.testbed.local","query_class":"C_INTERNET","query_type":"A","rcode_name":"NOERROR","rcode":0,"AA":false,"TC":false,"RD":true,"RA":true,"Z":0,"answers":["10.0.0.50"],"TTLs":[3600],"rejected":false}
```

### DLP 价值（⭐⭐⭐⭐）

- **DNS Tunneling 检测**：`query` 字段含 Base64 编码长字符串 = `*.aGVsbG8.evil.com`
- **DNS 异常频率**：单 IP 短时间大量查询 = C2 信道
- **NXDOMAIN 高峰** = DNS rebinding 或扫描
- **请求超长子域名** = DGA（域名生成算法）恶意软件

---

## 5.2 dhcp.log（地址分配）

**行数：130,805**
**职责**：DHCP 租约分配记录

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `mac` | **客户端 MAC 地址** |
| `assigned_addr` | **分配的 IP 地址** |
| `lease_time` | 租约秒数（43200 = 12 小时） |
| `msg_types` | **DHCP 消息类型列表**（如 `["OFFER","ACK"]`） |
| `duration` | 协商时长 |

### 实战样例

```json
{"ts":1788500539.863578,"uid":"CYBQeg4L3MtfGwFl74","id.orig_h":"10.0.0.123","id.resp_h":"10.0.0.50","id.resp_p":67,"mac":"aa:c1:ab:0d:fc:9f","assigned_addr":"10.0.0.123","lease_time":43200.0,"msg_types":["OFFER"],"duration":0.0}
```

### DLP 价值

- **未授权 DHCP 服务器**：识别 rogue DHCP（多个 DHCP OFFER）
- **MAC-IP 映射**：定位设备（绕过 NAT 后的设备识别）
- **新设备检测**：未登记的 MAC 出现 = 内部威胁

---

## 5.3 ntp.log（时间同步）

**行数：3**
**职责**：NTP 协议记录

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `version` | NTP 版本 |
| `mode` | NTP 模式 |
| `stratum` | **层级**（0=unspec, 1=primary, 16=unsync） |
| `poll` | 轮询间隔（秒，log2） |
| `precision` | 精度（log2 秒） |
| `root_delay` | 根延迟 |
| `root_dispersion` | 根离散度 |
| `ref_id` | 参考 ID |
| `ref_time` | 参考时间戳 |
| `org_time` | 原始时间戳（客户端发送） |
| `rec_time` | 接收时间戳（服务器接收） |
| `xmt_time` | 发送时间戳（服务器发送） |
| `num_exts` | 扩展数 |

### DLP 价值

- **NTP 反射攻击**：放大 DDoS
- **异常 stratum 0** = 配置错误或攻击

---

## 5.4 snmp.log（网络管理）

**行数：3**
**职责**：SNMP 请求和响应

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `version` | SNMP 版本（1/2c/3） |
| `community` | **社区字符串**（如 `public` / `private`）——**v1/v2c 明文密码** |
| `get_requests` | get 请求数 |
| `get_bulk_requests` | get-bulk 请求数 |
| `get_responses` | get 响应数 |
| `set_requests` | set 请求数（**修改操作 = 高危**） |
| `trap_pdu` | TRAP 数 |
| `inform_requests` | INFORM 数 |

### DLP 价值

- **community = `public`** = 默认配置，可被嗅探枚举
- **set_request 出现** = 配置修改（攻击）
- **get_bulk** = 信息收集

---

## 5.5 syslog.log（系统日志）

**行数：3**
**职责**：通过 UDP/TCP 传输的 syslog 消息

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `message` | **原始 syslog 消息** |
| `proto` | 传输协议（`udp` / `tcp`） |

### 实战样例

```json
{"ts":1788500000.0,"uid":"C..","id.orig_h":"10.0.0.10","id.resp_h":"10.0.0.50","id.resp_p":514,"message":"<134>Mar 15 14:23:45 server sshd[1234]: Accepted publickey for alice from 10.0.0.10 port 5000 ssh2","proto":"udp"}
```

### DLP 价值

- syslog 内容**完全明文**（Zeek 不解析）
- 审计/合规事件监控

---

# 6. 数据库层

---

## 6.1 postgresql.log（Postgres 查询）

**行数：56**
**职责**：PostgreSQL 客户端-服务器消息

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `version` | PG 协议版本 |
| `msg_type` | 消息类型：`Q`（查询） / `P`（解析/绑定） / `B`（绑定） / `D`（描述） / `E`（执行） / `S`（同步） / `T`（行数据） / `R`（命令完成） / `C`（关闭） / `N`（通知） / `1`-`9`/`Z` |
| `statement` | **SQL 语句**（明文，**最大 DLP 价值**） |
| `param_types` | 参数类型（OID 列表，如 `[23, 25]`） |
| `param_values` | **参数值**（明文，敏感数据） |
| `num_bind_params` | bind 参数数 |
| `result_rows` | 返回行数 |
| `result_sent` | 是否返回行 |
| `command_complete_tag` | 命令完成标签（如 `SELECT 42`） |

### 实战样例

```json
{"ts":1788500000.0,"uid":"C..","id.orig_h":"10.0.0.10","id.resp_h":"10.0.0.20","id.resp_p":5432,"msg_type":"Q","statement":"SELECT * FROM users WHERE username='alice' AND password='P@ssw0rd!'","num_bind_params":0,"result_rows":1}
```

### DLP 价值（⭐⭐⭐⭐⭐）

- **SQL 明文**（包括密码、身份证、电话号码）
- **`UPDATE/DELETE`** 高危（数据篡改）
- **`COPY**` 大文件导出
- **`param_values`** = 数据外泄（批量 SELECT 大量行）

---

## 6.2 redis.log（Redis 命令）

**行数：7**
**职责**：Redis 协议 RESP 消息

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `request_command` | **Redis 命令**（GET / SET / DEL / KEYS / CONFIG / SLAVEOF） |
| `request_arg` | **命令参数**（明文） |
| `response_value` | **响应值**（GET 返回值 = 缓存内容） |
| `response_size` | 响应字节数 |
| `response_status` | 响应状态（如 `OK` / `PONG`） |
| `num_response_args` | 响应参数个数 |
| `is_error` | 是否错误 |

### 实战样例

```json
{"ts":1788500000.0,"uid":"C..","request_command":"GET","request_arg":"session:abc123","response_value":"user_id=42; role=admin"}
{"ts":1788500000.0,"request_command":"CONFIG","request_arg":"SET","response_status":"OK"}
```

### DLP 价值

- **KEYS *** = 枚举所有 key（侦察）
- **CONFIG SET dir /db/dump** = 备份目录配置（数据外泄准备）
- **DEBUG SET-ACTIVE-EXPIRE 0** = Redis 持久化绕过
- **SLAVEOF attacker_ip 6379** = 主从复制劫持（外泄）

---

# 7. 物联网与工控层

---

## 7.1 mqtt_connect.log（MQTT 连接）

**行数：11**
**职责**：MQTT 客户端连接消息

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `proto_name` | 协议名（`MQTT`） |
| `proto_version` | 协议版本（`3.1.1` / `5.0`） |
| `client_id` | **客户端 ID**（设备唯一标识） |
| `connect_status` | **连接结果**：`Connection Accepted` / `Rejected: Protocol_Violation` |
| `session_present` | 会话恢复标志 |

### 实战样例

```json
{"ts":1788513651.74733,"uid":"CRmVLr2CrMH1tT2kh8","id.orig_h":"10.0.0.40","id.orig_p":56510,"id.resp_h":"10.0.0.50","id.resp_p":1883,"proto_name":"MQTT","proto_version":"3.1.1","client_id":"","connect_status":"Connection Accepted"}
```

### DLP 价值

- **client_id 含设备型号**（`sensor-12345` / `factory-pump-3`）
- **拒绝连接** = 攻击或配置错误

---

## 7.2 mqtt_publish.log（MQTT 发布）

**行数：10**
**职责**：MQTT 客户端发布消息

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `from_client` | 是否客户端→broker（vs broker→客户端） |
| `retain` | 是否保留消息（持久化） |
| `qos` | **服务质量**：`at most once` (0) / `at least once` (1) / `exactly once` (2) |
| `status` | 投递状态（`ok`） |
| `topic` | **MQTT 主题**（如 `sensor/temperature` 或 `iot/device/secret`） |
| `payload` | **消息 payload**（明文，最大 DLP 价值） |
| `payload_len` | payload 字节数 |

### 实战样例

```json
{"ts":1788513651.74733,"uid":"CRmVLr2CrMH1tT2kh8","id.orig_h":"10.0.0.40","id.orig_p":56510,"id.resp_h":"10.0.0.50","id.resp_p":1883,"from_client":true,"retain":false,"qos":"at least once","status":"ok","topic":"sensor/temperature","payload":"{\"value\":21,\"unit\":\"C\"}","payload_len":23}
```
```json
{"topic":"iot/device/secret","payload":"api_key=sk-1234567890abcdef","qos":"exactly once","retain":true,"payload_len":30}
```

### DLP 价值（⭐⭐⭐⭐）

- **payload 明文**（包括凭证、传感器数据、消息）
- **topic 命名**：`/secret` `/admin` `/config`
- **`qos: exactly once + retain: true`** = 持久敏感消息

---

## 7.3 mqtt_subscribe.log（MQTT 订阅）

**行数：1**
**职责**：MQTT 订阅请求

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `action` | 订阅动作：`SUBSCRIBE` / `UNSUBSCRIBE` |
| `topics` | **订阅主题列表**（含 QoS） |
| `qos` | 订阅 QoS |
| `granted_qos` | broker 授予的 QoS |

### 实战样例

```json
{"ts":1788510000.0,"uid":"C..","action":"SUBSCRIBE","topics":[{"topic":"sensor/#","qos":1}],"granted_qos":1}
```

### DLP 价值

- **订阅 `+` 或 `#`** = 通配符订阅（关注所有消息）
- **订阅 `/secret`** = 偷取敏感数据

---

## 7.4 modbus.log（Modbus 工控）

**行数：4**
**职责**：Modbus TCP 工业控制协议

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `function_code` | **功能码**：`Read Coils` / `Read Holding` / `Write Single Coil` / `Write Multiple` / `Read/Write Multiple` |
| `request_unit_id` | Modbus 单元 ID（从站地址） |
| `request_reference_num` | 寄存器地址 |
| `request_word_count` | 寄存器数量 |
| `request_bitnum` | 位号 |
| `response_word_count` | 响应寄存器数 |
| `response_bitnum` | 响应位数 |
| `request_data` | **请求数据**（写入值） |
| `response_data` | **响应数据**（读出的值） |
| `request_byte_count` | 请求字节数 |

### 实战样例

```json
{"ts":1788500000.0,"uid":"C..","function_code":"Read Holding","request_unit_id":1,"request_reference_num":100,"request_word_count":10,"response_word_count":10,"response_data":[100,200,300,400,500,600,700,800,900,1000]}
```

### DLP 价值

- **`Write` 操作** = 修改设备参数（攻击）
- **`Read` 操作** = 读取工业数据
- Modbus **无认证**（攻击者可任意读写）

---

# 8. SMB 文件共享

---

## 8.1 smb_files.log（SMB 文件访问）

**行数：4**
**职责**：SMB 文件操作记录

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `action` | **SMB 操作**：`SMB::FILE_OPEN` / `SMB::FILE_READ` / `SMB::FILE_WRITE` / `SMB::FILE_DELETE` / `SMB::FILE_RENAME` |
| `path` | **完整 UNC 路径**（如 `\\10.0.0.50\shared`） |
| `name` | 文件名（`financial.txt` / `<share_root>`） |
| `size` | 文件大小（字节） |
| `times.modified` / `times.accessed` / `times.created` / `times.changed` | MACE 时间戳 |

### 实战样例

```json
{"ts":1788508304.207495,"uid":"C2p7Do39kmp9Dus561","id.orig_h":"10.0.0.40","id.orig_p":48142,"id.resp_h":"10.0.0.50","id.resp_p":445,"action":"SMB::FILE_OPEN","path":"\\\\10.0.0.50\\shared","name":"financial.txt","size":23,"times.modified":1788508074.0226994,"times.accessed":1788508074.02594,"times.created":1788508074.0226994,"times.changed":1788508074.0226994}
```

### DLP 价值

- **完整 UNC 路径** = 知道谁访问什么文件
- **大文件访问** = 数据外泄
- **FILE_DELETE** + **FILE_RENAME** = 破坏活动

---

## 8.2 smb_mapping.log（SMB 共享映射）

**行数：5**
**职责**：SMB 共享树连接（访问哪个共享）

### 字段含义

| 字段 | 含义 |
|------|------|
| `ts` | 时间戳 |
| `uid` | 连接 UID |
| `id.orig_h` / `id.resp_h` / `id.orig_p` / `id.resp_p` | 四元组 |
| `path` | **UNC 路径**（`\\10.0.0.50\IPC$` / `\\10.0.0.50\shared`） |
| `share_type` | **共享类型**：`DISK` / `PIPE` / `PRINT` |

### 实战样例

```json
{"ts":1788508301.067595,"uid":"CDsTS32kEj5n5bydN3","id.orig_h":"10.0.0.40","id.orig_p":49948,"id.resp_h":"10.0.0.50","id.resp_p":445,"path":"\\\\10.0.0.50\\shared","share_type":"DISK"}
{"ts":1788508301.068398,"uid":"CDsTS32kEj5n5bydN3","id.orig_h":"10.0.0.40","id.orig_p":49948,"id.resp_h":"10.0.0.50","id.resp_p":445,"path":"\\\\10.0.0.50\\IPC$","share_type":"PIPE"}
```

### 关键 share_type

| 类型 | 含义 | 攻击信号 |
|------|------|---------|
| `DISK` | 磁盘共享（文件） | 正常访问文件 |
| `PIPE` | **命名管道**（\\pipe\\*） | RPC 操作（结合 dce_rpc.log） |
| `PRINT` | 打印共享 | 罕见 |

### DLP 价值

- **DISK 共享访问** = 文件级 DLP（文件名已知）
- **PIPE 访问** + **多个 DCE-RPC 操作** = 域控枚举/攻击
- **IPC\$ + samr 管道** = 用户枚举

---

## 附录：Zeek 日志总览

| 协议族 | 日志文件 | 行数 | DLP 价值（5星） |
|--------|---------|------|---------------|
| **核心** | conn.log | 96,944 | ⭐⭐⭐⭐ |
| **核心** | files.log | 3 | ⭐⭐⭐⭐⭐ |
| **核心** | weird.log | 191 | ⭐⭐⭐ |
| **核心** | notice.log | 183 | ⭐⭐⭐⭐⭐ |
| **核心** | reporter.log | 1 | ⭐ |
| **核心** | analyzer.log | 1 | ⭐ |
| **核心** | packet_filter.log | 1 | ⭐⭐ |
| **认证** | ssh.log | 20 | ⭐⭐ |
| **认证** | ssl.log | 3 | ⭐⭐⭐ |
| **认证** | kerberos.log | 6 | ⭐⭐⭐ |
| **认证** | ntlm.log | 3 | ⭐⭐⭐⭐⭐ |
| **认证** | ldap.log | 3 | ⭐⭐⭐⭐ |
| **认证** | dce_rpc.log | 17 | ⭐⭐⭐⭐⭐ |
| **认证** | radius.log | 3 | ⭐⭐⭐ |
| **Web** | http.log | 1 | ⭐⭐⭐⭐⭐ |
| **Web** | websocket.log | 1 | ⭐⭐ |
| **Web** | sip.log | 6 | ⭐⭐⭐ |
| **Web** | irc.log | 6 | ⭐⭐ |
| **文件** | ftp.log | 12 | ⭐⭐⭐⭐⭐ |
| **文件** | smtp.log | 2 | ⭐⭐⭐⭐ |
| **基础设施** | dns.log | 8,709,553 | ⭐⭐⭐⭐ |
| **基础设施** | dhcp.log | 130,805 | ⭐⭐⭐ |
| **基础设施** | ntp.log | 3 | ⭐ |
| **基础设施** | snmp.log | 3 | ⭐⭐⭐ |
| **基础设施** | syslog.log | 3 | ⭐⭐ |
| **数据库** | postgresql.log | 56 | ⭐⭐⭐⭐⭐ |
| **数据库** | redis.log | 7 | ⭐⭐⭐⭐ |
| **物联网** | mqtt_connect.log | 11 | ⭐⭐⭐ |
| **物联网** | mqtt_publish.log | 10 | ⭐⭐⭐⭐⭐ |
| **物联网** | mqtt_subscribe.log | 1 | ⭐⭐⭐ |
| **工控** | modbus.log | 4 | ⭐⭐⭐⭐ |
| **SMB** | smb_files.log | 4 | ⭐⭐⭐⭐⭐ |
| **SMB** | smb_mapping.log | 5 | ⭐⭐⭐⭐ |

---

## 总结：Zeek 字段最佳实践

### 给 DLP 系统的建议

1. **重点关注这 5 类日志**（DLP 核心）：
   - **conn.log** - 流量基线 + 异常检测
   - **files.log** + **ftp.log** + **smb_files.log** - 文件级 DLP
   - **http.log** + **ssl.log**（server_name） - Web DLP
   - **postgresql.log** + **redis.log** - 数据库 DLP
   - **mqtt_publish.log** - IoT DLP

2. **加密协议限制**：
   - HTTPS / LDAPS / SMTPS / IMAPS / SSH SFTP：**只能看元数据，无法解析内容**
   - 必须配合终端 DLP / SSL 解密才能看到内容

3. **关联分析**：
   - 使用 **uid 字段**关联 conn.log + http.log + ssl.log
   - 使用 **fuid 字段**关联 files.log + ftp.log + smb_files.log + smtp.log
   - 跨协议追踪：`uid=xxx` 可串起 SSH 握手 + SSH 数据流

4. **告警优先级**：
   - `ftp.log` 明文凭证 → **P0 严重**
   - `dce_rpc.log` DCSync 操作（DRSGetNCChanges）→ **P0 严重**
   - `mqtt_publish.log` payload 含凭证 → **P0 严重**
   - `http.log` SQL 注入 → **P1 高**
   - `weird.log` 异常 → **P2 中**

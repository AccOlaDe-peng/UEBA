# CALDERA 防泄密行为特征分类清单

日期：2026-08-28  
用途：结合 CALDERA abilities 与网络流量、UEBA 异常行为检测清单，整理可用于防泄密监控、检测建模和模拟验证的行为特征。

## 一、分析范围与结论

本清单基于以下材料：

- 本地 CALDERA 副本中的 258 个 ability 文件；
- 《网络流量与 UEBA 框架下的异常行为检测清单》；
- CALDERA 中与 Discovery、Credential Access、Collection、Exfiltration、Lateral Movement、Command and Control、Defense Evasion 相关的能力。

防泄密检测不应只关注“出网流量是否变大”，而应识别完整的数据移动链：

> 身份或权限异常 → 数据定位与收集 → 集中暂存 → 压缩、加密或分片 → 外传或外部共享 → 清理痕迹

CALDERA 本地 abilities 中，与防泄密相关的主要分类数量如下：

| CALDERA 分类 | Ability 数量 | 与防泄密的关系 |
|---|---:|---|
| Discovery | 79 | 定位主机、账号、共享盘、文件、服务和安全软件 |
| Defense Evasion | 30 | 隐藏暂存目录、清理日志、停用防护、混淆执行 |
| Collection | 18 | 文件、邮件、屏幕、剪贴板、音视频及共享盘数据收集 |
| Credential Access | 12 | 获取私钥、注册表凭据、历史命令和系统凭据 |
| Exfiltration | 11 | C2、FTP、S3、Dropbox、GitHub、分片和定时外传 |
| Lateral Movement | 11 | SMB、SSH、WinRM、SCP、USB 及主机间中转 |
| Command and Control | 8 | Web 协议、非常规端口和周期性回连 |

单条行为通常不能证明泄密。只有多项行为落在同一主体、同一时间窗å口，而且数据形态能够衔接时，才适合生成高风险告警。

---

## 二、网络流量行为特征

| 分类 | 关键行为特征 | 对应 CALDERA 能力或技术 |
|---|---|---|
| 异常上传量 | 短时上行突增、上传下载比例反转、POST 请求体增大、单一对端流量集中 | C2 通道外传、FTP、S3、Dropbox、GitHub |
| 新增外传目的地 | 首次访问域名或 IP、个人云盘、外部代码仓库、陌生云 API、非企业邮箱 | T1567.001、T1567.002、T1537 |
| 分片与慢速外传 | 大量等体积小连接、固定时间间隔、长期每日少量上传、定时传输 | T1030、T1029 |
| 异常协议与端口 | 端口协议不匹配、无 DNS 前置解析、裸 TCP/ICMP、非常规端口 | T1048.003、T1571 |
| DNS 隧道 | 超长子域、高熵标签、唯一子域大量增加、TXT 查询异常、查询量与上行数据相关 | 替代协议或 C2 通道 |
| 加密流量异常 | 罕见 JA3/JA4、SNI 与 Host 不一致、自签名证书、不校验证书、TLS 指纹与岗位不匹配 | Web/C2 通道 |
| 机器化通信 | 固定心跳、周期性回连、低抖动请求、大量重复 URI 或相似包长 | CALDERA Agent/C2 |
| 内网收集与暂存 | SMB/NFS 共享读取激增、终端之间突然大流量、横向复制、扫描与连接失败增加 | T1039、T1021、T1570 |
| 内容风险 | 出网文件类型异常、压缩或加密文件占比上升、敏感文件哈希或 DLP 指纹命中 | 压缩后经云服务或 C2 外传 |

### 2.1 分片外传

CALDERA 包含将暂存文件加密压缩并拆成小块的能力。对应的网络特征包括：

- 短时间出现大量等体积或近似体积的上传；
- 文件名、URI 或请求结构高度相似；
- 同一目的地连续接收大量小文件；
- 单次流量不大，但累计上传量明显异常；
- 主机侧先生成连续编号分片，网络侧随后逐个发送。

### 2.2 定时与慢速外传

- 每天或每周固定时段出现类似上传；
- 请求时间间隔规律，随机性低于人工操作；
- 单次上传不超过固定阈值，但长期累计量持续增长；
- 用户自己的历史基线逐渐升高，但相对同岗位群体仍明显偏高；
- 非工作时间的周期性外联与定时任务创建相互对应。

---

## 三、主机文件与进程行为特征

### 3.1 数据发现

- 短时间遍历大量目录和文件；
- 搜索包含“密码、合同、客户、财务、密钥”等关键字的文件；
- 按扩展名批量查找文档、源码、数据库、密钥或配置文件；
- 首次访问敏感目录、其他部门目录、代码仓库或邮件目录；
- 枚举共享盘、网络主机、数据库服务、打印队列和 USB 设备；
- 文件读取频率明显超过人工操作速度；
- 同一进程跨越多个无业务关联目录进行搜索。

对应 CALDERA 示例：

- T1083：File and Directory Discovery；
- T1135：Network Share Discovery；
- T1217：Browser Information Discovery；
- T1120：Peripheral Device Discovery；
- T1518.001：Security Software Discovery。

### 3.2 数据访问与收集

- 单进程短时间打开或读取大量文件；
- 大量复制共享盘、管理员共享或可移动介质中的文件；
- 读取本地邮件、SSH 配置、私钥、浏览器数据或注册表凭据；
- 截屏、剪贴板读取、音频采集或视频采集；
- 个人终端直接连接数据库并执行大结果集查询；
- 数据库导出量与随后产生的本地文件大小接近；
- 脚本或自动化程序的读取节奏明显快于人工操作。

对应 CALDERA 技术：

| 技术 | 行为 |
|---|---|
| T1005 | 从本地系统收集数据 |
| T1039 | 从网络共享盘收集数据 |
| T1025 | 从可移动介质收集数据 |
| T1114.001 | 收集本地邮件 |
| T1113 | 屏幕捕获 |
| T1115 | 剪贴板数据收集 |
| T1123 | 音频捕获 |
| T1125 | 视频捕获 |
| T1119 | 自动化收集 |

### 3.3 数据暂存

- 在 `/tmp`、回收站、用户公共目录或隐藏目录创建暂存目录；
- 不同业务目录的文件突然被复制到同一目录；
- 暂存目录在短时间内快速增长；
- 暂存目录增长后紧接着发生压缩、上传或删除；
- 远程主机、共享目录或容器被用作中转位置；
- 文件创建者、原始路径与暂存路径之间缺乏正常业务关系；
- 临时目录中出现大量敏感扩展名文件。

对应 CALDERA 技术：T1074.001 Local Data Staging。

### 3.4 打包、加密和分片

- `tar`、`zip`、`7z`、`gpg` 等进程处理大量敏感文件；
- 新产生大体积压缩包、加密包或大量连续编号分片；
- 压缩包生成后很快被网络进程读取；
- 有压缩文件落盘，但没有常见压缩程序，可能使用代码库或 API 打包；
- 压缩、上传和删除在短时间内连续发生；
- 压缩包位于临时目录、回收站或隐藏目录；
- 加密口令通过命令行、脚本参数或环境变量传递。

对应 CALDERA 技术：

- T1560.001：Archive via Utility；
- T1030：Data Transfer Size Limits。

### 3.5 外传进程

- 非批准进程主动访问 Dropbox、S3、GitHub、FTP 或外部 Web API；
- `curl`、云 CLI、Git 客户端或脚本解释器读取压缩包后建立外联；
- 云盘客户端的进程活动与上传流量不匹配；
- 浏览器、办公软件或脚本进程上传大量本地文件；
- 容器内外出现异常文件复制或挂载目录搬运；
- 上传进程的父进程、运行账号或启动路径偏离正常基线；
- 上传完成后源文件或暂存目录被快速删除。

---

## 四、身份、权限与 UEBA 特征

### 4.1 登录和账号异常

- 非工作时间登录后立即访问或导出敏感数据；
- 从新设备、新 IP 或新地理位置首次访问敏感系统；
- 多次认证失败后成功，随后发生批量读取；
- 沉寂账号、刚创建账号或服务账号突然交互式使用；
- 单账号横向登录大量主机；
- 单台主机短时间出现大量不同账号；
- 两次登录位置在时间上不可能到达。

### 4.2 权限和访问范围异常

- 权限提升或授权变更后立即大量访问；
- 用户访问范围超出岗位、项目或部门边界；
- 第一次访问高敏感目录或数据库表；
- 被拒绝的权限请求密集增加；
- 服务账号被用于终端登录、命令执行或人工查询；
- 个人终端绕过应用服务器直接连接数据库；
- 安全、审计或运维账号查询对象超出职责范围。

### 4.3 群体偏离和长期趋势

- 单项行为未超固定阈值，但“登录范围＋文件读取＋压缩＋外联”整体偏离同岗位；
- 离职、调岗等风险窗口内，读取量、导出量或外联目的地逐渐变化；
- 用户长期缓慢抬高个人基线，但相对同岗位仍持续偏高；
- 用户行为向量持续远离岗位群体中心；
- 平时只访问少量系统的用户突然横跨多个业务域。

### 4.4 UEBA 基线设计

UEBA 至少应同时维护三类基线：

1. **个人历史基线**：判断当前行为是否偏离用户自身习惯；
2. **同岗位或同部门基线**：发现长期缓慢抬高个人基线的泄漏行为；
3. **企业全局罕见度**：识别全局少见的工具、协议、目的地和操作链。

只与个人历史相比容易被长期缓慢泄漏污染；只与群体相比又可能误伤职责特殊的用户，因此三类基线需要联合评分。

---

## 五、应用、云与物理通道

### 5.1 应用与协作平台

- 知识库、CRM、IM、邮箱批量查询或导出；
- 创建外部邮件自动转发规则；
- 使用个人邮箱发送大附件；
- 代码仓库大量 clone、下载、压缩或推送到个人仓库；
- Webhook 突然向外部地址发送大量数据；
- 大模型网页或客户端出现大段剪贴板粘贴、敏感文件上传；
- 用户导出量显著高于同岗位人员。

### 5.2 云环境

- 云存储桶、快照或共享链接突然授权给外部账号；
- 从一个云账号向另一个非企业账号转移数据；
- AWS CLI、云 SDK 或第三方同步工具首次出现在终端；
- 云对象下载量与终端出网量在时间和数量上相互对应；
- API Token 被异常进程读取后发生云端访问；
- 云访问来源从应用服务器变成个人终端。

### 5.3 物理与端点通道

- USB 大量写入或敏感文件复制；
- 蓝牙传输、AirDrop 或其他近场传输；
- 打印敏感文档或打印量显著增加；
- 高频截屏或录制敏感业务界面；
- 摄像头、麦克风在非业务场景被占用；
- 浏览器 Cookie 库被非浏览器进程读取；
- 非批准程序注册键盘钩子或读取剪贴板。

---

## 六、规避与破坏监控行为

- 清除系统日志、命令历史或临时文件；
- 停止 EDR、审计代理、防火墙或日志采集服务；
- 修改审计、脱敏、留存、镜像流量或检测抑制规则；
- 隐藏暂存目录或修改文件属性；
- 伪装文件名，将工具复制成系统文件名；
- 使用混淆命令行或异常父子进程链；
- 使用签名系统程序代理执行；
- 删除 Agent、Payload、压缩包或暂存目录；
- 原始事件量稳定，但某条检测命中率突然下降；
- 主机审计心跳消失的同时，网络侧出现异常上传；
- 检查安全软件、虚拟化环境或调试器后再决定是否执行；
- 延迟执行或使用固定睡眠规避短时间沙箱分析。

这些行为本身不一定属于泄密，但如果与数据收集、压缩或外传同时出现，应显著提高事件风险分。

---

## 七、跨域关联与高置信度行为链

建议优先实现以下关联检测：

### 7.1 批量文件外传

> 批量文件读取 → 暂存目录增长 → 压缩包生成 → 新云端点上传

重点关联：用户、主机、原始文件、压缩包、上传进程、目的域名和时间窗口。

### 7.2 数据库数据外传

> 数据库大结果集 → 导出文件落盘 → 压缩或加密 → 出网字节近似匹配

数据库导出量、本地文件大小和出网字节量可以进行近似数量守恒分析。

### 7.3 分片慢速外传

> 敏感目录首次访问 → 机器化读取 → 大量等体积分片 → 周期性外联

单次流量可能不超过阈值，应结合文件分片事件、时间规律和累计上传量。

### 7.4 身份异常驱动的外传

> 异常登录或提权 → 跨部门数据访问 → 外部邮箱或云盘上传

身份风险和数据风险应汇聚到同一主体，而不能由不同平台分别告警。

### 7.5 代码资产外传

> 代码仓库大量读取 → 仓库压缩 → GitHub、Gist 或外部仓库上传

重点判断目标仓库是否属于企业、用户是否有正常发布职责，以及上传量是否符合岗位基线。

### 7.6 防护规避后的外传

> 安全工具停用或日志清除 → 暂存文件删除 → 同期异常出网

主机遥测缺失不能简单视为“无风险”，应由网络流量和平台健康监控进行互证。

### 7.7 服务账号和内网中转

> 服务账号交互登录 → 共享盘批量读取 → 内网中转 → 外传

需要统一关联服务账号、操作者、来源终端、跳板机和最终出网终端。

### 7.8 非文件型数据外传

> 截屏或剪贴板采集 → 浏览器或大模型上传 → 本地痕迹快速清除

此类数据可能不会出现传统文件读写，需要端点 DLP、浏览器遥测和网络侧联合检测。

---

## 八、告警评分建议

可以按照以下维度进行组合评分：

| 维度 | 低风险 | 中风险 | 高风险 |
|---|---|---|---|
| 数据敏感度 | 普通业务文件 | 内部文件 | 密钥、客户、财务、源码等高敏数据 |
| 行为新颖度 | 用户经常执行 | 偶尔执行 | 用户及岗位首次出现 |
| 数据量 | 个人正常范围 | 超个人基线 | 同时超个人和岗位基线 |
| 目的地 | 企业批准服务 | 新外部服务 | 个人账号、匿名网络或威胁情报命中 |
| 自动化程度 | 人工操作 | 疑似脚本 | 高频、固定间隔、批量机器化操作 |
| 行为链长度 | 单点行为 | 两阶段关联 | 三阶段及以上、数据形态连续 |
| 规避行为 | 无 | 有混淆或隐藏 | 停审计、清日志、删痕迹 |
| 身份风险 | 正常身份 | 新设备或异常时段 | 服务账号滥用、提权、跨部门访问 |

推荐处置原则：

- 单点低风险行为只记录为线索；
- 两阶段行为结合敏感度与岗位基线决定是否告警；
- 三阶段以上且数据形态可衔接的行为链应优先告警；
- 收集、外传和规避同时出现时，应直接提升为高风险事件；
- 涉及密钥、凭据、客户数据、核心源码时，应降低告警阈值。

---

## 九、CALDERA 覆盖边界

CALDERA 适合验证以下检测能力：

- 主机文件发现、自动化收集和暂存；
- 主机进程、命令行、压缩、加密和分片遥测；
- Dropbox、S3、GitHub、FTP、C2 等外传通道；
- 共享盘、远程服务和主机间中转；
- 凭据访问、日志清理、停用防护和隐藏痕迹；
- 多阶段攻击链的跨源关联能力。

CALDERA 原生 abilities 对以下场景覆盖相对有限，需要额外构造安全测试数据或自定义 ability：

- CRM、知识库、IM、邮件 SaaS 批量导出；
- 数据库真实查询结果和业务语义；
- 大模型粘贴和文件上传；
- 云权限、快照和共享策略变更；
- USB、打印、蓝牙和 AirDrop 等物理通道；
- 离职、调岗、岗位画像等组织上下文；
- DLP 内容指纹、数据分类分级和敏感字段识别。

因此，CALDERA 应作为防泄密模拟验证的一部分，而不能作为完整的数据泄露场景全集。

---

## 十、数据源建议

| 监控面 | 建议数据源 |
|---|---|
| 网络 | Zeek、DNS、代理、网关、TLS 指纹、NetFlow、邮件网关、云访问安全日志 |
| 主机 | EDR、进程创建、命令行、文件审计、目录监控、系统日志、定时任务、外设日志 |
| 身份 | IAM、AD、SSO、VPN、MFA、堡垒机、权限审批和账号生命周期 |
| 应用 | CRM、知识库、代码平台、邮箱、IM、网盘和大模型审计日志 |
| 数据库 | 查询审计、返回行数、导出事件、敏感表标签和连接来源 |
| 云 | CloudTrail 或同类审计、对象存储访问、共享策略、快照和 Token 使用记录 |
| 组织 | 部门、岗位、项目、数据权限、调岗离职和风险窗口信息 |
| 平台健康 | Agent 心跳、采集延迟、规则变更、抑制配置和检测命中趋势 |

---

## 十一、总体判断原则

1. **异常不等于泄密。** 压缩、上传和批量读取都可能是正常业务行为。
2. **数据形态必须可衔接。** 前一步的输出应能成为后一步的输入。
3. **同一主体是关联基础。** 需要统一用户、账号、设备、主机、进程和云身份。
4. **时间顺序必须合理。** 数据发现、收集、暂存、打包和外传一般具有阶段顺序。
5. **个人与群体基线缺一不可。** 长期慢速泄漏可能逐渐污染个人基线。
6. **网络与主机需要互证。** 主机审计缺失时不能将其解释为没有风险。
7. **规避行为是风险放大器。** 收集或外传同时伴随清日志、停防护时，可信度显著提高。
8. **内容敏感度决定处置优先级。** 相同行为作用于不同数据，风险等级应不同。


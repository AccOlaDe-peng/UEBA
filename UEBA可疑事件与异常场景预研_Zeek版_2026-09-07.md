# UEBA 可疑事件与异常场景预研（仅 Zeek）

> 日期：2026-09-07  
> 依据：`Zeek日志字段详解_2026-09-04.md`（Zeek 8.2.2 实测字段）  
> 目标：说明仅依赖 Zeek 日志时，哪些可疑事件能够检测、使用哪些日志和字段、怎样聚合判断，以及哪些结论不能可靠得到。

## 1. 结论

仅使用 Zeek，最适合检测的是网络侦察、认证失败聚集、横向连接扩散、异常远程管理、DNS 隧道/信标、明文协议中的文件与数据异常、数据库批量读取、SMB 文件访问、工控写操作及采集链路异常。

Zeek 能回答“哪台 IP 在何时与谁通过什么协议通信、通信量和协议元数据是什么”，部分明文协议还能回答“访问了哪个对象、执行了什么协议命令”。但它通常不能可靠回答：

- 操作者对应的真实人员是谁；
- 主机上启动了哪个进程、命令行是什么；
- 账号是否已停用、是否绕过 MFA、是否获得了某项权限；
- HTTPS、SSH/SFTP、加密 SMB 中的正文、命令和文件名；
- 文件是否真正包含敏感数据（除非另有文件提取、哈希或内容检测能力）。

因此，Zeek 事件应优先以 `host/IP` 为实体；只有 `ntlm.username`、`kerberos.client`、`radius.username`、`ftp.user` 等字段存在时才附加账号实体，而且仍需 DHCP/资产台账校正归属。

## 2. 判定约定与数据前提

### 2.1 判定方式

| 代码 | 含义 | 使用方式 |
|---|---|---|
| R | 规则 | 单条或少量日志命中明确策略，如禁止协议、危险 RPC 操作 |
| Q | 聚合 | 在时间窗内按源、目标、账号或对象聚合计数/去重 |
| B | 基线 | 与实体自身、同角色主机、同协议历史分布比较 |
| S | 评分 | 多个弱特征加权，超过阈值才生成事件 |

### 2.2 必须先配置的上下文

1. `local_nets`、出口/NAT、代理、DNS、域控、文件服务器、数据库和扫描器资产清单。
2. 允许协议、端口、远程管理源、维护窗口、邮件外部域和敏感路径/表/主题清单。
3. DHCP 租约映射：以 `dhcp.ts + assigned_addr + mac + lease_time` 将动态 IP 归属到设备。
4. 数据质量门槛：`conn.missed_bytes`、`files.missing_bytes`、`packet_filter` 丢包异常时降低置信度或暂停相关检测。
5. 方向语义：`id.orig_h` 是连接发起方，不必然是内网客户端；`orig_bytes/resp_bytes` 是相对发起方/响应方的有效载荷字节，不可直接固定命名为上传/下载。

### 2.3 基线建议

- 训练期：至少 28 天，覆盖工作日、周末和月末；季节性强的业务建议 8～12 周。
- 比较层级：实体自身 → 同资产角色 → 全企业；新实体先用同角色基线。
- 鲁棒阈值：优先使用中位数和 MAD，候选条件可取 `x > median + 6*MAD` 且超过绝对下限。
- 低基数类别：使用“过去 30 天未见”或出现频率 `<1%`，同时要求最小样本量。
- 阈值均为起始值，必须通过正常流量回放与攻击 Demo 校准，不能直接作为生产最终值。

## 3. 可疑事件检测矩阵

表格中的字段均使用 `日志名.字段名` 表示。实现时遵循以下关联优先级：

1. 协议日志之间优先使用 `uid` 等值关联，例如 `http.uid = conn.uid`；文件对象使用 `fuid`，例如 `ftp.fuid = files.fuid`。
2. 没有共同 UID 时，使用规范化后的实体键和时间窗口关联，例如同一源设备、目标 IP、目标端口在前后 5 分钟内的 DNS 与 TLS 行为；此类关联必须降低置信度。
3. IP 到设备的转换使用事件发生时刻有效的 DHCP 租约：`事件.id.orig_h = dhcp.assigned_addr`，且事件时间位于租约有效期内；不能使用设备当前 IP 反推历史事件。
4. 表中的 `COUNT`、`SUM`、去重数和比例都只计算满足过滤条件的记录；缺少作为判断依据的关键字段时，不以默认值代替，而是停止该条检测或降级为弱信号。
5. “成功”必须由协议结果字段确认。只有连接建立、请求发出或字节量增加时，不得自动解释为认证、修改、下载或写入成功。

### 3.1 身份认证与目录服务

| ID | 可疑事件 | 行为解释 | 方式 | 主判断日志及字段 | 关联日志及关联方式 | 详细起始判据 | 常见正常原因及误报排除条件 |
|---|---|---|---:|---|---|---|---|
| ZE-001 | SSH 单连接多次认证 | 同一次 SSH 远程连接中反复尝试验证身份，像是在同一扇门前多次尝试钥匙；也可能是客户端依次尝试多个合法密钥。 | R | `ssh.log`：`ts, uid, id.orig_h, id.resp_h, id.resp_p, auth_attempts` | 以 `uid` 关联 `conn.log.uid`，补充 `conn_state, duration, orig_bytes, resp_bytes` | 每条 `ssh.log` 直接判断：`auth_attempts >= 5`。事件主体=`id.orig_h`，目标=`id.resp_h:id.resp_p`；若字段缺失则不检测 | 跳板机、自动化发布机；SSH 中该字段不等于精确的失败次数，结论写成“单连接认证尝试异常” |
| ZE-002 | SSH 暴力破解聚集 | 一台设备短时间反复连接同一台 SSH 服务器并尝试认证，可能在自动猜密码；网络日志不足以确认猜中了哪个账号。 | Q | `ssh.log`：`ts, uid, id.orig_h, id.resp_h, id.resp_p, auth_attempts` | `ssh.uid = conn.uid`；使用 `conn.conn_state` 判断连接结果，用 DHCP 的 `assigned_addr, mac, ts, lease_time` 映射源设备 | 按 `(id.orig_h,id.resp_h,id.resp_p)` 建 10 分钟滑窗：`COUNT(*) >= 5` 且 `SUM(auth_attempts) >= 20`；`REJ/S0/RST*` 比例高时加分。Zeek SSH 无可靠账号/成败字段，因此不得输出“某账号爆破成功” | 漏洞扫描器、跳板机、错误自动化任务 |
| ZE-003 | 密码喷洒（NTLM/RADIUS） | 短时间对许多账号各尝试少量登录，类似拿几个常见密码逐个试账号。日志能发现这种模式，但通常看不到是否使用了同一个密码。 | Q | `ntlm.log`：`ts,id.orig_h,id.resp_h,username,success`；或 `radius.log`：`ts,id.orig_h,id.resp_h,username,result` | 同一协议内聚合；`uid` 关联 `conn.log`；不得跨 NTLM/RADIUS 直接合并同名账号，除非已有账号规范化 | 按 `(日志类型,id.orig_h,id.resp_h)` 建 10 分钟窗：失败账号去重数 `>=10`、失败总数 `>=10`，且每账号失败 `1～3` 次；NTLM 失败=`success=false`，RADIUS 失败=`result=failed` | VPN/NAS 健康检查、共享代理/NAT；服务账号批量失效通常每账号会持续重复失败 |
| ZE-004 | 单账号暴力破解 | 盯住同一个账号连续尝试登录并大量失败，可能是在反复猜密码，也可能是程序一直使用旧密码。 | Q | `ntlm.log`：`username,success,id.orig_h,id.resp_h,ts`；或 `radius.log`：`username,result,id.orig_h,id.resp_h,ts` | `uid` 关联 `conn.log`；账号统一为 `domainname\\username`（NTLM）或规范化后的 RADIUS username | 按 `(账号,id.resp_h)` 建 10 分钟窗：失败数 `>=10`，来源 IP 去重数 `<=3`；失败率 `>=80%`。若来源 IP 大量轮换，应另建“分布式爆破”检测 | 保存旧密码的服务、密码刚变更、账号迁移 |
| ZE-005 | 失败后成功认证 | 一个账号先连续认证失败，随后认证成功。可能是用户终于输入正确，也可能是猜测或盗用凭据成功，需要看来源和后续行为。 | Q | `ntlm.log` 或 `radius.log`：使用与 ZE-003/004 相同的账号、来源、目标和结果字段 | 在同一日志类型内按 `(规范化账号,id.resp_h)` 排序；可用 `uid` 关联成功连接的 `conn.log` | 前序 15 分钟失败 `>=5`，之后出现成功：NTLM `success=true` 或 RADIUS `result=success`；若成功来源与失败来源相同则高置信，不同来源仅中置信。SSH 不适用 | 用户多次输错后正常登录；结合来源设备、时间和后续行为判断 |
| ZE-006 | 非常用认证协议 | 账号或设备突然使用以前很少用的身份验证协议，例如平常用 Kerberos，突然改用 NTLM；可能是系统调整，也可能是异常账号使用。 | B | `kerberos.log`：`client,request_type,success,id.orig_h,id.resp_h,ts`；`ntlm.log`：`username,success,...`；`radius.log`：`username,result,...` | 将三类日志映射为统一 `auth_protocol={kerberos,ntlm,radius}`；账号必须经过域名/realm 规范化 | 对 `(账号或源设备,auth_protocol)` 统计过去 30 天成功使用次数和占比；当前协议过去未见，或占比 `<1%` 且历史成功认证样本 `>=100` 时命中 | 域故障导致 NTLM 回退、系统升级、应用切换认证方式 |
| ZE-007 | 管理员账号异常认证 | 管理员等高权限账号从不常用的电脑、未批准的网段或异常时间通过身份验证，值得核实是否为本人或批准的运维操作。 | R+B | `kerberos.log.client`、`ntlm.log.username/domainname/success`、`radius.log.username/result`，以及各日志 `id.orig_h,id.resp_h,ts` | 账号与特权账号清单关联；源 IP 与管理网段/堡垒机清单关联；DHCP 映射设备 | 仅对认证成功记录判断：账号属于特权清单，且满足任一条件：源不在批准管理网段、时间不在维护窗、源设备过去 30 天未使用该账号。命中两项及以上升为高危 | 已批准应急运维；临时堡垒机必须有时间有效的审批记录 |
| ZE-008 | Kerberos 请求量异常 | 设备短时间向域认证服务器申请大量访问凭证，或申请访问许多不同服务；可能是在批量访问业务，也可能是在探查域内服务。 | Q+B | `kerberos.log`：`ts,id.orig_h,id.resp_h,request_type,client,service,success,error_code,error_msg` | `uid` 关联 `conn.log`；按 `client` 规范化账号，按 `service` 提取 SPN | 按 `(id.orig_h,client)` 建 10 分钟窗：`request_type=TGS` 数量 `>=50` 或不同 `service` 数 `>=20`，并且任一指标超过该实体相同时段基线 `median+6*MAD` | 应用服务器、批处理、域登录风暴；阈值应按资产角色区分 |
| ZE-009 | 异常票据属性 | 用于证明身份的 Kerberos 票据，其有效期、加密方式或可转发等设置不符合策略或历史习惯；属于异常凭证线索。 | R+B | `kerberos.log`：`ts,client,service,from,till,cipher,forwardable,renewable,success` | 与域策略表关联允许的票据时长和加密类型；与服务账号/SPN 清单关联 | 对成功响应计算 `ticket_lifetime=till-from`：超过域策略上限即命中；`cipher` 不在允许集合或票据标志从历史常态突变时加分。缺少 `from/till` 时不做时长判断 | 跨域信任、特殊服务票据；不能仅凭长时票据断言 Golden Ticket |
| ZE-010 | LDAP 认证/操作失败聚集 | 设备在企业账号目录服务上反复登录、查询或修改失败，可能是权限探测，也可能是账号失效或应用配置错误。 | Q | `ldap.log`：`ts,uid,id.orig_h,id.resp_h,opcode,result,status,error_code,object` | `uid` 关联 `conn.log`；按 `opcode` 分开统计 bind、search、modify 等操作 | 按 `(id.orig_h,id.resp_h,opcode)` 建 10 分钟窗：总操作 `>=10`，且失败数 `>=10` 或失败率 `>=80%`；失败定义为 `result` 非 success，必要时结合 `status/error_code` | 失效服务账号、应用配置错误、目录服务故障 |
| ZE-011 | LDAP 目录枚举 | 设备集中查询企业目录里的用户、组、电脑等信息，相当于批量翻查内部通讯录和资产名单，可能是在摸清组织和系统结构。 | Q | `ldap.log`：`ts,id.orig_h,id.resp_h,opcode,object,argument,result` | `uid` 关联 `conn.log`；源 IP 关联资产角色；`object` 规范化为搜索基准 DN | 按 `(id.orig_h,id.resp_h)` 建 5 分钟窗，仅统计 `opcode=search`：请求数 `>=50` 或不同 `object` 数 `>=20`，且源不是目录同步/资产发现资产；成功结果占比高时提高置信度 | IAM 同步、EDR、资产发现、邮件地址簿查询 |
| ZE-012 | LDAP 账号/权限对象修改 | 通过 LDAP 目录协议新增、修改或删除账号、组等目录对象，尤其涉及敏感对象或非管理设备；具体改了什么权限还要看日志是否完整。 | R | `ldap.log`：`ts,uid,id.orig_h,id.resp_h,opcode,object,argument,result,status` | `object` 与敏感 DN/OU/组清单匹配；`uid` 关联连接；源 IP 关联批准管理资产 | 单条记录满足 `opcode in (modify,add,delete)`、`result=success`，且 `object` 命中敏感目录对象，或来源不是批准管理资产。只能表述为“观察到目录写操作”；变更内容不完整时不能断言具体权限变化 | 域管/IAM 自动化；按变更单、来源和对象范围排除 |
| ZE-013 | 明文/弱认证尝试 | 使用缺少传输保护或不符合安全策略的认证方式，使凭据存在暴露风险；这说明认证方式有风险，不代表凭据已经被盗。 | R | `ldap.log`：`opcode,diagnostic_message`；`ftp.log`：`command,user,password`；`http.log`：`username,password`；`snmp.log`：`version,community` | 以各日志自身四元组和时间定位连接；必要时用 `uid` 关联 `conn.log` 确认方向 | 任一条件命中：LDAP simple bind 且未使用加密端口/StartTLS；FTP 出现 USER/PASS；HTTP `username/password` 非空；SNMP `version in (1,2c)` 或 `community in (public,private)`。只告警协议暴露，不在告警中展示完整密码 | 合规扫描、遗留设备；敏感字段必须脱敏、限权和缩短保存期 |

### 3.2 网络侦察、横向移动与远程管理

| ID | 可疑事件 | 行为解释 | 方式 | 主判断日志及字段 | 关联日志及关联方式 | 详细起始判据 | 常见正常原因及误报排除条件 |
|---|---|---|---:|---|---|---|---|
| ZE-014 | 协议与端口不匹配 | 实际识别出的协议运行在企业未批准的端口上，例如 SSH 使用了不允许的端口；可能是特殊配置，也可能是在躲避端口管控。 | R | `conn.log`：`ts,uid,id.orig_h,id.resp_h,id.resp_p,proto,service` | 与“资产角色—协议—允许端口”策略表匹配；协议专用日志用同一 `uid` 验证 DPD 识别 | `service` 非空且 `(service,proto,id.resp_p)` 不在允许集合；例如 `service=ssh` 且端口不在批准 SSH 端口。仅按端口推测协议不得命中 | 合法端口迁移、反向代理、Zeek DPD 误识别 |
| ZE-015 | 使用禁止协议 | 某类设备使用了企业明确禁止它使用的通信协议，例如办公终端使用明文 FTP；是否违规取决于该设备适用的策略。 | R | `conn.log.service`；协议专用日志如 `ftp.log,irc.log,snmp.log,ldap.log` 的存在 | 源资产通过 `id.orig_h + ts` 关联 DHCP/CMDB 角色，再匹配角色的禁止协议表 | `service` 或专用日志确认协议，且该协议属于源资产角色禁止集合；连接至少已建立或专用日志已有有效记录 | 按资产角色配置；扫描探测但未建立会话可降级 |
| ZE-016 | 非授权直连外网 | 本应通过企业代理上网的设备，直接与外部服务器通信，可能绕过访问审计或过滤，也可能是配置或例外清单遗漏。 | R | `conn.log`：`ts,id.orig_h,id.resp_h,id.resp_p,local_orig,local_resp,orig_bytes,resp_bytes,conn_state` | 源资产关联“必须走代理”策略；目标关联外网/例外地址表；代理 IP 清单用于排除 | `local_orig=true AND local_resp=false`，源资产要求走代理，目标不在直连例外，且 `conn_state=SF` 或有效载荷字节 `orig_bytes+resp_bytes>0` | CDN、更新服务、Local_Nets 配置错误、旁路安全设备 |
| ZE-017 | 内网主机扫描 | 一台设备短时间接触很多内网 IP，像是在逐个查看哪些电脑或服务器在线；需要排除批准的资产扫描。 | Q | `conn.log`：`ts,id.orig_h,id.resp_h,id.resp_p,local_orig,local_resp,conn_state,duration` | DHCP 映射源设备；源资产关联扫描器清单 | 按 `id.orig_h` 建 5 分钟窗，仅统计 `local_orig=true AND local_resp=true`：不同 `id.resp_h >=30`，并且失败状态 `REJ/S0/RSTO/RSTR` 比例 `>=60%` 或 `duration<1s` 比例 `>=80%` | 授权扫描器、监控、服务发现；白名单必须限制扫描时间和目标范围 |
| ZE-018 | 内网端口扫描 | 一台设备连续尝试同一目标的许多端口，像是在检查一栋楼有哪些门可进入，以发现可连接的服务。 | Q | `conn.log`：`ts,id.orig_h,id.resp_h,id.resp_p,proto,conn_state,duration` | 源设备/扫描器清单；按同一目标聚合 | 按 `(id.orig_h,id.resp_h)` 建 5 分钟窗：不同 `(proto,id.resp_p)` `>=20`，且失败状态比例 `>=60%`；目标必须为内部地址 | 运维探测、漏洞扫描、端口健康检查 |
| ZE-019 | 连接失败聚集 | 设备短时间出现大量连接被拒绝、无响应或异常断开；可能是探测活动，也可能是网络或服务故障。 | Q | `conn.log`：`ts,id.orig_h,id.resp_h,id.resp_p,conn_state` | 与网络设备故障/变更窗口关联；源资产角色用于分类 | 按 `id.orig_h` 建 5 分钟窗：`conn_state in (REJ,S0,RSTO,RSTR)` 数量 `>=50`，并且不同目标 `>=10` 或不同端口 `>=10`。若多个源同时异常，优先判断网络故障 | 网络故障、ACL 变更、服务批量下线 |
| ZE-020 | 横向连接扩散 | 一台设备突然通过远程管理或共享协议连接许多内网主机，访问范围迅速扩大；只说明连接扩散，不能据此确认已执行远程命令。 | Q+B | `conn.log`：`ts,uid,id.orig_h,id.resp_h,id.resp_p,service,local_orig,local_resp,conn_state` | 同 `uid` 或四元组+近邻时间关联 `ssh.log,ntlm.log,kerberos.log,smb_mapping.log,dce_rpc.log`；DHCP 映射源设备 | 按源设备建 30 分钟窗，只统计内网远程管理连接：端口 22/445/3389/5985/5986 或识别出的 SSH/SMB；成功/有数据的不同目标 `>=10`，且超过该角色基线 `median+6*MAD` | 堡垒机、软件分发、补丁和备份系统 |
| ZE-021 | 异常远程管理协议 | 设备以不符合职责或使用习惯的方式远程管理其他主机，例如普通办公电脑在深夜首次连接服务器的远程桌面。 | B | `conn.log`：`id.orig_h,id.resp_h,id.resp_p,service,ts,conn_state` | `ssh.client`、`ntlm.username/success`、`smb_mapping.path/share_type` 用 `uid` 关联；资产角色与维护窗作为上下文 | 对成功或有数据的远程管理连接判断：源角色不允许该协议，或 `(源设备,目标,协议)` 过去 30 天未见，或发生在非维护时段；三项分别计分，两项命中生成事件 | 临时运维、资产迁移；首次出现只能作为一个评分项 |
| ZE-022 | RPC 域/主机枚举 | 通过 Windows 远程调用接口询问用户、组、主机或策略信息，类似远程索取系统名单和配置，可能用于内部侦察。 | R+Q | `dce_rpc.log`：`ts,uid,id.orig_h,id.resp_h,named_pipe,endpoint,operation` | `uid` 关联 `smb_mapping.log` 的 `path,share_type` 和 `ntlm.log.username,success` | 单条命中明确枚举 operation，或按 `(id.orig_h,id.resp_h)` 5 分钟内枚举类 operation `>=5` 且不同 operation `>=3`；关注 `samr,lsarpc,wkssvc` | 域管工具、资产盘点、终端管理系统 |
| ZE-023 | 疑似 DCSync 网络行为 | 非批准设备向域控发起通常用于同步目录数据的请求，可能在模仿域控复制行为以获取账号相关数据；看到请求不等于凭据已被窃取。 | R | `dce_rpc.log`：`ts,uid,id.orig_h,id.resp_h,endpoint,operation,named_pipe` | 源/目标关联域控和授权复制服务器清单；`uid` 关联 NTLM/Kerberos 身份线索 | `endpoint=drsuapi` 且 `operation` 为目录复制相关操作（如 `DRSGetNCChanges`），同时源不是域控或授权同步服务器；operation 未解析时只能输出低置信“异常 drsuapi 访问” | 合法域控复制、身份同步、备份产品 |
| ZE-024 | IPC$/命名管道异常使用 | 设备异常频繁地使用 Windows 共享中的进程通信入口或命名管道与多台主机交互，可能用于枚举或远程管理；这些通道本身也常用于正常业务。 | Q+B | `smb_mapping.log`：`ts,uid,id.orig_h,id.resp_h,path,share_type`；`dce_rpc.log`：`named_pipe,endpoint,operation` | 两日志以 `uid` 关联；`ntlm.log` 同 `uid` 补充 `username,success` | 按 `id.orig_h` 建 30 分钟窗：访问 `path` 以 `IPC$` 结尾或 `share_type=PIPE` 的不同目标 `>=10`，并在关联 RPC 中出现管理/枚举端点；只有 IPC$ 映射而无 RPC 时降级 | Windows 管理、监控、软件分发 |
| ZE-025 | 未授权 DHCP 服务器 | 未经批准的设备向其他设备提供 IP 地址分配服务，可能让终端使用错误的网络配置；需先排除合法备用服务器和转发设备。 | R | `dhcp.log`：`ts,uid,id.orig_h,id.resp_h,mac,assigned_addr,msg_types,lease_time` | 与批准 DHCP 服务器/relay 清单匹配；同一 MAC/IP 和近邻时间关联 OFFER/ACK | 发送 OFFER/ACK 的服务器地址不在批准清单，或同一客户端协商窗口内收到来自多个非批准服务器的 OFFER。需按实际采集方向确认服务器字段，不能固定假设 orig/resp | DHCP relay、双机热备、NAC；先用实测确认报文字段方向 |

### 3.3 DNS、TLS 与隐蔽通信

| ID | 可疑事件 | 行为解释 | 方式 | 主判断日志及字段 | 关联日志及关联方式 | 详细起始判据 | 常见正常原因及误报排除条件 |
|---|---|---|---:|---|---|---|---|
| ZE-026 | 疑似 DNS 隧道 | 原本用来查询域名地址的 DNS 通信，呈现持续携带编码数据的特征，可能被用来传命令或传数据；需要多个特征共同支持。 | S | `dns.log`：`ts,uid,id.orig_h,id.resp_h,query,query_type,rcode_name,answers,TTLs,total_answers,total_replies` | `uid` 关联 `conn.log.orig_bytes,resp_bytes,duration`；用 Public Suffix List 从 `query` 提取注册主域 | 按 `(id.orig_h,注册主域)` 建 30 分钟窗并评分：查询 `>=30`；唯一子域 `>=20`；标签长度 p95 `>=40`；平均熵 `>=3.5`；唯一子域比例 `>=0.8`；TXT/罕见类型比例、周期性或流量异常各加分；总分达到阈值才命中，不能只凭长域名 | CDN、杀毒、遥测、DKIM、EDR 域名；按注册主域白名单而非完整随机子域白名单 |
| ZE-027 | DNS 内网枚举 | 设备批量查询内部主机名或服务位置，类似通过企业域名系统寻找域控、邮件服务器和数据库。 | Q | `dns.log`：`ts,id.orig_h,id.resp_h,query,query_type,rcode_name` | `query` 与企业内部域后缀/命名规则匹配；DHCP 映射源设备 | 按 `id.orig_h` 建 5 分钟窗：不同内部 `query >=30`，或 `query_type=SRV` 且涉及 `_ldap._tcp/_kerberos._tcp` 等服务查询 `>=10`；NXDOMAIN 比例高时增强 | 域加入、邮件客户端、AD 服务发现、资产发现 |
| ZE-028 | NXDOMAIN 异常聚集 | 设备连续查询许多不存在的域名并收到“不存在”的答复；可能是随机域名探测，也可能是软件配置错误。 | Q+B | `dns.log`：`ts,id.orig_h,query,query_type,rcode_name` | 从 `query` 提取注册主域和标签特征；源 IP 映射设备 | 按 `id.orig_h` 建 5 分钟窗：`rcode_name=NXDOMAIN` 数 `>=50`、NXDOMAIN/全部查询 `>=70%`、不同 query `>=30`，并超过实体同时间槽基线 `median+6*MAD` | 搜索域补全、拼写错误、断网、失效软件域名；进一步区分 DGA 与内部枚举 |
| ZE-029 | DGA/随机域名行为 | 设备查询大量看起来随机生成的域名，可能是程序自动生成名称来寻找控制服务器；随机外观本身不足以证明恶意。 | S | `dns.log`：`ts,id.orig_h,query,rcode_name,answers,TTLs` | 对 `query` 提取注册域和最长标签；外部域首次出现信息来自 30 天历史索引 | 按源 30 分钟计算不同注册域数、平均/最大标签长度、Shannon 熵、数字比例、NXDOMAIN 比例和新域比例；查询 `>=30` 且综合分达阈值。若异常集中在同一注册主域，优先归类 DNS 隧道 | 动态 DNS、CDN、追踪域名、安全产品随机域名 |
| ZE-030 | 周期性通信/Beacon | 设备以近似固定的间隔反复联系同一个目标，像定时签到或领取任务；正常监控和软件心跳也有这种表现。 | S | `conn.log`：`ts,id.orig_h,id.resp_h,id.resp_p,proto,duration,orig_bytes,resp_bytes,conn_state` | `ssl.log.server_name`、`dns.log.query/answers` 通过 `uid` 或目标 IP+时间关联，形成稳定目的实体 | 按 `(源设备,目的IP或SNI,端口)` 观察 1～24 小时，至少 20 次成功/有数据连接；计算相邻 `ts` 间隔，`CV=std/mean <=0.2`，且单次总字节 CV `<=0.3`；目的地罕见或外部时才生成 | 监控、NTP、消息客户端、更新检查和健康心跳 |
| ZE-031 | 罕见 TLS 指纹 | 设备建立加密连接时，握手特征组合在企业中很少见，可能来自新软件或不同通信工具；这种指纹不是程序身份的确定证明。 | B | `ssl.log`：`ts,uid,id.orig_h,id.resp_h,server_name,ja3,ja3s,version,cipher,established` | `uid` 关联 `conn.log`；指纹关联批准软件/恶意指纹库；字段存在性由生产 schema 确认 | 仅统计 `established=true`：`ja3` 在企业或同资产角色过去 30 天出现占比 `<0.1%` 且样本 `>=1000`，同时 SNI/目的 IP 对该主机也罕见；首次浏览器指纹单独不告警 | 浏览器、TLS 库和代理升级；JA3 可能来自扩展包而非默认日志 |
| ZE-032 | TLS 证书或握手异常 | 加密连接使用了不允许的版本或算法，证书验证有问题，或反复握手失败，可能是配置问题或异常通信。 | R+B | `ssl.log`：`ts,uid,id.orig_h,id.resp_h,version,cipher,server_name,established,subject,issuer,validation,last_alert` | `notice.log` 以 `uid` 关联 `note,msg`；证书 file UID 可关联 `files.log`（若采集） | 任一强条件：`validation` 明确失败、版本/算法在禁止集合、`notice.note` 命中证书告警；弱条件：外部 TLS 无 SNI、`established=false`、issuer/subject 罕见。只有弱条件时要求至少两项并偏离基线 | 内部 PKI、TLS inspection、自签设备证书、旧固件 |
| ZE-033 | SNI 与 DNS/目标不一致 | 建立加密连接时声明要访问的域名，与近期 DNS 查询及目标地址等证据不一致；可能涉及代理或共享服务器，需要核实。 | S | `ssl.log`：`ts,uid,id.orig_h,id.resp_h,server_name,subject,issuer` | 查同一源在 TLS 前 5 分钟的 `dns.log.answers`，要求 `dns.answers` 包含 `ssl.id.resp_h`；比较对应 `dns.query` 与 `server_name` | 找到近期 DNS 候选但 `server_name` 与所有候选 query 均不匹配，同时证书 subject/issuer 也不支持该 SNI，且该关系过去 30 天未见时命中低/中置信事件 | CDN、代理、共享 IP、DNS 缓存、ECH；关联不到 DNS 时不得判“不一致” |
| ZE-034 | 命中恶意目的地 | 设备查询或连接的 IP、域名命中了有效的高置信威胁情报；说明与已知风险目标有接触，不等于设备必然已失陷。 | R | `conn.log.id.resp_h`、`dns.log.query/answers`、`ssl.log.server_name`、`http.log.host` | 每个字段分别与 IP/域名情报匹配；以 `uid` 串联同一连接；情报必须带 `valid_from,valid_to,confidence,source` | 任一可观测值命中处于有效期内的高置信情报即生成；域名解析 IP 命中与直接 SNI/Host 命中分别记录证据，不因共享 IP 自动断言域名恶意 | sinkhole、共享托管、过期情报；保留情报版本和命中字段 |

### 3.4 Web、邮件、文件与数据库

| ID | 可疑事件 | 行为解释 | 方式 | 主判断日志及字段 | 关联日志及关联方式 | 详细起始判据 | 常见正常原因及误报排除条件 |
|---|---|---|---:|---|---|---|---|
| ZE-035 | HTTP 敏感路径/管理接口访问 | 来自未批准来源的设备访问网站的敏感页面或管理接口，例如管理后台、配置或导出接口；是否真正取得资源还要结合响应和业务含义。 | R+B | `http.log`：`ts,uid,id.orig_h,id.resp_h,method,host,uri,username,status_code` | `uid` 关联 `conn.log`；`host+uri+method` 与敏感接口策略表匹配；源 IP 关联授权网段 | `host/uri` 命中敏感资源，`method` 属于策略关注方法，响应成功（通常 `200～399`），且来源不在该接口授权集合；只有 401/403 表示尝试，不表示成功访问 | 健康检查、运维平台；仅明文 HTTP 可见，查询参数须脱敏 |
| ZE-036 | HTTP 上传量异常 | 设备通过 HTTP 向外部网站发送的数据量明显超过平常，可能是批量上传文件或数据；是否为泄露还取决于内容和授权。 | B | `http.log`：`ts,uid,id.orig_h,id.resp_h,method,host,uri,request_body_len,status_code` | `uid` 关联 `conn.log.orig_bytes,local_orig,local_resp`；Host 关联内部/外部域分类 | 按 `(id.orig_h,host)` 建 1 小时窗，仅统计外部目标且 `method in (POST,PUT,PATCH)`、成功响应：`SUM(request_body_len)>=200MB` 且超过主机/角色基线 `median+6*MAD`；若 body 长度缺失，以 `conn.orig_bytes` 作低置信估算 | API、备份、软件上传；必须确认发起方是内网客户端 |
| ZE-037 | HTTP 下载量异常 | 设备通过 HTTP 接收的数据量或文件数量明显增加，可能在批量下载数据，也可能是正常软件更新。 | B | `http.log`：`ts,uid,id.orig_h,host,uri,response_body_len,status_code,resp_fuids,resp_mime_types` | `resp_fuids` 关联 `files.log.fuid`，使用 `total_bytes,mime_type,missing_bytes,is_orig`；`uid` 关联 conn | 按 `(id.orig_h,host)` 建 1 小时窗：成功响应的 `SUM(response_body_len)>=500MB` 或文件数 `>=100`，并超过基线；敏感 MIME 或单文件异常大时加分 | 更新服务器、镜像仓库、软件分发 |
| ZE-038 | FTP 异常上传/下载 | 通过 FTP 传送文件的数量、大小、对象或目的地不符合策略或习惯，例如突然向外部服务器上传大量文件。 | R+B | `ftp.log`：`ts,uid,id.orig_h,id.resp_h,user,command,arg,reply_code,reply_msg,fuid` | `ftp.fuid=files.fuid` 关联 `files.log.total_bytes,mime_type,is_orig,missing_bytes`；`uid` 关联控制连接 | 上传按成功 `STOR`、下载按成功 `RETR` 判断，成功以 `reply_code=226` 或明确完成响应确认；敏感文件名/类型、外部目标直接加分；按用户/主机 1 小时累计文件数和字节量与基线比较 | 合法文件交换、批处理；方向优先按 STOR/RETR，不单靠 is_orig |
| ZE-039 | 敏感附件发送到外部 | 邮件附件发给了企业外部地址，同时命中敏感标签或其他风险线索；附件格式、大小或主题只能提示风险，不能单独确认内容敏感。 | R+Q | `smtp.log`：`ts,uid,mailfrom,rcptto,subject,msg_id,tls,fuids,last_reply_code` | `fuids` 关联 `files.log.fuid,mime_type,total_bytes`；收件域与企业域名表匹配 | 邮件从内部地址发出，至少一个 `rcptto` 为外部域，发送成功，且附件 MIME/大小/提取标签命中敏感策略；仅有主题关键词时只能作为弱信号 | 合法外发、邮件网关；加密 SMTP 可能无法获得完整附件证据 |
| ZE-040 | 异常群发/钓鱼迹象 | 同一发件人在短时间向大量外部收件人发送相似邮件，可能是异常群发；是否属于钓鱼还需要邮件内容等证据。 | Q | `smtp.log`：`ts,mailfrom,rcptto,subject,msg_id,last_reply_code,last_reply_msg` | 按 `msg_id` 去重；解析 `rcptto` 数组和外部域；`uid` 关联源连接 | 按 `mailfrom` 建 10 分钟窗：不同外部收件人 `>=50` 或消息数 `>=20`，成功发送比例高，且规范化主题重复度 `>=80%` | 营销、告警通知、账单系统；按批准发件账号及时间范围排除 |
| ZE-041 | SMB 文件读取量异常 | 设备通过 Windows 文件共享访问或读取的文件数量、范围或数据量突然增加；只看到打开文件时，不能认定已经完整读取。 | B+Q | `smb_files.log`：`ts,uid,id.orig_h,id.resp_h,action,path,name,size` | `uid` 关联 `smb_mapping.path/share_type`、`ntlm.username/success`、`conn.resp_bytes`；文件身份可用 `(服务器,path,name)` | 按 `(源设备,规范化账号,文件服务器)` 建 30 分钟窗：`FILE_READ` 数或不同文件数、目录数、`SUM(size)`/`conn.resp_bytes` 超过基线且达到绝对下限（如文件 `>=100` 或响应 `>=500MB`）。只有 FILE_OPEN 时仅算访问，不算完整读取 | 备份、索引、杀毒；size 是文件大小，不一定等于实际读取字节 |
| ZE-042 | SMB 大量删除/重命名 | 设备通过文件共享短时间删除或改名大量文件，可能是集中整理，也可能是破坏行为；单凭改名不能确认加密或勒索。 | Q | `smb_files.log`：`ts,uid,id.orig_h,id.resp_h,action,path,name,size` | `uid` 关联 `ntlm.username` 和 `smb_mapping.path`；按源设备和文件服务器聚合 | 5 分钟内 `action in (FILE_DELETE,FILE_RENAME)` 数 `>=100`，或超过该实体基线 `median+6*MAD` 且绝对数 `>=20`；涉及多共享/多服务器时加分 | 文件整理、部署、生命周期清理；不能仅凭该事件确认文件被加密 |
| ZE-043 | PostgreSQL 查询/返回量异常 | 客户端短时间查询 PostgreSQL 数据库的范围、返回记录数或导出量明显增加，可能是在集中提取数据，也可能是报表或备份。 | R+B | `postgresql.log`：`ts,uid,id.orig_h,id.resp_h,msg_type,statement,param_values,result_rows,result_sent,command_complete_tag` | `uid` 关联 `conn.log`；SQL 解析器从 `statement` 提取操作、库表和 COPY 方向 | 单条成功 `COPY ... TO` 或查询敏感表触发规则；另按 `(id.orig_h,id.resp_h)` 30 分钟聚合 `SUM(result_rows)`、不同表数、查询数，超过基线并达到绝对下限时触发 | 报表、ETL、备份；仅对未加密且解析完整的 PostgreSQL 流量有效 |
| ZE-044 | PostgreSQL 破坏性操作 | 客户端对 PostgreSQL 执行删表、清空表、改结构或批量删改数据等高影响操作，需要核实是否在批准的变更范围内。 | R+Q | `postgresql.log`：`ts,uid,id.orig_h,id.resp_h,statement,result_rows,command_complete_tag` | SQL 解析器提取 `DROP/TRUNCATE/ALTER/DELETE/UPDATE` 及目标对象；维护窗口和 DBA 源清单关联 | 成功 `DROP/TRUNCATE/ALTER` 且不在批准变更范围直接命中；或 10 分钟大量 `DELETE/UPDATE`，以 `command_complete_tag/result_rows` 判断影响量 | DBA 变更、迁移任务；不要用简单字符串包含代替 SQL 解析 |
| ZE-045 | Redis 枚举/导出/高危配置 | 客户端对 Redis 执行批量查找数据项、生成持久化文件、修改复制关系或清空数据等操作，可能影响数据或配置；具体风险由命令和参数决定。 | R | `redis.log`：`ts,uid,id.orig_h,id.resp_h,request_command,request_arg,response_status,response_size,is_error` | `uid` 关联 `conn.log`；命令和参数转大写、去空白后匹配策略 | `is_error=false` 且命令属于 `KEYS,CONFIG,SLAVEOF,REPLICAOF,SAVE,BGSAVE,MODULE,FLUSHDB,FLUSHALL`；结合参数区分只读查询和配置修改，高危命令来自非批准源直接命中 | 运维备份、集群管理；保留规范化命令及原始证据 |

### 3.5 IoT、工控与平台质量

| ID | 可疑事件 | 行为解释 | 方式 | 主判断日志及字段 | 关联日志及关联方式 | 详细起始判据 | 常见正常原因及误报排除条件 |
|---|---|---|---:|---|---|---|---|
| ZE-046 | MQTT 未授权设备/客户端 | 设备连接 MQTT 消息服务器时使用未登记或与设备不符的客户端标识，或多个来源异常复用同一标识，可能是误配置或冒用。 | R+B | `mqtt_connect.log`：`ts,uid,id.orig_h,id.resp_h,client_id,proto_version,connect_status,session_present` | `client_id` 与设备注册表匹配；`uid` 关联 conn；源 IP 通过 DHCP 映射设备 | `connect_status=Connection Accepted` 且 client_id 未登记或登记设备与源不符；或同一 client_id 在 5 分钟内从多个不相关源 IP 并发。拒绝连接 `>=10/5分钟` 另作为尝试事件 | 允许空 client_id 的客户端、NAT、客户端漂移；注册表需时间有效 |
| ZE-047 | MQTT 通配符/敏感主题订阅 | 设备订阅了 MQTT 中无权查看的消息主题，或用通配符扩大接收范围，类似要求收听一整组消息频道；订阅请求是否被接受需看响应。 | R | `mqtt_subscribe.log`：`ts,uid,id.orig_h,id.resp_h,action,topics,qos,granted_qos` | `uid` 关联 `mqtt_connect.client_id`；topic 与“客户端—允许主题”ACL 匹配 | `action=SUBSCRIBE`，topic 含 `#`/`+` 或命中敏感主题，且 client_id 不在对应授权集合；`granted_qos` 存在则说明 broker 已接受，严重度提高 | 运维监控、桥接 broker、合法全局订阅者 |
| ZE-048 | MQTT 敏感或异常发布 | 设备向 MQTT 的未授权敏感主题发送消息，或发送内容、数量、大小明显异常；可能涉及数据暴露或异常控制消息。 | R+B | `mqtt_publish.log`：`ts,uid,id.orig_h,id.resp_h,from_client,retain,qos,status,topic,payload,payload_len` | `uid` 关联 `mqtt_connect.client_id`；topic/payload 与敏感策略和客户端 ACL 匹配 | 客户端向未授权敏感 topic 发布且 `status=ok`；或按 `(client_id,topic)` 10 分钟聚合 payload 总量/最大值超过基线；首次使用 `retain=true` 作为增强 | 合法配置发布、固件更新；payload 必须脱敏、截断、限权 |
| ZE-049 | 非授权 Modbus 写操作 | 设备向工业控制设备发出修改参数或状态的请求，但来源、时间或操作范围不符合授权；仅看到写请求不能确认设备状态已改变。 | R | `modbus.log`：`ts,uid,id.orig_h,id.resp_h,function_code,request_unit_id,request_reference_num,request_word_count,request_data` | 源/目标关联“工程站—PLC—unit—寄存器范围—维护窗”允许矩阵；`uid` 关联 conn | `function_code` 属于 Write 类，且源不被授权、目标/unit/寄存器超出允许范围或不在维护窗，任一成立即命中；若无响应状态，只表述为“观察到写请求” | 授权工程变更；网络请求不保证 PLC 已成功执行 |
| ZE-050 | Modbus 扫描/批量读取 | 设备集中探测工业设备编号，或批量读取存储状态和参数的位置，可能在摸清生产设备信息；正常人机界面也会定期读取这些数据。 | Q+B | `modbus.log`：`ts,id.orig_h,id.resp_h,function_code,request_unit_id,request_reference_num,request_word_count,response_word_count` | 资产角色和正常轮询模板关联；`uid` 关联 conn | 按 `(id.orig_h,id.resp_h)` 建 5 分钟窗：不同 unit `>=5`、寄存器跨度异常、读取请求 `>=100` 或 word_count 总量超过基线；访问模式不符合该 HMI/历史库固定轮询模板时命中 | HMI、历史数据库、工程调试；工控轮询基线必须按设备对建立 |
| ZE-051 | 关键 Zeek 日志中断 | 原本应持续产生的 Zeek 日志长时间不再到达，系统可能暂时看不到对应网络行为，也可能确实没有业务流量。 | Q | 每类 Zeek 日志的 `ts`、采集时间和传感器 ID（采集平台字段） | 同传感器的 `conn.log` 活跃度作为网络仍有流量的旁证；`reporter.log` 检查进程错误 | 对 `(传感器,日志类型)` 计算最后事件时间：当前间隔 `>max(3×过去7天同时间槽p95间隔,5分钟)`，同时该传感器仍有其他日志/心跳，判定单日志中断；所有日志同时停止则判采集器中断 | 真正无业务、维护停机；必须区分事件时间和入库时间 |
| ZE-052 | 抓包丢失/解析质量下降 | Zeek 观察的网络数据出现较多缺失，或协议解析报错，导致日志不完整、检测可靠性下降；这是采集质量问题。 | R+B | `conn.log.missed_bytes,orig_ip_bytes,resp_ip_bytes`；`files.log.missing_bytes,total_bytes`; `packet_filter.log` 抓包统计；`reporter.log.level/message`; `analyzer.log.kind/name` | 按传感器和 5 分钟窗聚合；使用同一采集节点标识关联，不能用网络 `uid` 关联运行日志 | 计算 `missed_ratio=SUM(missed_bytes)/SUM(orig_ip_bytes+resp_ip_bytes)`、文件缺失率和抓包 drop；任一超过生产阈值（起始可用 1%）或 reporter ERROR、关键 analyzer deactivate 即命中。该事件降低其他检测置信度 | 突发流量、传感器维护、镜像口拥塞；它是质量告警，不是攻击证据 |

## 4. 基于 Zeek 证据判定的异常场景

异常场景以实际行为及其证据是否充分为成立依据，不要求必须由多种可疑事件组合。一个可疑事件可能已经包含多条日志的聚合结果，也可能由一条明确违反授权策略的协议操作触发。

- **单事件成立**：某个事件自身已满足行为判据、授权范围校验和误报排除条件，即可形成相应场景。例如，已确认非批准来源发起 Modbus 写请求，可以判为“非授权工控写请求”，无须等待扫描事件。
- **关联后成立**：场景名称包含先后关系或跨阶段行为时，必须有对应证据。例如，“数据收集后外传”需要收集证据和后续外向传输证据，并证明两者属于同一设备或账号。
- **保留为线索**：首次目的地、罕见指纹、长连接、单纯大流量等弱信号，如果不足以说明具体异常行为，应保留为调查线索，不强行升级场景。

场景成立不等于确认攻击成功。场景名称应区分“请求/尝试”“观察到的操作”“疑似攻击”和“已确认结果”。增强证据只能提高已有结论的置信度，不能补替缺失的必要条件。以下为现有典型场景的判定表，不代表完整覆盖清单。

| 场景 ID | 异常场景 | 必要事件 | 增强证据 | 关联键 | 窗口 |
|---|---|---|---|---|---|
| ZS-001 | 密码喷洒并成功 | `ZE-003` + `ZE-005` | 新目标、非工作时间 | 账号、来源 IP、认证服务 | 30 分钟 |
| ZS-002 | 内网侦察 | `ZE-017` 或 `ZE-018` | `ZE-027`、`ZE-022`、`ZE-019` | 源主机 | 30 分钟 |
| ZS-003 | 异常远程管理连接扩散 | `ZE-020`，已排除批准的批量管理任务；单事件足以说明连接扩散，不能直接确认横向执行 | `ZE-021`、NTLM 成功、`ZE-024` | 源主机、账号、目标集合 | 2 小时 |
| ZS-004 | 异常域枚举或目录复制请求 | `ZE-022` 的明确枚举操作或 `ZE-023` 的非授权复制请求；按实际分支输出具体名称，不能将普通域枚举解释为凭据复制 | 特权账号异常认证、SMB PIPE 扩散 | 源主机、域控 | 1 小时 |
| ZS-005 | 疑似 DNS 隐蔽通道 | `ZE-026` 已满足多特征评分、最低样本量及正常业务排除条件；可由该单事件成立 | `ZE-030`、`ZE-028`、稀有目的地 | 源主机、注册主域 | 30 分钟 |
| ZS-006 | 疑似周期性控制通信 | `ZE-030` 加目的地风险或资产角色不符的独立证据；仅周期性不足以成立，需排除正常心跳 | `ZE-034`、`ZE-031`、`ZE-032`；同一连接产生的重复特征不能重复加分 | 源主机、目的地 | 1 小时 |
| ZS-007 | SMB 异常批量访问/读取 | `ZE-041` 已超过有效基线且排除备份等任务；只有 FILE_OPEN 时输出“批量访问”，有读取证据时输出“批量读取” | 敏感共享、首次路径、异常时间 | 主机、账号、文件服务器 | 2 小时 |
| ZS-008 | 疑似数据收集后网络外传 | （`ZE-041` 的读取证据或 `ZE-043` 的返回/导出证据）之后出现外向传输；必须确认同一设备/账号和先后顺序。同 IP 时间接近不足以证明文件相同 | 新目的地、FTP/HTTP/SMTP 明确上传；文件内容或哈希一致可进一步增强 | 主机、账号、目的地 | 4 小时 |
| ZS-009 | SMB 异常批量删除/重命名 | `ZE-042` 满足数量及授权校验并排除清理任务即可成立；仅凭删除/重命名不能确认勒索或加密 | `ZE-020`、多个共享、账号认证扩散 | 源主机、账号、目标服务器 | 30 分钟 |
| ZS-010 | 非授权工控写请求 | `ZE-049` 中写功能码及授权矩阵违规均明确即可单事件成立；缺少执行结果时保留“请求”结论 | `ZE-050`、首次工程站→PLC 边 | 源设备、PLC、unit/register | 15 分钟 |

## 5. 判断示例

### 示例 A：密码喷洒后登录成功

假设 `ntlm.log` 中 10 分钟内出现：

```json
{"ts":1788513400.1,"id.orig_h":"10.0.0.40","id.resp_h":"10.0.0.50","username":"alice","success":false}
{"ts":1788513402.2,"id.orig_h":"10.0.0.40","id.resp_h":"10.0.0.50","username":"bob","success":false}
{"ts":1788513404.3,"id.orig_h":"10.0.0.40","id.resp_h":"10.0.0.50","username":"carol","success":false}
...
{"ts":1788513477.6,"id.orig_h":"10.0.0.40","id.resp_h":"10.0.0.50","username":"Administrator","success":true}
```

判断过程：

1. 按 `id.orig_h=10.0.0.40` 聚合，失败用户名去重数达到阈值，形成 `ZE-003 密码喷洒`。
2. 在同一来源、目标和 15 分钟窗口内发现 `Administrator success=true`，形成 `ZE-005 失败后成功`。
3. 两事件共享来源和目标且时间连续，组合为 `ZS-001`；若该来源不是授权管理主机，严重度提高。
4. 该证据能说明“网络侧观察到 NTLM 多账号失败后有成功认证”，不能单独证明成功账号已执行恶意操作。

### 示例 B：DNS 隧道评分

某主机每 5 秒查询：

```json
{"id.orig_h":"10.0.0.23","query":"MFRGGZDFMZTWQ2LK.attacker.example","query_type":"TXT","rcode_name":"NOERROR"}
{"id.orig_h":"10.0.0.23","query":"ONSWG4TFOQXG4ZLS.attacker.example","query_type":"TXT","rcode_name":"NOERROR"}
```

对 `10.0.0.23 + attacker.example` 的 30 分钟窗口计算：子域中位长度、Shannon 熵、唯一子域比例、TXT 比例、查询间隔变异系数、查询/响应字节量。示例评分为 `长标签 2 + 高熵 2 + TXT 2 + 周期性 2 + 稀有主域 1 = 9`，阈值 7，形成 `ZE-026`。若只有“长域名”一个特征则不告警。

### 示例 C：SMB 批量读取后 HTTP 外传

1. `smb_files.log`：主机 `10.0.0.40` 30 分钟对 `\\fileserver\\finance` 出现 800 个 `FILE_OPEN/FILE_READ`，累计文件大小和 `conn.resp_bytes` 均超过其 30 天基线，形成 `ZE-041`。
2. 随后 `http.log`：同主机向外部 `upload.example` 发送 `POST /upload`，`request_body_len=524288000`，形成 `ZE-036`。
3. 以源主机和 4 小时时间窗关联为 `ZS-008 数据收集后网络外传`。
4. 若 HTTP 为 TLS，只能从 `conn.orig_bytes`、SNI 和时间关系得到较弱外传证据，不能确认上传内容就是前述文件。

## 6. 落地查询示意

以下为逻辑 SQL，字段名需按实际 ELK/ClickHouse 映射调整。

```sql
-- 内网端口扫描
SELECT id_orig_h, id_resp_h,
       uniqExact(id_resp_p) AS dst_ports,
       countIf(conn_state IN ('REJ','S0','RSTO','RSTR')) AS failed
FROM zeek_conn
WHERE ts >= now() - INTERVAL 5 MINUTE
  AND local_orig = true AND local_resp = true
GROUP BY id_orig_h, id_resp_h
HAVING dst_ports >= 20 AND failed >= 15;
```

```sql
-- NTLM 密码喷洒
SELECT id_orig_h,
       uniqExactIf(username, success = false) AS failed_users,
       countIf(success = false) AS failures
FROM zeek_ntlm
WHERE ts >= now() - INTERVAL 10 MINUTE
GROUP BY id_orig_h
HAVING failed_users >= 10 AND failures >= 10;
```

## 7. 编号规则与能力边界

- 可疑事件统一使用 `ZE-001～ZE-052`，按照本文出现顺序连续编号，不继承或映射其他文档编号。
- 异常场景统一使用 `ZS-001～ZS-010`，场景只引用本文的 `ZE-*` 事件。
- `ZE-001～ZE-013`：认证与目录服务；`ZE-014～ZE-025`：网络侦察与横向活动；`ZE-026～ZE-034`：DNS、TLS 与隐蔽通信；`ZE-035～ZE-045`：Web、邮件、文件与数据库；`ZE-046～ZE-052`：IoT、工控与平台质量。
- Zeek 单独无法可靠判断停用账号、MFA 绕过、账号生命周期、GPO/sudoers、进程树、计划任务、终端防护关闭和云 IAM 等行为，应由其他数据源形成独立检测。

## 8. 参考资料

- [本项目 Zeek 字段实测文档](./Zeek日志字段详解_2026-09-04.md)
- [Zeek 8.2 Common Logs](https://docs.zeek.org/en/current/reference/logs/index.html)
- [Zeek conn.log 使用说明](https://docs.zeek.org/en/current/reference/logs/conn.html)
- [Zeek SMB、DCE-RPC、Kerberos、NTLM 日志说明](https://docs.zeek.org/en/current/reference/logs/smb.html)
- [Zeek ssl.log 使用说明](https://docs.zeek.org/en/current/reference/logs/ssl.html)

> 注意：本项目文档列出的 `ja3/ja3s`、部分扩展协议字段可能来自额外脚本、包或本地定制。部署检测前应以生产 Zeek schema 为准做字段存在性检查。

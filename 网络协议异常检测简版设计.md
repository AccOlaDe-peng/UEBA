# 网络协议异常检测简版设计

## 1. 建设目标

本系统通过 Zeek 等网络探针采集协议元数据，发现异常协议、DNS 隧道、横向扫描、异常外联和机器化周期通信。

系统只分析连接和协议特征，不进行以下处理：

- 不解密 TLS 流量；
- 不解析 HTTP Body 或文件正文；
- 不判断传输内容是否属于敏感数据；
- 不分析终端上的文件、进程、USB 等主机行为。

因此，系统输出的是“疑似异常网络通信”或“疑似异常外传行为”，不是对数据泄露事实的直接确认。

## 2. 总体流程

```text
交换机镜像流量 / TAP
  ↓
Zeek 协议解析
  ├── conn.log：连接、端口、协议、字节数、连接状态
  ├── dns.log：查询域名、查询类型、响应码
  ├── tls.log：SNI、TLS 版本、证书、JA3/JA4
  └── weird.log：协议解析异常
  ↓
Filebeat 日志采集
  ↓
Logstash 清洗和标准化
  ├── 字段映射、类型转换
  ├── 统一事件时间
  ├── 生成事件 ID 和去重键
  └── 写入 Kafka / Elasticsearch
  ↓
事件关联和上下文富化
  ├── 按 Zeek UID 关联 conn、dns、tls
  ├── IP 映射资产
  ├── 判断内网、外网和资产角色
  ├── 查询端口协议策略
  └── 查询批准目的地和例外策略
  ↓
检测计算
  ├── 原子信号：固定规则、窗口统计、目的地新颖度
  ├── 基线信号：主机自身和同类主机偏离
  ├── 场景编排：信号组合和时序匹配
  └── 模型信号：孤立森林发现的多维罕见组合
  ↓
信号与场景复核
  ├── 白名单和例外检查
  ├── 告警去重和抑制
  ├── 数据质量与关联置信度检查
  └── 案件优先级计算
  ↓
原子信号 / 场景告警
  ↓
按资产、目的地和时间合并为风险案件
  ↓
Elasticsearch 存储 + Kibana 调查展示
  ↓
人工反馈、规则调优、基线和模型更新
```

各组件职责：

| 组件 | 职责 |
| --- | --- |
| Zeek | 从镜像报文中提取连接和协议元数据 |
| Filebeat | 可靠读取 Zeek 日志并转发 |
| Logstash | 清洗单条日志、统一字段和数据类型 |
| Kafka | 缓冲事件、解耦采集和计算、支持重放 |
| Flink / 流计算引擎 | 去重、日志关联、滑动窗口、规则和模型评分 |
| Elasticsearch | 保存原始事件、特征、模型分数、告警和案件 |
| Kibana | 展示趋势、告警、案件时间线和原始证据 |

小规模验证阶段可以先不部署 Kafka/Flink，通过 Elasticsearch Transform 和定时任务生成固定时间桶特征。生产环境如需重叠滑动窗口、乱序处理和实时模型评分，建议使用 Kafka/Flink。

## 3. Zeek 日志与关联

### 3.1 主要日志

| 日志 | 关键内容 | 主要用途 |
| --- | --- | --- |
| `conn.log` | 五元组、服务识别、时长、字节数、包数、连接状态 | 所有连接类检测 |
| `dns.log` | 查询名、查询类型、响应码 | DNS 隧道 |
| `ssl.log` / `tls.log` | SNI、版本、证书、JA3/JA4、ALPN | TLS 异常和目的地识别 |
| `weird.log` | 协议格式和解析异常 | 异常协议补充信号 |
| `notice.log` | Zeek 内置通知 | 已有检测信号补充 |

### 3.2 日志关联

Zeek 为一次连接生成一个 `uid`。同一连接的不同协议日志通过 `uid` 关联：

```text
conn.log：连接端口、实际服务、流量大小
      ↕ 相同 uid
dns.log：查询域名
tls.log：SNI、TLS 版本、JA3/JA4
```

跨 Zeek、Suricata、NetFlow 等数据源关联时，可补充使用 `community_id`。

## 4. 最小统一事件对象

清洗后的一条增强连接事件如下：

```json
{
  "@timestamp": "2026-09-02T10:03:12.123Z",
  "event_id": "zeek-gw-01-C8abc",
  "sensor_id": "zeek-gw-01",
  "zeek_uid": "C8abc",
  "source": {
    "ip": "10.10.2.15",
    "port": 52341,
    "bytes": 85231,
    "packets": 85
  },
  "destination": {
    "ip": "203.0.113.8",
    "port": 443,
    "bytes": 6102,
    "packets": 29
  },
  "network": {
    "transport": "tcp",
    "protocol": "tls",
    "direction": "outbound",
    "duration_seconds": 2.6,
    "connection_state": "SF"
  },
  "tls": {
    "server_name": "example.com",
    "version": "1.3",
    "ja4": "t13d..."
  },
  "asset": {
    "id": "PC-023",
    "role": "office_endpoint",
    "zone": "office"
  },
  "destination_context": {
    "key": "domain:example.com",
    "is_internal": false,
    "is_approved": false,
    "is_first_seen_by_asset": true,
    "is_first_seen_by_enterprise": true,
    "asn": 12345,
    "country": "US"
  }
}
```

字段说明：

| 字段 | 含义 |
| --- | --- |
| `@timestamp` | 网络事件真实发生时间，用于窗口计算 |
| `event_id` | 标准化事件唯一 ID，用于去重和证据回查 |
| `sensor_id` | 采集该流量的 Zeek 传感器 ID |
| `zeek_uid` | Zeek 连接 ID，用于关联 `conn`、`dns`、`tls` 等日志 |
| `source.ip` | 发起连接的源 IP |
| `source.port` | 发起连接的源端口 |
| `source.bytes` | 源端向目的端发送的字节数；出站流量中通常代表上行量 |
| `source.packets` | 源端向目的端发送的报文数 |
| `destination.ip` | 连接目标 IP |
| `destination.port` | 连接目标端口 |
| `destination.bytes` | 目的端向源端发送的字节数；出站流量中通常代表下行量 |
| `destination.packets` | 目的端向源端发送的报文数 |
| `network.transport` | 传输层协议，如 TCP、UDP |
| `network.protocol` | Zeek 实际识别出的应用协议，如 DNS、TLS、HTTP |
| `network.direction` | 通信方向，如出站、入站、内部 |
| `network.duration_seconds` | 连接持续时间，单位秒 |
| `network.connection_state` | Zeek 连接状态，用于成功率和扫描检测 |
| `tls.server_name` | TLS ClientHello 中的 SNI 域名 |
| `tls.version` | TLS 协议版本 |
| `tls.ja4` | TLS 客户端握手指纹；也可使用 JA3 |
| `asset.id` | 源主机的稳定资产 ID，不建议只使用动态 IP |
| `asset.role` | 资产角色，如办公终端、服务器、跳板机 |
| `asset.zone` | 资产所在网络或安全区域 |
| `destination_context.key` | 标准化目的地键，优先使用域名，其次 IP+ASN，最后使用 IP |
| `destination_context.is_internal` | 目标是否位于企业内部网段 |
| `destination_context.is_approved` | 目标是否属于批准服务或合作方 |
| `destination_context.is_first_seen_by_asset` | 当前主机是否首次访问该目的地 |
| `destination_context.is_first_seen_by_enterprise` | 企业是否首次观察到该目的地 |
| `destination_context.asn` | 目标 IP 所属自治系统编号 |
| `destination_context.country` | 目标 IP 所属国家或地区代码 |

## 5. 数据清洗与富化

Logstash 主要完成：

```text
Zeek ts          → @timestamp
id.orig_h        → source.ip
id.orig_p        → source.port
id.resp_h        → destination.ip
id.resp_p        → destination.port
orig_bytes       → source.bytes
resp_bytes       → destination.bytes
proto            → network.transport
service          → network.protocol
conn_state       → network.connection_state
uid              → zeek_uid
```

清洗完成后需要查询以下参考数据：

| 参考数据 | 作用 |
| --- | --- |
| 资产/CMDB | 将 IP 映射为资产 ID、角色和区域 |
| DHCP/VPN 历史映射 | 根据事件时间解决动态 IP 归属 |
| 内部网段表 | 判断出站、入站和内部通信 |
| 端口协议表 | 判断实际协议是否符合受管端口要求 |
| 批准目的地表 | 识别合法 SaaS、合作方和基础设施 |
| 规则例外表 | 按资产、网段、目标、协议、端口和有效期进行豁免或降级 |
| Public Suffix List | 正确提取 DNS 注册主域和子域 |
| GeoIP/ASN | 提供目标所属国家、ASN 和组织信息 |

## 6. 时间窗口计算

### 6.1 事件时间

检测窗口必须使用 Zeek 事件发生时间，而不是日志接收时间：

```text
event_time：流量真实发生时间
ingest_time：ELK 接收到日志的时间
process_time：计算引擎处理事件的时间
```

建议允许日志迟到 2 分钟：

```text
watermark = 当前已观察到的最大 event_time - 2分钟
```

严重迟到的事件仍保存到 Elasticsearch，并标记为迟到数据，用于后续回算。

### 6.2 窗口建议

| 场景 | 分组键 | 窗口长度 | 滑动步长 |
| --- | --- | --- | --- |
| DNS 隧道 | 资产 + 注册主域 | 5 分钟 | 1 分钟 |
| 横向扫描 | 源资产 | 5 分钟 | 1 分钟 |
| 异常外联 | 资产 + 目的地 | 1 小时 | 5 分钟 |
| 周期通信 | 资产 + 目的地 + 协议 | 1 小时 | 5 分钟 |
| 资产模型画像 | 资产 | 1 小时 | 1 小时或 5 分钟 |

滑动窗口的意义是每隔一个步长重新计算最近一个完整窗口。例如每分钟计算一次“过去 5 分钟”，可以避免异常行为跨越固定时间桶边界后被拆散。

## 7. 主要检测场景与判定方法

### 7.1 识别日志异常网络信息的总体方法

网络日志中的单个异常特征通常不足以证明恶意。例如 TLS 使用非典型端口、长 DNS 子域、深夜访问新域名，都可能由正常业务产生。因此检测应先从原始日志提取**原子信号**，再按明确的场景条件组合或排序；不能把所有特征简单加分，达到某个统一阈值就认定攻击。

```text
原始网络日志
  → 固定规则 / 窗口统计 / 基线 / 模型产生原子信号
  → 例外、资产角色、数据完整性复核
  → 同一资产、目的地或会话内的组合与序列匹配
  → 形成场景告警
  → 对已成立场景计算案件优先级
```

| 判定方法 | 何时使用 | 产出与处置原则 |
| --- | --- | --- |
| 固定规则 | 行为明确违反策略，或协议语义足以说明风险 | 产生高置信原子告警；不因模型低分取消 |
| 基线 + 异常 | 判断某主机、目的地或同类资产的当前行为是否罕见 | 产生基线异常信号；通常需叠加其他证据才升级 |
| 异常组合 | 多个弱异常在同一对象、窗口内共同出现才构成风险 | 明确必要条件、增强条件、排除条件和最小命中数 |
| 行为序列 | 行为的先后顺序本身有意义 | 限定每一步最大间隔和整体窗口，命中后形成高置信场景 |
| 统计/模型 | 多维特征联合罕见、难以用少数阈值准确描述 | 只产生模型异常信号或作为场景增强条件，不直接等同攻击 |
| 风险评分 | 多个已成立场景需要排队处置 | 仅决定案件优先级；不得代替场景判定 |

每条原子信号至少记录 `signal_type`、`detection_method`、资产/目的地、时间窗口、命中证据、原始 Zeek 日志引用、例外结果和数据完整率。组合时必须使用关联键（如 `asset.id + destination_context.key`），避免把不同主机、不同目的地的低风险信号错误相加。

场景应以如下结构配置并版本化：

```text
场景：疑似 DNS 隧道
聚合键：asset.id + 注册主域
窗口：5 分钟
必要条件：DNS 查询量达到最小样本数
异常条件：高熵/长子域、唯一子域比例高、TXT 比例异常、NXDOMAIN 异常、查询节奏规律中至少命中 2 项
增强条件：目标未批准、仅一台主机访问、同一资产出现异常外联
排除条件：批准的安全软件、CDN、DNS 防护或有效变更例外
质量要求：可取得注册主域，DNS 字段完整率达到阈值
```

### 7.2 协议与端口不匹配（固定规则）

建立端口协议表：

```text
53       → DNS
80       → HTTP
443      → TLS / HTTP
25/465/587 → SMTP / TLS
22       → SSH
3389     → RDP
445      → SMB
```

判断逻辑：

```text
实际协议已被 Zeek 明确识别
AND 目标端口在受管端口表中
AND 实际协议不在允许协议集合
AND 未命中有效例外
→ 生成协议端口不匹配事件
```

Zeek 未识别出协议时不能直接判为不匹配。TLS 运行在 8443、9443 等非典型端口时先记为低风险信号，再结合目的地新颖度、上行量和周期性决定是否升级。

### 7.3 DNS 隧道（窗口特征 + 异常组合）

按“资产 + 注册主域 + 5 分钟窗口”计算：

```text
query_count
unique_subdomain_ratio
avg_subdomain_length
avg_subdomain_entropy
txt_ratio
nxdomain_ratio
interval_cv
```

| 特征 | 含义 |
| --- | --- |
| `query_count` | DNS 查询总数 |
| `unique_subdomain_ratio` | 不同子域数除以查询总数 |
| `avg_subdomain_length` | 子域平均字符长度 |
| `avg_subdomain_entropy` | 子域字符 Shannon 熵平均值 |
| `txt_ratio` | TXT 查询占全部查询的比例 |
| `nxdomain_ratio` | NXDOMAIN 响应占全部响应的比例 |
| `interval_cv` | 查询间隔变异系数；越低说明查询节奏越固定 |

先分别生成可解释的原子信号；下面的权重可作为“DNS 隧道场景内的证据强度”计算，不可与其他无关场景直接相加：

```text
唯一子域比例高       +25
子域平均长度高       +20
字符熵高             +25
TXT 查询比例异常     +20
NXDOMAIN 比例异常    +10
查询间隔高度规律     +15
企业内仅一台主机访问 +15
```

建议的场景判定为：`query_count` 达到最小样本数，且上述异常条件至少命中 2 项；若命中目标未批准、企业内仅一台主机访问或同一资产异常外联任一增强条件，则升级。长域名本身不能直接证明 DNS 隧道，CDN、安全软件和云服务也可能产生高熵子域。

### 7.4 横向扫描（窗口规则 + 资产角色例外）

按“源资产 + 5 分钟窗口”计算：

```text
unique_dst_ip_count
unique_dst_port_count
connection_count
failed_connection_count
failed_ratio
```

| 特征 | 含义 |
| --- | --- |
| `unique_dst_ip_count` | 不同内部目标 IP 数 |
| `unique_dst_port_count` | 不同目标端口数 |
| `connection_count` | 参与统计的连接总数 |
| `failed_connection_count` | 失败连接数量 |
| `failed_ratio` | 失败连接占可判定连接的比例 |

规则示例：

```text
主机扫描：目标主机数 >= 30 AND 失败比例 >= 70%
端口扫描：目标端口数 >= 20 AND 目标主机数 <= 3 AND 失败比例 >= 70%
```

漏洞扫描器、跳板机和安全设备需要通过资产角色或有效例外降级，但相关连接仍应留存。

### 7.5 异常外联（基线 + 一个或多个异常）

按“资产 + 目的地 + 1 小时窗口”计算：

```text
outbound_bytes
inbound_bytes
upload_ratio
connection_count
is_first_seen_by_asset
is_first_seen_by_enterprise
is_approved
is_workhour
```

规则示例：

```text
办公终端
AND 外部目的地
AND 未批准目的地
AND 企业首次出现
AND 非工作时间
AND 上行量 > 100MB
```

上例仅满足“高流量 + 新颖目的地 + 非工作时间”时，应先作为中风险组合场景。以下任一独立异常可作为增强条件：直连 IP、无 SNI、罕见 TLS 指纹、周期通信、上行量同时超过主机自身和同类主机 P99、目的地在企业内使用范围极小。满足两个增强条件，或后续出现 DNS 隧道信号时，升级为高风险案件。

### 7.6 周期性未知外联（固定周期特征 + 目的地上下文）

按连接发生时间排序：

```text
t1, t2, t3, ..., tn
Δt = [t2-t1, t3-t2, ..., tn-t(n-1)]
```

计算：

```text
interval_mean = avg(Δt)
interval_cv = stddev(Δt) / avg(Δt)
outbound_bytes_cv = stddev(单次上行量) / avg(单次上行量)
duration_cv = stddev(连接时长) / avg(连接时长)
```

初始规则：

```text
连接次数 >= 20
AND 平均间隔在 30～3600 秒
AND interval_cv < 0.15
AND 目的地为企业首次出现或未批准目标
```

周期性本身只生成低至中风险原子信号。只有“周期稳定”与“企业首次出现或未批准目的地”同时成立，才形成周期未知外联场景；若再叠加罕见 TLS 指纹、无 SNI、异常上行或协议端口不匹配，可升级为高风险。软件更新、监控、备份和终端安全产品也可能产生周期通信，因此周期性必须与目的地和资产上下文组合判断。

### 7.7 跨场景叠加与序列检测

以下场景体现“多个异常行为叠加后才符合一个场景”的原则。所有条件均需在同一关联键和限定窗口内满足。

| 场景 | 关联键与窗口 | 必要条件 | 异常条件/增强条件 | 结论 |
| --- | --- | --- | --- | --- |
| 疑似隐蔽控制通道 | 资产 + 目的地，1 小时 | 周期未知外联 | 非典型端口 TLS、无 SNI、罕见 TLS 指纹、目的地企业首次出现中至少 1 项 | 形成中高风险场景 |
| 疑似分阶段外传 | 资产，2 小时 | DNS 隧道信号或隐蔽控制通道 | 后续异常外联，且上行量超过主机和同类 P99 | 形成高风险案件 |
| 疑似内网探测后外联 | 资产，2 小时 | 横向扫描场景 | 之后出现新外部目的地、周期通信或异常上行任一项 | 形成需优先调查的序列案件 |
| 非典型 TLS 大流量外传 | 资产 + 目的地，1 小时 | 非典型端口 TLS | 未批准目的地 + 上行量基线异常；直连 IP/无 SNI/罕见指纹任一项可增强 | 形成中高风险场景 |

序列匹配必须保留每一步的开始/结束时间、原子信号 ID 和原始日志引用；后续步骤缺失时，不应把未完成序列升级为完整攻击链。

### 7.8 严重度、置信度与案件优先级

严重度表示场景潜在影响，置信度表示证据是否充分，案件优先级则决定谁先处置，三者不能混为一个分数。推荐计算方式：

```text
案件优先级 = 场景基础风险
           + 关键资产/业务区域影响
           + 多源证据一致性（DNS、连接、TLS 等）
           + 完整组合或序列命中
           + 基线或模型异常增强
           - 有效例外、已批准变更
           - 数据缺失、资产归属不确定
```

高置信协议端口违规和完整高风险序列可直接告警；单个低置信基线异常或仅有模型高分，应保存为信号并等待后续证据，不自动判为高危。

## 8. 主机基线

每个指标同时与两套基线比较：

```text
当前主机 vs 自身过去 7/30 天
当前主机 vs 同角色、同区域主机
```

推荐保存：

```text
median：中位数
P95/P99：历史高分位数
MAD：中位数绝对偏差
sample_count：有效样本数
```

对流量、连接数等长尾指标，可使用：

```text
当前值 > max(自身 P99, 同类 P99)
AND 当前值 > 最小绝对门槛
```

对较稳定指标，可使用 Robust Z-score：

```text
robust_z = 0.6745 × (当前值 - 中位数) / MAD
```

基线命中输出 `baseline_anomaly` 信号，并附带当前值、比较对象、分位数或偏离度、样本量和基线版本；除非场景明确规定，不能单独作为高危告警。基线需要区分工作日/非工作日、工作时间/非工作时间。办公终端、服务器、跳板机、扫描器等资产也不能放在同一个基线组中。

冷启动时遵循以下降级顺序：主机自身历史充足时使用“自身 + 同类”双基线；历史不足时仅使用同角色、同区域基线；同类样本仍不足时只记录目的地首次出现或数据不足，不自动升级为高风险。

## 9. 孤立森林

孤立森林用于发现多项指标组合起来很罕见的窗口样本，作为固定规则和统计基线的补充。

建议模型输入一条“资产 1 小时画像”：

```json
{
  "feature_set_id": "asset-hour-v1",
  "window_start": "2026-09-02T09:00:00Z",
  "window_end": "2026-09-02T10:00:00Z",
  "asset_id": "PC-023",
  "model_group": "office_endpoint|office|non_workhour",
  "features": {
    "outbound_bytes_log": 19.25,
    "upload_ratio_log": 2.91,
    "connection_count_log": 6.42,
    "new_destination_ratio": 0.38,
    "failed_connection_ratio": 0.12,
    "dns_nxdomain_ratio": 0.08,
    "dns_high_entropy_ratio": 0.21,
    "tls_no_sni_ratio": 0.17,
    "rare_tls_fingerprint_ratio": 0.09,
    "periodic_destination_count_log": 1.10
  },
  "completeness_ratio": 0.98
}
```

字段说明：

| 字段 | 含义 |
| --- | --- |
| `feature_set_id` | 特征集合及版本，保证训练和推理字段一致 |
| `window_start` | 模型样本窗口开始时间 |
| `window_end` | 模型样本窗口结束时间 |
| `asset_id` | 被评分资产 ID；用于归属，不直接作为数值特征 |
| `model_group` | 模型分组，由资产角色、区域和时间类型组成 |
| `features` | 输入孤立森林的数值特征集合 |
| `outbound_bytes_log` | 对窗口上行字节数进行 `log1p` 转换后的值 |
| `upload_ratio_log` | 对上行/下行比例进行 `log1p` 转换后的值 |
| `connection_count_log` | 对窗口连接数进行 `log1p` 转换后的值 |
| `new_destination_ratio` | 新目的地数占全部目的地数的比例 |
| `failed_connection_ratio` | 失败连接比例 |
| `dns_nxdomain_ratio` | NXDOMAIN 查询比例 |
| `dns_high_entropy_ratio` | 高熵子域查询比例 |
| `tls_no_sni_ratio` | 无 SNI 的 TLS 连接比例 |
| `rare_tls_fingerprint_ratio` | 罕见 TLS 指纹连接比例 |
| `periodic_destination_count_log` | 对周期通信目的地数量进行 `log1p` 转换后的值 |
| `completeness_ratio` | 该窗口必需数据完整率；低于要求时不进行评分 |

模型按资产角色、网络区域和时间类型分组训练，不建议每台主机单独训练。训练数据使用最近 30～60 天的干净窗口，排除已确认攻击、采集故障、变更窗口和明显规则告警。

模型输出可统一校准为 0～100 异常分，便于排序：

```text
0～69：仅保存
70～84：低风险模型辅助信号
85～94：中风险模型辅助信号
95～100：高异常度模型辅助信号，仍需场景证据确认
```

模型高分不表示攻击概率，也不应单独产生“高风险模型事件”。模型结果应与固定规则、基线偏差、目的地新颖度和例外策略共同计算案件优先级；高置信度固定规则不能因模型分数低而被取消。

## 10. 原子信号、场景告警与案件

场景告警对象示例：

```json
{
  "event_id": "det-net-20260902-001",
  "event_type": "periodic_unknown_connection",
  "scenario_id": "NET-PERIODIC-UNKNOWN-001",
  "scenario_version": 1,
  "detection_method": "composite_pattern",
  "severity": "high",
  "confidence": 0.86,
  "window_start": "2026-09-02T09:00:00Z",
  "window_end": "2026-09-02T10:00:00Z",
  "asset_id": "PC-023",
  "destination_key": "domain:example.com",
  "case_priority": 88,
  "matched_signal_ids": ["sig-periodic-001", "sig-new-destination-002"],
  "source_event_refs": ["zeek-gw-01-C8abc"],
  "exception_matched": false,
  "data_quality": {"required_fields_complete": true, "completeness_ratio": 0.98}
}
```

字段说明：

| 字段 | 含义 |
| --- | --- |
| `event_id` | 检测告警唯一 ID |
| `event_type` | 异常场景类型 |
| `scenario_id/version` | 命中的场景及其版本，用于回放、审计和调优 |
| `detection_method` | 固定规则、基线、组合、序列或模型 |
| `severity` | 告警严重度 |
| `confidence` | 检测成立的置信度，不等同于攻击概率 |
| `window_start` | 告警统计窗口开始时间 |
| `window_end` | 告警统计窗口结束时间 |
| `asset_id` | 告警关联资产 |
| `destination_key` | 告警关联的标准化目的地 |
| `case_priority` | 已成立场景的处置优先级，不是攻击概率或统一判定阈值 |
| `matched_signal_ids` | 组成场景的原子信号 ID，保留信号之间的关系 |
| `source_event_refs` | 支撑告警的原始事件引用 |
| `exception_matched` | 是否命中有效例外策略 |
| `data_quality` | 必填字段完整性和数据质量，用于决定是否允许自动升级 |

固定规则、基线、窗口统计和模型首先产生原子信号；满足第 7 节的组合或序列条件后才生成场景告警。多个已成立的场景告警再按“资产 + 目的地 + 时间范围”合并成案件。例如：

```text
企业首次出现目的地
→ 罕见 TLS 指纹
→ 固定周期通信
→ 上行量超过主机和同类 P99
→ 孤立森林异常分 97（增强证据）
```

案件应展示完整时间线、规则依据、基线偏差、模型版本、异常分、例外检查和原始日志证据。

## 11. ELK 存储与展示

推荐索引：

```text
logs-zeek.conn-*          Zeek 连接事件
logs-zeek.dns-*           Zeek DNS 事件
logs-zeek.tls-*           Zeek TLS 事件
net-dns-feature-*         DNS 窗口特征
net-conn-feature-*        扫描、外联、周期通信特征
net-asset-profile         主机和同类基线
net-destination-profile   目的地首次出现和使用范围
net-ml-score-*            孤立森林评分
net-detection-signal-*    固定规则、基线、窗口统计和模型的原子信号
net-detection-event-*     已命中的组合或序列场景告警
net-risk-case-*           风险案件
```

Kibana 至少提供：

- 采集健康：事件量、延迟、解析失败和字段缺失；
- 网络概览：协议、流量、目的地和 DNS/TLS 趋势；
- 异常检测：规则、基线和孤立森林高分样本；
- 案件调查：时间线、资产、目的地和原始日志回查。

## 12. 实施顺序

1. 部署 Zeek 并采集 `conn`、`dns`、`tls` 日志；
2. 用 Filebeat 和 Logstash 建立最小统一事件模型；
3. 接入资产、网段、端口协议、批准目的地和例外数据；
4. 实现协议端口不匹配和横向扫描；
5. 实现 DNS 5 分钟滑动窗口、特征提取和 DNS 隧道原子信号；
6. 建立目的地画像，实现企业和主机首次出现判断；
7. 实现异常外联和周期通信的 1 小时窗口；
8. 建立主机自身与同类主机基线；
9. 使用小时窗口特征训练第一版孤立森林；
10. 建立原子信号、组合场景和序列场景的版本化配置，并记录命中条件与排除条件；
11. 将已成立场景告警合并为风险案件，使用案件优先级决定处置顺序；
12. 通过人工反馈持续调整阈值、白名单、基线分组、场景条件和模型。
